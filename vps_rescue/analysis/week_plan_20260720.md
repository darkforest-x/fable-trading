# 一周执行计划(2026-07-20 → 07-27)— 交给 Grok 执行版

目标一句话:**让 v12(H-TIP)通过诚实评测后接管检测层,使实时 tip 信号真正流进
实盘,开始积累 100 笔新鲜裁决。** 实时管道已就绪(HANDOFF 顶部"当前真相"),
唯一缺的是会在盘口开火的检测器。

## 当前状态(接手时核对) — 2026-07-20 夜更新

- **D1 完成**: tip_hit **0.925** / frozen-F1 **0.650** → 双门过。
- **Owner 强制检测主线 = v12**（`owner_best.pt`）；判断仍 v11 freeze；**未** holdout。
  见 `analysis/p2a_v12_mainline_cutover.md`。回滚：`owner_best_pre_v12.pt`。
- D2 影子代码仍可用；主线已是 v12 后影子变为可选对照。
- 裁决 0/100 继续积累（时钟未清）。

## D1:v12 评测 — ✅ 2026-07-20

1. ~~等训练~~ ep32 early stop + 自动 tip/F1。
2. **通过线**: tip ≥0.20 → 0.925；F1 回撤 ≤0.03 → −0.008。
3. 不达标路径未触发。

## D2-D3:v12 影子验证(不动主线,不耗 holdout) — 🔧 代码就绪 / 待 VPS 48h

1. VPS:`scp models/owner_v12_htip.pt` → 设 `FABLE_V12_SHADOW=1`（systemd 环境或
   drop-in）。tip-only 单窗；禁止全窗影子。
2. 日志:`data/forward_log_v12_shadow.csv`（防呆拒写主线）。
3. 48h 后:`analysis/p_v12_shadow_48h.md`（tip 数 / 与主线重合 / 分数 / 诚实声明）。

## D4-D5:切换决策(owner 决策点,不是 Grok 的)

- 影子数据好(有真实 tip 检出、分布不异常)→ 向 owner 申请:
  a) v12 池全量重建 + judgment 重冻结 + accept 回测 = **holdout 第 6 次消耗,必须
  owner 在对话中明确批准并记账**;b) 批准后按 v10→v11 切换先例执行
  (`analysis/p3_v11_pool_cutover.md` 是模板)。
- 影子数据差 → 不切换,走备选路线,v11 继续守主线(反正它的事后检出进不了裁决)。

## 备选路线(v12 不达标时,按顺序)

1. H-TIP 加强:tip 渲染样本加倍/tip-only 微调(仍用现有标注,无需 owner 打标)。
2. **盘口视角打标**:出"右缘=信号 bar、无后文"的打标包给 owner(这是 owner 最初
   就提出的打标方式,是终极修正);300 张起,走 Label Studio 现有流程。
3. 短周期支线(src/short_tf/)只许继续观察,不许接 executor。

## 全周被动任务

- 裁决积累:0/100 → 只看不动,不清账本、不调门。
- 每日看一眼 live_health 摘要与脉冲耗时(discover_wall 若 >600s 要查)。

## 红线(违反 = 返工)

1. holdout(≥2026-05-04)任何动用先问 owner,记"第 N 次消耗"(当前 N=5)。
2. 新鲜度三门 30min 是从管道时序推导的(见 docs/learnings/freshness-gates-*.md),
   改动必须附延迟预算表且三处同改。
3. 脉冲必须 <15min:禁止加扫描窗、禁止在脉冲里塞新任务;性能杠杆见 HANDOFF。
4. VPS 是 K 线唯一写者:deploy 不推 data/kline_fetched(脚本已改,别改回去)。
5. 阈值/成本假设/障碍参数 = owner 决策;forward_log 不清空。
6. 不自动 promote 任何模型;`models/ACTIVE` 与 frozen 默认配置的切换要 owner 点头。
7. 报告必须含"风险与诚实声明";只报好消息 = 污染实验日志。

## Owner 个人待办(提醒,非 Grok 任务)

- OKX API key 已在聊天中暴露过:轮换 + 关提币 + IP 白名单(仍未确认完成)。
- OKX 里核对是否还有无保护 DOGE 仓位残留。
