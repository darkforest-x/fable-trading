# SPIKE V6 + original WVF reclaim pre-registration — 2026-09-12

## Status

This is the implemented, unrun pre-registration. No market-data replay,
economic result, random control, holdout score, model training, Pine update,
or production action occurred in this step. The formal source mapping is still
being frozen by the root agent, so no result table is fabricated here.

## Fixed intervention

Variants A, B12 and C24 are defined in
`experiments/active/exp-spike-v6-wvf-20260912-v1/PROJECT_PLAN.md` and
`config.json`. B12 and C24 apply exactly one original CM Williams Vix Fix
long-entry background condition. `ph=0.85` means 85% of a 50-bar rolling WVF
maximum; it is not a quantile. V6 raw opposite confirmations remain exits for
all variants, including when a long entry is rejected by WVF.

The old Freqtrade bridge cannot supply this comparison's exit behavior:
`FrozenV6Bridge` disables exit signals and does not populate an exit trend.
The new study model therefore fixes the currently requested next-open opposite
V6 exit convention directly. Its stop/trail convention is the frozen five-bar
extreme plus/minus 0.2 ATR, minimum 2 ATR risk, close-at-2R activation, and
next-bar 4 ATR protection with 0.2% round-trip cost.

## Reproduction and intended data split

The frozen pool is eight preselected OKX swaps: BTC, ETH, SOL, XRP, DOGE, PEPE,
SOPH and USELESS. It reads the existing 30-minute normalized inputs from the
previous two-year study, aggregates complete 1H/4H bars, and takes each
instrument tick from the frozen source catalog. Evaluation is
2024-09-10 through 2026-09-10 exclusive; development ends 2025-09-10 and later
rows are validation. The realized history start may be later for SOPH/USELESS
and will be reported, not backfilled. It is a fixed mainstream plus
owner-interest altcoin pool, not an all-market claim.

Before any formal run, root must commit this builder and record input hashes and
per-symbol ticks in the experiment receipt. Then run:

```bash
pytest -q tests/test_spike_v6_wvf_study.py
python3 scripts/md_to_html.py analysis/p1_spike_v6_wvf_20260912.md --out-dir analysis/html
```

The formal evaluator will use time splits only. Rows at or after 2026-05-04,
if selected, are this configuration's authorized research evaluation #1 and
must be reported as previously readable research history, not a new blind
holdout. Candidate counts, positive rate, time ranges and validation counts
are pending the frozen mapping.

## Results and controls

There are no results yet. When run, each variant will include all raw signals
and actual single-position trades by coin/period, split and side; win rate,
profit factor, net R, gross/net return, 1x-notional maximum drawdown, baseline
10R retention/miss, MFE>=10R, failure exclusion and deleted winners. A matched
random-entry null will use 99 deterministic seeds and match coin, time block
and causal volatility bucket under identical stop/cost execution. AUC,
permutation p and top-decile performance do not apply: no prediction score or
ranking is fitted in this filter study.

## Risk and honest statement

Strict filtering can simply reduce activity, costs, and tail winners; a better
selected trade ledger would not prove a better portfolio without the complete
raw signal ledger, common unfiltered exit policy, single-position replay and
matched controls. C24 is not a blind choice opportunity after B12; it remains
a named sensitivity result. No short WVF semantic interpretation is made.

## Next decision

Root agent must provide the frozen source/data mapping and commit reference for
the formal single-process replay. No strategy conclusion or promotion is
available from this pre-registration.
