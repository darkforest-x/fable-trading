# spike：指标启动后 YOLO 确认已接入

**已按 Owner 授权接入 Mac 的 15m / 1H / 4H 前端与 TG、Bark。**
指标箭头先登记候选，模型在原箭头至之后9根收盘内同方向确认，才产生通知信号。
运行页面：[spike](http://127.0.0.1:8766/#signals)。详细规则见 `docs/ops/SPIKE_MODEL_CONFIRMATION.md`。

## 与上一版对照

|项目|上一版|当前1.6.0|
|---|---|---|
|通知条件|IMACD可见启动箭头|启动箭头＋固定YOLO确认|
|等待期间|没有模型候选状态|待确认、检测异常、结构失效、超时|
|信号时间与点位|原箭头收盘|保留原箭头，并另列模型确认收盘|
|前端|主图启动列表|模型确认、等待确认、全部候选|
|TG图片|指标与六均线|另有紫色模型核心、原箭头及确认竖线|
|模型异常|不适用|不放行原始箭头，保留异常重试|
|通知新鲜度|原箭头收盘后30分钟|确认收盘后30分钟|

同根主线回零/反向优先取消；没有额外改变密集、高周期过滤或阈值。
模型与参数沿用已展示报告：18/19根原白底输入，核心4/5根，核心后2–9根，与原蓄势段至少重叠1根；置信门槛0.25。
原始事件和派生事件分别保存，确认与两路通知入队是一个事务；重启不重新推送。

## 验证与数据统计

运行实现源码 `8f0c1dc`：**514项Python监控与边界检查、38项前端/主题检查通过**。
独立运维审计工具另有26项检查通过（`fe36064`）。
真实权重兼容runner `4779754`先提交后运行：只选择三个已发布正例，没有重新筛选币种或收益样本。

|已知样例|原箭头到模型确认|真实模型输入与旧记录|临时TG/Bark|
|---|---|---|---|
|ETH 15m · 1778263200|先pending，下一根confirmed|像素SHA、核心、时钟完全一致|各1次，重跑0次|
|ETH 1H · 1778572800|同根空头确认|同上|同上|
|ETC 4H · 1777910400|同根多头确认|同上|同上|

共8张真实MPS模型输入。各端点只解析当时及之前的OHLC；未向真实TG/Bark发送这些历史验证样例。
三例是已知正例，3/3只表示接线兼容性，不是模型真实准确率或信号胜率。
原决策日期在2026-05-04至05-12内；这是本在线接入配置第1次复用已消费holdout的工程复核。
Owner已在本对话明确允许任意时间段数据。无新训练、调参或收益评分，val样本数不适用。

浏览器实测：模型/候选三页签、三个周期、真实BICO 4H站内120根K线、六均线、零轴、放大/Esc、深浅主题通过。
390×844窄屏scrollWidth=390，无横向溢出；浏览器error/warn日志为空；恢复系统主题。
上线初检页面如实为0个新确认，并未伪造已确认卡片的实盘图。紫框、双时钟及确认卡片由软件负控和真实样例后端复核覆盖。

## 本机运行验收

21:28:54首轮预热完成：473个合约、1419个周期窗口，耗时628.33秒，0扫描错误。
健康检查ok=true，模型就绪于MPS，固定SHA一致；77个新/短历史窗口尚不足指标预热，未伪造信号。
本协议初检0个新确认、TG/Bark各0发送、0失败、0未知；旧TG201笔、Bark143笔回执仍是历史。
源码/启用时间/回执/身份检查保存在实验results目录，重复身份组0。
21:32:10首轮增量扫描完成，75.52秒，1419/1419、0错误。真实SUI-USD-SWAP 15m空头原箭头收盘21:30，0.8097，已检查2张模型输入但未通过，状态pending；没有TG/Bark推送。
这证明实机原箭头→候选→模型检查链路运行，尚未观察到本新协议的自然确认推送；真实正例通知路径通过临时数据库及模拟发送验收。

实际pending页面在Chrome及IAB均复核：SUI原箭头、29根蓄势、0/9等待、23:45截止与API一致；不显示模型确认线、紫框、通知回执或新鲜徽章。IAB旧页面已刷新至新版。

## 仓库既有校验差异

额外执行因果/迁移校验：301项通过，2项旧迁移哈希校验失败，分别是`yoyo/layers/l1_detection/candidates.py`和`render.py`；本次未修改这两个文件。
`render.py`在本次实现之前的提交与当前文件SHA均为`def5b7317c13e37fc43657243a7cc900bc3fafee71bdf2e405e0336a1a3c0eea`；本次三例模型输入另已逐像素核对。
登记schema/引用加载通过；固定holdout消费白名单在HEAD基线已漏13个旧实验，本次只明确增加自己这一个已授权配置，未把其它缺项自动放行。
因此不能声称整仓所有测试全绿；上述是已有台账差异，未更改它们的守门标准或隐藏失败。

## 对照与因果校验

本轮为非方向性工程验收：AUC、置换p值、top-decile毛/净收益、胜率、单特征收益基线及匹配随机入场不适用。
等价的严格反例控制是固定输入逐项破坏：反方向、错误权重/像素证明、缺检测ID、时间整体偏移1ms、非同一蓄势段、同根md取消、未收盘输入、超时、切换前旧箭头，均不得进入通知。
未来OHLC扰动不能改变此前模型输入；推理失败不推进检查游标；缓存同一根重试可恢复；同一端点多个原箭头不被误合并。

## 复现

需保留原仓已安装依赖、固定权重及既有`data/kline_deep`和三份报告决策/提案文件；不安装或替换库。

```bash
.venv/bin/python -m pytest tests/monitor tests/boundaries -q
node --test tests/monitor/frontend_cards.test.cjs tests/monitor/frontend_theme.test.cjs
.venv/bin/python tests/monitor/replay_model_known_examples.py
.venv/bin/python -m yoyo.monitor.acceptance --label spike-yolo-live --output experiments/active/exp-spike-yolo-monitor-20260908-v1/results/live_acceptance.json
python3 scripts/md_to_html.py analysis/p0_spike_yolo_monitor_20260908.md --out-dir analysis/html
```

部署用既有LaunchAgent重启一次，启动约21:18:26北京时间。原指标、ACTIVE、VPS、实盘执行和凭据均未修改。
原自动巡检仍是暂停状态，只更新了旧巡检说明里的协议，防止未来启用时按旧规则误判。

## 风险与诚实声明

三例不能证明去噪率或收益提高；此前后续走势报告中匹配超额未证明为正，本轮不改写该结论。
15m权重复用于1H/4H沿用已授权冻结方案，不等于证明跨周期盈利。
未来72根只属于旧报告的事后观察，当前模型和通知图不会读取它们。
重启会从新720根起点重新播种递归指标，边界像素可能轻微变化；持久化原箭头、进度和回执不等于重新计算结果逐字节相同。
Mac必须开机联网；扫描或模型队列过久会使真实确认超出30分钟，从而跳过推送，不会伪装成刚发生。

## 下一步

继续按此固定配置积累真实模型确认、等待时间与后续走势；盈利、假启动率需另行设计固定样本外评估，不能用这三个已知正例代替。
涉及模型、确认阈值或实盘执行的改动需Owner另行决定。
