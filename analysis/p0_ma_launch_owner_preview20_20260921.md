# 六均线收拢后向上启动：20 张真实行情预览

已交付 20 个不同币种的真实 OKX 历史事件，13 个 1H、6 个 5m、1 个 3m。每例含 1600×900 完整走势图、独立早期视图和可追溯 OHLC/均线数值。红圈为待确认的建议启动区域；没有生成 YOLO 金标、划分训练集或进行训练。

主入口：`experiments/active/exp-ma-launch-owner-preview20-20260921-v1/preview_v2/index.html`。

## 来源与范围

- Owner 2026-09-21 提供 MUBARAK 5m/3m、WIF 1H 四张参考截图。共同视觉目标为长背景整理、六线收拢、价格向上脱离与后续展开。
- 数据为 `plan_v2.json` 冻结列出的 70 个现有 OKX 本机缓存，包含 54 个 1H、14 个 5m、2 个 3m。只读，没有生成虚构价格或联网补数。
- 描述性检索得到 805 个候选锚点，再按每币一例、同 UTC 日期最多两例及视觉排除得到 20 例。候选不等于独立行情事件数。
- SMA/EMA20/60/120 从源文件完整前缀计算；蓝涨紫跌、灰蓝紫均线与截图配置对应。每个导出 CSV 含启动前 900 根预热与后续 40 根。
- 完整图展示前 140 根与建议启动根至后第 40 根，共 181 根；早期图截止后第 3 根，共 144 根，价格轴只使用此前已存在的数据。
- **选择过程明确使用后续 40 根**。早期图不含未来像素，不代表样本选择本身无后见偏差。两套图片物理分目录，均没有训练 labels。

## 初版与交付版

|项目|preview_v1|preview_v2（交付）|
|---|---:|---:|
|规则候选锚点|805|805，候选 JSON 完全相同|
|事件 / 不同币种|20 / 20|20 / 20|
|1H / 5m / 3m|13 / 7 / 0|13 / 6 / 1|
|完整图 / 早期图|20 / 20|20 / 20|
|人工复核排除|无|OKB 与 BTC5m 各一处小试探事件及同源 ±40 根邻域|
|新增替补|无|另一时段 BTC3m 与 NEAR1H|
|Owner 逐样本确认|未取得|未取得|
|training / production eligible|false / false|false / false|

初版图、manifest 与 plan 全部保留。最终次序是检索分数次序，不是收益、胜率或训练质量排名。v1 对不合格下跌窗口计算 log1p 时出现数值域警告；v2 将评分自变量下限限定为零，两个版本全部 805 个合格候选的内容逐项完全相同。

## 验证

- 20/20 导出 OHLC 与对应源文件区间数值一致；源文件 SHA 与扫描时记录一致。
- 40/40 PNG 解码、1600×900 尺寸、SHA 通过，40 个文件哈希互不相同。
- 1H、5m、3m 各选一例，物理删除启动后第 3 根之后所有数据并重新计算均线/绘图，三张早期图片均逐字节相同。没有将此抽查称作 20 例全量因果重渲染。
- 事件联结零假设对照：真实 CSV 锚点联结 20/20；将 20 个导出窗口循环错配后为 0/20。仅检验事件身份联结，不检验形态是否符合 Owner 定义。
- 全部完整图通过两页总览逐张视觉检查；两个替补另行复核。浏览器已显示 20 个条目，完整/早期切换时图片、链接与说明同步改变。
- 层间 import 门：71 passed。

这是非方向性的参考图预览，val 样本数、val AUC、收益置换 p、top-decile 毛/净收益、胜率、单特征交易基线和匹配随机入场对照均不适用。没有收益实验，也没有用上涨外观替代这些指标。

## 复现命令

已有源文件齐备时，从仓库根目录运行；使用未存在的新输出名，旧产物不会被覆盖：

```bash
.venv/bin/python -m yoyo.datasets.ma_launch_preview20 \
  --plan-name plan_v2.json --output-name preview_repro_01
.venv/bin/pytest -q tests/boundaries/test_layer_imports.py
.venv/bin/python -m http.server 8784 --bind 127.0.0.1 \
  --directory experiments/active/exp-ma-launch-owner-preview20-20260921-v1/preview_v2
```

原版 builder/plan 在 `dddfd50a6503d33eea84d6e34ce716b12fd3d3bf` 先提交后生成；视觉排除与数值域修正在 `05c4b359b56ae25b6722ccfc9b294ab482136ba5` 先提交后生成。精确源路径及 SHA、输出 manifest、构建 receipt 与 QA receipt 均在交付目录。

## 风险与诚实声明

这 20 张是按已经完成的上涨形态挑出的正例候选，不是市场随机样本，不含经 Owner 裁决的负例，也不证明检测器可提前识别或产生交易收益。红圈范围不等于最终 YOLO 框。自动锚点仍需 Owner 看图确认；本轮只排除了已观察到的两个明显错位事件，没有声称其余全为完美金标。

未来若做训练数据，应先确认这批整体形态与启动时间，再建立不含后文的输入、对应标签及匹配反例。该步骤和新训练尚未在本轮实施。
