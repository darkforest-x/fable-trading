# A 股 1h / 会话 4h 多头扫描：数据源预检失败（2026-09-02）

## 结论先行

本轮**没有产生信号结果，也不能解释为“零多头”**。冻结的 Eastmoney 60m 接口虽然接受
`lmt=1024`，上证综指参考请求实际仅返回 **127 根**（2026-07-21 10:30 至
2026-09-02 14:00 CST）。它低于 1h 完整模型图的最低 warmup **138 根**，更远低于
预注册的 160 个完整 4h 交易日所需 **640 根 60m 源 bar**。管道因此在请求任何个股、
加载模型、运行推理之前 fail-closed。

实验：`exp-ashare-grade-a-yolo-1h4h-long-20260902-v1`。这是该 Grade-A checkpoint 的
1h 配置 **holdout 使用 #9**、会话 4h 配置 **holdout 使用 #10**；失败仍计消费。

## Owner 口径与冻结定义

Owner 原话：

> 给我跑 4h  1h 周期的  只保留多头，  4h 周期可以接受信号已经出来几天的

预注册后才发起网络请求：

- 普通账户池：冻结 A 股身份表中的沪深主板，排除科创板、创业板、北交所、未知板块、
  `ST/*ST/S*ST/SST`、`PT` 与名称含“退”的证券，共 **3,111** 只；SH 主板 1,666，
  SZ 主板 1,445。
- 1h：只看 2026-09-02 14:00 CST 已完成端点。
- 会话 4h：同一交易日严格聚合 10:30、11:30、14:00、15:00 四根 60m bar，
  `open_time=09:30`、`available_at=15:00`；当前未收盘日不拼部分 4h，回看最近 5 个
  完整交易日。
- 模型：Grade-A full40 native-1280，权重 SHA
  `862705b999594355c1133640acc540f4de19b561889e89d9e050ddad5c6db838`；
  conf 0.25、NMS 0.70、W18/W19、core4/5、post2–9 与语义门全部不变。
- 内部原计划审计 LONG/SHORT，交付只保留 LONG；本轮没有进入模型阶段。

## 数据预检结果

| 项目 | 上一轮 A 股 15m #8 | 本轮 60m #9/#10 | 冻结要求 | 裁决 |
|---|---:|---:|---:|---|
| 参考请求目标根数 | 512 | 1,024 | 按配置 | — |
| 参考实际保留根数 | 504 | **127** | 1h 至少 138；预注册 160 | **失败** |
| 完整交易日等价数 | 31.5 | **31 + 当日 3 根** | 4h 160 日 | **失败** |
| 普通账户候选身份 | 后处理口径 | 3,111 | 3,111 | 通过 |
| 已请求个股 | 5,908 | **0** | 先参考预检 | 正确止损 |
| 模型窗口 | 10,988 | **0** | 数据通过后才允许 | 未运行 |
| 多头/空头事件 | 8 / 23（全市场 OOD） | **未计算 / 未计算** | 不得写成 0 / 0 | 未运行 |

127 根只覆盖预注册 1h 时序门的 79.4%；完整 4h 交易日只覆盖 160 日要求的 19.4%。
最长均线为 MA120，而 W19 图要求窗口内每个位置均有完整均线，因此最少需要
`120 + 19 - 1 = 138` 根 1h bar。降低时序门、容忍图中均线缺失或把日线冒充连续墙钟 4h
都会改变已冻结输入合同，本轮没有这样做。

保存的参考 CSV：
`analysis/output/ashare_1h4h_long_20260902_v1.building/reference_60m.csv`，SHA-256：
`e5a20ff3d8f63bd0301dab6c778abf811f66fa323cc135f181ca4afb012dc3e5`。

失败凭据：
`experiments/active/exp-ashare-grade-a-yolo-1h4h-long-20260902-v1/source_preflight_failure.json`。

## 指标与零假设对照

这是数据能力预检，不是方向性收益实验。没有未来 A 股标签、入场、障碍或模型推理，因此
val AUC、置换检验 p、top-decile 毛/净收益、胜率、单特征基线和匹配随机入场对照均**不适用**；
编造这些指标会把“未运行”伪装成模型结论。

同等严格的确定性零假设是：**同源参考响应满足预注册的实际根数与精确时序门**。验收直接比较
保存响应，而不是相信请求参数：`127 < 138 < 160`，且仅 31 个完整会话日 `< 160`，所以零假设
被确定性拒绝，不需要也不存在随机 p 值。方向翻转语义对照只有在结构框存在后才有定义，本轮
结构框为“未计算”，不是零。

## 复现命令

```bash
cd /Users/zhangzc/fable-trading
git show --stat 770f5f99556244c688169f53c6874066d06b5667
python3 -m pytest -q \
  tests/test_scan_ashare_yolo_1h4h_long.py \
  tests/test_filter_ashare_signals_for_standard_retail.py \
  tests/boundaries/test_layer_imports.py
python3 scripts/scan_ashare_yolo_1h4h_long.py --fetch --workers 12
```

最后一条会复用已保存参考文件，并在 `1h_insufficient_history:127` 处再次 fail-closed；它不会
请求个股或加载模型。

## 风险与诚实声明

- [AKShare 股票数据文档](https://akshare.akfamily.xyz/data/stock/stock.html)将 Eastmoney 分时接口描述为只能获取近期数据；`lmt` 是请求参数，不是上游历史
  覆盖承诺。本地同源旧 `end` 探测被断开连接，不能假设可向前分页。
- 即使换成长历史源，模型仍是 crypto-15m → A 股 1h/4h 的双重 OOD，只能叫研究命中，
  不能叫经过验证的买入信号；置信度不是胜率。
- 本轮没有训练、调阈值、改 ACTIVE/frozen、promote、部署、Telegram 发送或下单。
- 两个消费已经发生；换供应商或改 warmup 是新数据配置，不能沿用 #9/#10 冒充原配置成功。

## 下一步选项（需要 Owner 决策）

1. **建议：授权 #11/#12，改用 AKShare 已文档化的新浪 60m 源。** 其实现请求
   `datalen=1970`，理论上足够 1h warmup 和 160 个 4h 交易日；仍需先只对参考指数做实际根数、
   时段、QFQ 与 Eastmoney 重叠区 parity 预检，通过后才扇出 3,111 只票。
2. 停止，不再消费 holdout。#9/#10 仅保留为数据源失败记录。

若选 1，必须重新冻结来源实现 SHA、QFQ 方法、参考时序、覆盖门和消费编号；不会在看过模型结果
后改阈值或回退到更宽板块。
