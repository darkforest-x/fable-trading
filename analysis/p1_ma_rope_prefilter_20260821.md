# P1 · 六均线“拧成一股绳”代码预筛与数据扩充入口（2026-08-21）

## 先说结论

代码预筛已经完成，并生成了两个可操作入口：

1. **旧训练实际使用的 1,345 张**：A 核心 385、B 扩展 575、C 其余 385；页面默认只看
   A 核心，先把最像 Owner 参考图的 385 张快速过一遍。
2. **完整方向复核池 2,525 个框**：A 核心 744、B 扩展 1,067、C 其余 714；保留
   long 1,152 / short 1,361 / skip 12，可作为“更多数据”的现成候选池。

但这套代码**不能自动替 Owner 删除或保留**。在独立的 390 条人工 keep/drop 反证集上，
`rope_score` AUC **0.489**、单侧标签置换 **p=0.635**；A+B 的 precision **18.21%**，与
不筛时的 keep base rate **18.21%** 几乎完全相同。正确用法是**排序、减少第一轮工作量**，
不是把低分图直接删掉。

本轮未训练、未改 split/label、未改 `training_eligible`、未 promote。正式评分、阈值校准与 390 条
反证全部停在 `<2026-05-04`；Owner 另行明确要求查看的最近 BTC/ETH 被单列为已消费的设计示例，
不冒充未见 holdout 或最终验收。

## 1. Owner 参考图到底定义了什么

截图左上角明确是“20 / 60 / 120”双均线，仓库渲染合同对应六条线：

- SMA20 / SMA60 / SMA120；
- EMA20 / EMA60 / EMA120。

需要同时满足的视觉因素不是“六条线距离近”一个变量，而是：

1. 六线带宽窄：`(max(MA)-min(MA))/close`；
2. 六条线在近 12 根内反复换序/交叉；
3. K 线实体近 12 根内反复接触、完整穿过均线束；
4. 密集具有持续性，而不是偶然一根重合；
5. 当前在收紧或尚未大幅散开。

全部特征只使用当前决策 bar 及以前数据，方向无关，不读取上涨/下跌结果。最终显式权重为：

| 分量 | 权重 |
|---|---:|
| 六线窄带 | 0.35 |
| 六线交叉/换序 | 0.30 |
| K 线实体与均线束交互 | 0.25 |
| 密集持续率 | 0.05 |
| 继续收紧 | 0.05 |

斜率一致性只保留为诊断，权重固定为 0，避免“六线平行上涨/下跌”反而拿高分。

## 2. 这 1,345 张从哪里来

“它是不是以前一万多张里手动标出来的？”——**是，但 1,345 是层层收窄后的空头训练子集，
不是最初全部人工正例。**

本轮重新读取 15 份 canonical Label Studio export，谱系闭合如下：

| 层级 | 数量 | 含义 |
|---|---:|---|
| Label Studio task rows | 13,621 | round1–9 canonical exports 的任务行 |
| 完成人工 annotation | 12,684 | 全部 `completed_by=2`，同一个 Owner 账号 |
| 唯一人工看过的图 | 12,565 | 去除 export 间重复 stem |
| 人工矩形框 | 6,291 | 所有 completed rectangle |
| 唯一有正框的图 | 5,946 | 其余 6,619 张为背景 |
| `dense_owner_v11` | 11,730 图 / 5,831 框 | 后续旧检测训练主源 |
| 可重新对齐到 OHLC 的有效框 | 3,318 | `<2026-05-04`，见旧 feature verdict |
| Owner 方向复核框 | 2,525 | long 1,152 / short 1,361 / skip 12 |
| 实际旧 short 训练正例 | 1,345 | 1,361 short − 15 alias − 1 time-split purge |

因此：这 1,345 张确实来自当年一万多张人工工作，但又经过“有效 OHLC 对齐 → 方向复核 →
只取 short → 去重与时间门”四次收窄。Owner 新参考图是**向上启动**，所以这轮不能只处理
1,345 个 short；完整 2,525 方向池也必须评分并保留 long/short 字段。

## 3. 数据质量与路径修复

`positive_manifest.jsonl` 中 1,345 行里有 **1,339 条 `source_csv` 文件名已经过期**：系统重装和
继续 fetch 后，文件尾部的行数后缀变了。代码没有相信旧路径，也没有按币名随便选文件，而是：

1. 枚举该 symbol 唯一的 `okx_{symbol}_15m_*.csv`；
2. 在原 `source_owner_global[1]` 索引处读取时间；
3. 必须与 `source_owner_cut_time` 完全相等才接受；
4. 0 个或多个匹配都 fail closed。

结果：1,345/1,345 全部评分；215 个 symbol 只计算 215 次时间序列。1,339 条 stale name 全部通过
索引时间一致性找回，另 6 条原文件名直接匹配。完整 2,525 也达到 2,525/2,525。

## 4. 分档结果

阈值只从 104 个 exact ⭐ Owner 标杆框校准，不看收益、不看未来走势，也不看 390 条反证标签：

- A 核心：`score >= 0.4301308627`（⭐ lower median，标杆 recall 50.96%）；
- A+B 宽松：`score >= 0.2950651631`（⭐ lower p10，标杆 recall 90.38%）；
- C 其余：低于宽松门，**仍不得自动删除**。

| 母池 | A 核心 | B 扩展 | C 其余 | 总计 |
|---|---:|---:|---:|---:|
| 旧训练实际正例 | 385 | 575 | 385 | 1,345 |
| 完整方向复核框 | 744 | 1,067 | 714 | 2,525 |

完整方向池的 A+B 共 1,811 个框，其中 long **825**、short **976**、skip **10**。去除 skip 后，
这是当前最直接的 **1,801 个“更多数据”候选框**；它们仍只是 Owner 旧方向裁决 + 代码排序，
不是已经批准的新训练集。

## 5. 独立反证：代码能不能代替人工

反证集是 `owner_side_short_tip_v1b_detect1000` 中已完成人工判断的 390 条：keep 71、drop 319。
它与 104 个 ⭐ 阈值校准集分开，且没有用于拟合分数或门限。

| 方法 | 选中 n | keep | precision | recall | 相对 18.21% base rate |
|---|---:|---:|---:|---:|---:|
| 不筛（对照） | 390 | 71 | 18.21% | 100% | 1.000× |
| 旧 expanded 密度门 | 172 | 22 | 12.79% | 30.99% | 0.703× |
| 新 A 核心 | 132 | 21 | 15.91% | 29.58% | 0.874× |
| 新 A+B 宽松 | 291 | 53 | 18.21% | 74.65% | 1.000× |

其他必报诊断：

- keep/drop AUC：**0.4887**；
- keep 平均分 − drop 平均分：**−0.00641**；
- 标签置换零假设：keep/drop 与分数可交换，10,000 次，seed=20260821；
- 单侧 `keep score > drop score`：**p=0.6352**；
- A+B precision Wilson 95% CI：**[14.20%, 23.05%]**，下界没有超过 base rate。

结论：旧“只看带宽”的规则会反向筛选；新分数在标杆视觉上更合理，但跨到独立 short-tip
proposal 后仍没有区分力。**允许做排序页面，不允许做自动删图门。**

本实验是标签/形态质量审计，不是方向收益实验，因此 val AUC、top-decile 收益、成本、胜率和
匹配随机入场对照按字面均不适用；不得编造。上述标签置换是同等严格的零假设对照。

## 6. 最近 BTC / ETH 只作示例，不作验收

Owner 明确要求查看最近行情，本轮通过 OKX 无鉴权公共 `GET /api/v5/market/candles` 读取 15m 已收
K 线；OKX 文档说明该接口每次最多 300 根，字段为 `[ts,o,h,l,c,...,confirm]`：
[OKX Market Data API](https://app.okx.com/docs-v5/en/#rest-api-market-data-get-candlesticks)。

截至 **2026-08-21 04:30 UTC**：

| 合约 | 起点（2026-08-18 02:00 UTC） | 终点 | 涨幅 |
|---|---:|---:|---:|
| BTC-USDT-SWAP | 64,152.0 | 74,929.8 | +16.80% |
| ETH-USDT-SWAP | 1,898.84 | 2,349.76 | +23.75% |

所以 Owner 所说“ETH 几天约涨二十个点”与官方公共 K 线一致。冻结分数后，按截图红框的大致时段
检查：BTC 最高 `0.764`（A），ETH `0.641`（A），说明参考形态没有被代码漏掉。

但从 2026-08-16 00:00 到 08-18 12:00 UTC 的 241 根 bar 中，BTC 有 **119** 根、ETH 有
**146** 根落入 A 档。它会在横盘密集区连续命中，因此**绝不是开仓信号，也不能一根 bar 生成
一张新训练图**。

这次最近行情是 Owner 授权的**设计示例 view #1**，已经失去“从未看过的最终验收”资格；没有用它
改任何权重或阈值，报告也不把上涨当策略 alpha。最终交易验收仍需另一个预注册、未读取的连续块。

## 7. 怎么把数据做多，而不是把重复 bar 做多

正确扩充顺序：

1. **先处理现成 2,525**：A 744 先审，再审 B 1,067；按 long/short 保持方向隔离，skip 不训练。
2. **再回收约 793 个旧有效框**：旧 OHLC 对齐有 3,318 个有效框，方向复核只覆盖 2,525；差额需
   重新生成同一原图页面，让 Owner 补方向/保留裁决。
3. **最后才从 456 币历史扫描新候选**：使用冻结分数找局部峰值，先按同币连续事件合并并设置
   冷却窗，再抽样人工审；禁止把每个连续 A bar 当独立样本。
4. 新候选只进 `candidate_review`，不直接当正例；方向和核心边界仍由 Owner 确认。
5. 生成新版本 manifest，不覆盖旧数据；重验 split 依赖、图像/标签 SHA、无未来和类别比例。
6. 只有 Owner 明确批准后，才能把新版本 `training_eligible` 改为 true，再讨论 3060 单变量训练。

## 8. 快捷页面

- 旧训练 1,345：
  `datasets/owner_short_gold_center_v1/review/ma_rope_prefilter_v1/public/index.html`
- 完整方向池 2,525：
  `datasets/owner_short_gold_center_v1/review/ma_rope_prefilter_v1/public/owner_2525.html`

两页默认 A 核心档，快捷键一致：`K/1` 保留、`X/2` 去掉、`?/3` 待定、`J/L` 前后、`U` 撤销、
`Z` 原尺寸；浏览器 localStorage 断点保存并可导出/导入 JSON。每次只显示一张 Owner 原图，
不存在左右两张不同 K 线，也不再使用无反应的 `R`。

## 9. 复现命令

生成器已先提交为 `7af88a8295d12664759a96330c748889c7ac980f`，再构建正式产物：

```bash
git branch --show-current  # 必须是 main
.venv/bin/python -m pytest -q \
  tests/test_ma_rope_filter.py \
  tests/test_ma_rope_review.py \
  tests/boundaries/test_layer_imports.py

.venv/bin/python tools/datasets/ma_rope_review.py build \
  --generator-commit 7af88a8295d12664759a96330c748889c7ac980f
.venv/bin/python tools/datasets/ma_rope_review.py verify
```

测试结果：**71 passed**。MA 与检测渲染合同独立 parity 检查最大绝对差 **0.0**。

正式身份：

- primary public manifest SHA256：
  `aee393d1abc2077999920fa14506ddb6159c973a51f00c9d19ea3783a8219712`；
- 2,525 public manifest SHA256：
  `ae65f868256632b32e627927fa7443b128cfe03492d52d6a1d055dcb8dd75e69`；
- 1,345 score rows SHA256：
  `b129206b51e396cacd13d36ade1bf2225f7cf2c4e4fce5dde09456fc1aae1150`；
- 2,525 score rows SHA256：
  `0752df544308e6c26f647fc1a520375a551ed003e234d12f3e50dd8a05deef0b`。

## 10. 风险与诚实声明

1. 单张截图不足以唯一确定决策 bar、触碰容差和窗口长度；当前参数由仓库六均线合同、旧严格带宽
   和 pre-holdout ⭐ 标杆确定，后续不能因上涨结果随意修改。
2. 390 条反证来自 short-tip detector proposal，与 1,345 个旧 Owner box 不是完全同分布；失败足以
   否决“自动过滤”，但不能证明排序对旧框毫无辅助价值。
3. 104 个 ⭐ 用来定阈值，所以 ⭐ recall 是校准描述，不是独立泛化指标。
4. 当前行情只证明“参考时段会得高分”，不证明“高分必涨”、方向可判或扣成本后能赚钱。
5. 2,525 中 long/short 是方向协议裁决，不等于这次重新确认每个样本质量；页面仍需 Owner 决定。
6. 本轮没有生成新训练版本；原 1,345、原标签和 eligibility 全部保持不变。

## 11. 下一步（Owner 决策）

建议现在只做一件事：先打开 **1,345 页面默认 A 档的 385 张**，用快捷键完成第一轮。A 审完导出
JSON；再决定是否审 B。任何结果都先生成新 manifest 预览，Owner 明确批准后才进入 3060。
