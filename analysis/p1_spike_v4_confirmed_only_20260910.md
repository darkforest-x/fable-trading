# V4：图表只显示确认信号

**已更新并保存 TradingView 私有「SPIKE V4 · 确认信号」；原生编译完成。** 当前 OKX PEPEUSDT.P 1H 图表布局也已保存。

## 图面调整

| 项目 | 前一版 V4 | 当前 V4 |
|---|---|---|
| 信号 | 预警文字＋所属确认点，另有完整显示 | 只画确认三角及实际确认收盘价 |
| 分层选项 | 低干扰／完整显示 | 删除 |
| 预警、补充、未确认 | 存在分层显示或面板文字 | 不在图上显示 |
| 状态、退出与 R 里程碑文字 | 默认打开 | 默认关闭，当前实例也已核实关闭 |
| 盈亏框起点 | 首个空闲预警收盘 | 首个空闲确认收盘 |
| 六条均线、双线及零轴 | 保留 | 保留 |

同根确认和随后 1–3 根的延迟确认，都在**实际确认 K 线**显示，使用这根的收盘价。确认引擎、量能与推进门槛、候选归属、冷却和到期规则完全保留。

已有风险参考期间再次确认，仍显示该确认，但不会重置原盈亏框；其所属关系放入悬停说明，不增加“补充确认”等图面分类。风险参考结束同根不重新开启，沿用原处理次序。独立的连续三角保留历史信号位置，价格标签仍受 TradingView 300 个共享标签对象上限限制。

## 为什么盈亏框要同步改起点

只隐藏预警、继续沿用预警价格，会让确认标签旁的盈亏框使用当时尚未确认的入场位置。本版从确认收盘才建立风险参考，并用确认当时的 ATR 和近期极值计算初始风险；当前和峰值 R 从 0 开始，随后才逐根跟踪。

止损倍数、风险取整、跳空保护、移动保护算法保持。**起始时刻不同，入场价、止损价、R 和保护触及时间会变化**，不能套用原预警回测的收益数字。3R 仍只是观察位，不是固定止盈。

## 验证和证据

6 项 V4 源码契约检查及 23 项冻结父版测试共 29 项通过，并有独立复核。特征和 ALERT 状态与冻结父源逐字一致；REFERENCE 的逻辑差异限定为确认起点和可选退出文字；风险框数值算法一致，仅更新价格来源文案。

原生编辑器显示保存版本 `3 ∙ 今天, 20:22`，编译计算完成后“图表更新”禁用；随后私有脚本重命名为「SPIKE V4 · 确认信号」。图面可见确认位置、价格、确认副图点、盈亏框，没有预警文字。设置里确认标签、六线、风险框开启，面板和里程碑关闭。保存布局后按钮禁用。

[Pine 源码](/Users/zhangzc/fable-trading/yoyo/evaluation/pine/spike_burst_v4.pine) · [原生保存收据](/Users/zhangzc/fable-trading/experiments/active/exp-spike-v3-layered-display-20260910-v1/qa/v4_confirmed_only_native_save.json)

Pine SHA：`05a813664397dc7e10ebeea44194c408a4d634887469d77554dde7e893579bf5`。源码提交：`c81d06e`。

```bash
.venv/bin/python -m pytest -q tests/test_spike_burst_v4_release.py tests/test_spike_burst_v3_layered_display.py
python3 scripts/md_to_html.py analysis/p1_spike_v4_confirmed_only_20260910.md --out-dir analysis/html
```

## 风险与诚实声明

本次是按 Owner 要求更改图表显示及其风险价格来源，没有新增行情回测、训练或 holdout 评分。候选数、正类率、val 样本数、AUC、置换 p、top-decile 收益、胜率、匹配随机对照均不适用；严格对照为冻结事件源码、有限差异风险契约与独立复核。它们不能证明确认信号更赚钱。

未改原 V3、监控服务、Bark/TG 或下单配置，也没有创建或更新 TV 警报。现存 Pine 原候选及确认 alertcondition 声明保留；保护触及事件跟随新的确认参考，所以不能称所有 Pine 输出数值都不变。本次只核验当前浅色 PEPE 1H 图表，不代表所有主题、周期和手机实例都已验收。HTML 进行静态检查，不宣称浏览器渲染验收。

降噪研究继续保留原 V1 和 V3 对照，单独验证信号质量，不将隐藏文字当作统计改善。
