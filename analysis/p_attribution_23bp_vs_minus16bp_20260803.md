# 归因:+23.49bp 与 -15.91bp 的 44bp 差从哪来

**日期**:2026-08-03
**预注册**:[`analysis/prereg_attribution_20260803.md`](prereg_attribution_20260803.md),
提交于 `41a788d`,**早于任何数字存在**。判据未在事后调整。
**执行边界**:pre-holdout 双方;**未读 holdout(计数仍为 9)**;未 promote、未改 ACTIVE、
未建 active_bundle、未改生产阈值或成本常数、未部署、未下单。训练的是一次性研究模型,
不落盘为 artifact。**本文不改变 P2 的 rejected 结论。**

---

## 结论(一句话)

**那 44bp 差的最大来源是切分方案,不是数据、不是特征语义。**
15 折 CPCV 换成 5 折 walkforward,顶档绝对收益掉 **-21.93bp**,符号也在这一步翻转。
其次是我多用的 19 个 alpha 因子(**-17.08bp**)。
**P1 重建的数据本身反而略好(+5.80bp)。**

预注册的判据是"四级之和覆盖总差 ≥80%"。实际 **残差 -0.00bp,覆盖 100%**。

---

## 复现命令

```bash
PYTHONPATH=. .venv/bin/python scripts/diag_attribution_23bp_vs_minus16bp.py
```

确定性:我的配置 `seed=20260730`,P2 配置 `seed=42`,均写在脚本常量里。
**本报告的数字是我亲手重跑产出的**(见下节"来源"),与先前一份产物逐字节相同。

## 数据统计

| | v10 池 | P1 immutable 池 |
|---|---|---|
| 文件 | `data/judgment_v10_wide.csv` | `data/p1/p1_short_l2_preholdout_aade2a334448d644.csv` |
| 行 / 币 | 18,379 / 232 | 18,103 / 230 |
| 时间 | 2026-02-01 → 2026-05-03 | 同 |
| 特征语义 | `legacy_unaligned` | `side_aligned_v1` |
| 目标 | `net_barrier_taker` | `net_ret_swap_taker` |
| 成本 | 均已含 10bp taker | 同 |

两侧目标口径一致(都已扣 10bp taker),因此 S0b→S1 的比较是干净的。

---

## 结果表(单变量阶梯)

| 级 | 隔离的变量 | 顶档提升 | **顶档绝对** | 折 | 中位轮数 |
|---|---|---|---|---|---|
| S0 | — 我 07-30 的配置 | +22.56bp | **+30.76bp** | 15 | 250 |
| S0b | alpha 因子(19 个) | +13.16bp | +13.68bp | 15 | 250 |
| S1 | 数据 + 特征语义 | +20.83bp | +19.48bp | 15 | 250 |
| S2 | **切分方案** | +8.85bp | **-2.45bp** | 5 | 250 |
| S3 | 成本口径 +5bp | +8.85bp | -7.45bp | 5 | 250 |
| S4 | 模型配置 + 早停 | +0.71bp | **-13.48bp** | 5 | **2** |

### 逐级增量(按绝对值排序)

| 变量 | 增量 | 解读 |
|---|---|---|
| **切分方案** | **-21.93bp** | **主因,且符号在此翻转** |
| **alpha 因子(19 个)** | **-17.08bp** | 我用了 47 特征,P1 池只有 28 |
| 模型配置 + 早停 | -6.03bp | P2 的模型停在第 2 轮 |
| 成本口径 +5bp | -5.00bp | 算术:加 5bp 成本就少 5bp |
| 数据 + 特征语义 | **+5.80bp** | **P1 的数据反而略好** |

```
S0→S4 总差 -44.24bp   四级之和 -44.24bp   残差 -0.00bp
本阶梯 S4 顶档绝对 -13.48bp  ·  P2 报告的逐折 exact-top -15.91bp
```

---

## 解读

### 主因:CPCV 高估了 22bp

15 折 CPCV(6 选 2,带 purge/embargo)在测试块的**两侧**取训练样本。
这是 de Prado 的标准做法,purge 也确实挡住了标签窗泄漏 —— 但它挡不住**制度泄漏**:
模型见过测试块之后的市场状态。

**实盘只有过去。** walkforward 只用测试块之前的数据训练,是唯一与实盘同构的切法。

所以这 -21.93bp 不是"哪一方算错了",而是 **CPCV 的乐观程度在这个问题上的实测值**。
我 07-30 那个 +23.49bp 没有算错,它回答的是"如果模型能看到未来制度,顶档值多少" ——
那不是一个可交易的问题。

### 次因:19 个 alpha 因子值 17bp,但它们不在 P1 池里

去掉 alpha,顶档绝对从 +30.76 掉到 +13.68。这些因子确实携带信息,
**但 P1 immutable dataset 只有 28 个 schema 特征**,所以 P2 从一开始就没有它们。
这不是 P2 做错了 —— 是数据合同里没写。

**这条是可行动的**:若 alpha 因子经得起 walkforward 检验,把它们并入 P1 schema 是有据可依的提案。
但**本报告不构成那个提案**,它需要单独的预注册(见"下一步")。

### 数据本身没问题,甚至更好

`+5.80bp` 说明 P1 的重建(side_aligned_v1 语义 + immutable 合同)**没有损害信号**。
我原先的假设是"我的数算在了 legacy 语义上所以虚高",**这个假设被否定了**。

### 成本那 5bp 是算术

加 5bp 滑点压力线,顶档绝对少 5.00bp。**这一级不含任何发现**,列出来是为了让它不混进别的解释。

### 模型配置只值 6bp

P2 的模型退化(1~2 棵树、15 个不同分值)看着触目,但在**已经用 walkforward 且加了成本**之后,
它只再拿走 6.03bp。**换句话说:即使模型不退化,P2 也过不了门。**

---

## 与上一版本对照

| 结论 | 07-30 我的说法 | 本报告 |
|---|---|---|
| +23.49bp 的成因 | 未解释 | **切分 -21.93 + alpha -17.08 为主** |
| "我的数算在 legacy 池所以虚高" | 我 08-03 的猜测 | **否定** —— 数据换成 P1 反而 +5.80bp |
| 顶档是不是真十分位 | 已实测:每折 613/6126 = 10.0%,唯一率 100% | 不变 |
| P2 rejected | — | **不变**,本报告不触碰该结论 |

---

## 风险与诚实声明

- **来源**:先前存在一份同名产物,出现在脚本写完 98 秒后,而我那次启动被 owner 拒绝,
  **我无法确定是哪个进程产生的**。因此我自己重跑一遍,只报自己产出的数;
  两份在确定性种子下**逐字节相同**,来源问题不影响结论 —— 但这件事本身如实记在这里。
- **没有匹配随机对照组**。本文是对既有差值的分解,不是方向性策略结果;
  CLAUDE.md 要求对照的是后者。**不得把本文任何一级当作"某配置能赚钱"的证据。**
- **S4 (-13.48bp) 与 P2 报告 (-15.91bp) 差 2.43bp。** 预注册已声明:P2 的 exact-top 对边界并列
  用等权 fractional weight,我用硬 `>= quantile`,该差异随"模型配置"一级一起进来,
  **无法单独隔离**。所以归因只能说"模型配置 + 并列处理"合计 -6.03bp。
- **两个池不是同一批候选**(18,379 vs 18,103)。差集本身未分析。
- **alpha 因子那 17bp 是在 CPCV 下测的**(S0→S0b 两级都是 15 折)。
  它在 walkforward 下值多少,本实验**没有测**。不得据此认为并入 alpha 就能救 P2。
- 单变量阶梯的增量依赖顺序。换个顺序,交互项会重新分配。**"覆盖 100%"是这一条路径上的,
  不代表各因素正交。**

---

## 下一步选项

1. **【需 owner 决策 + 新预注册】alpha 因子在 walkforward 下值多少。**
   本报告只证明它们在 CPCV 下值 17bp。若在 walkforward 下仍显著,
   才谈得上"把 alpha 并入 P1 schema"的提案。
2. **【建议】以后研究侧默认用 walkforward,CPCV 只作为上界参考。**
   本次实测的乐观幅度是 21.93bp,而本项目全部可争取的改进都在 5~20bp 量级 ——
   **CPCV 的偏差比要找的效应还大。**
3. **【不做】不要据此重开 P2。** 四级里没有一级指向"P2 执行有误";
   最大的两级(切分、alpha)说明的是我那个数为什么偏高,不是 P2 为什么偏低。
