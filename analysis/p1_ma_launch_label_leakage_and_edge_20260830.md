# MA 密集启动：标签泄漏与真实 edge（2026-08-30）

## 结论先行

**mAP 0.91 的那个检测器，学的不是形态，是「价格最后两根有没有动」。**

把标签从「启动成功」换成「最终赚钱 vs 最终亏钱」之后，同一套配方的 mAP50 从 **0.912 掉到 0.400**，
提案相对匹配对照组的 edge 从 +0.238 ATR 变成 **−0.024 ATR（95%CI 横跨零点）**。

而且用更严的基线一测，15m 检测器是**负贡献**：闭着眼做筛选门放行的每一个事件，
净收益 +0.059 ATR；让模型挑一遍之后变成 −0.029 ATR。**那点 edge 全属于筛选门，不属于模型。**

## 复现命令

```bash
# 1  5m 归档（139 GB，checksum 校验）
python3 scripts/fetch_binance_um_preholdout_15m.py --interval 5m \
  --output-dir data/kline_preholdout_binance_um5m --workers 16

# 2  用冻结硬门扫 5m 形态
python3 scripts/scan_5m_ma_launch_grade_a_candidates.py

# 3  障碍模拟（TP5/SL2，超时窗扫描 72/144/216/288 根）
python3 scripts/simulate_5m_ma_launch_outcomes.py

# 4  按结果重标数据集（1 张 / 8 张两版）
python3 scripts/build_5m_ma_launch_outcome_dataset.py
FABLE_5M_RENDER_ALL=1 FABLE_5M_OUTCOME_DST=datasets/ma_launch_5m_outcome_x8_v1 \
  python3 scripts/build_5m_ma_launch_outcome_dataset.py

# 5  训练（RTX 3060）
FABLE_3060_HOST=Administrator@<ip> bash scripts/train_5m_ma_launch_outcome_960_on_3060.sh

# 6  净收益 + 匹配随机对照组
python3 scripts/evaluate_detector_net_returns.py \
  --weights <best.pt> --dataset <dataset> --bar-minutes 5 --horizon 144 --label <name>
```

## 数据统计

| | 15m | 5m |
|---|---:|---:|
| 源 K 线 | 49,162,351 | 147,487,020 |
| 币种 | 638 | 638 |
| 通过硬门的形态 | 1,043 | 3,940 |
| 训练图（8 张渲染） | 8,000 正 / 24,000 负 | 31,400 正 / 94,136 负 |
| 时间范围 | 2020H1–2026H1 | 同 |
| holdout | ≥2026-05-04，未读 | 同 |

障碍：TP 5 ATR / SL 2 ATR，入场为核心结束 +2 根收盘，成本 0.2% 往返，同 bar 保守判止损。
超时窗 15m 用 72 根（18h），5m 用 144 根（12h）——**没有照抄 72 这个数字**，
因为 5m 的 72 根只有 6 小时，超时率 358/3936；扫描 72/144/216/288 后 144 根起超时降到 85 且净收益不再变化。

## 结果表

### 训练指标

| 运行 | 标签 | 渲染 | 图片 | 最佳 epoch | mAP50 | mAP50-95 | 第 40 轮 mAP50-95 |
|---|---|---:|---:|---:|---:|---:|---:|
| 15m 960 | 启动门 | 8 | 32,000 | 6 | **0.9116** | 0.7777 | 0.6943 |
| 15m 1280 | 启动门 | 8 | 32,000 | 4 | **0.9224** | 0.7865 | 0.6834 |
| 5m ×1 | **结果** | 1 | 3,812 | 16 | **0.3997** | 0.3418 | 0.2644 |
| 5m ×8 | **结果** | 8 | 29,619 | 3 | **0.4498** | 0.3692 | 前 19 轮后终止 |

### 净收益 vs 匹配随机对照组

对照组：同币 × 同月 × 同 ATR 三分位 × 同方向 × 同障碍 × 同成本，每单 20 次抽样。
收益一律用 **ATR 单位**——障碍是 ATR 倍数，用百分比等于在测币的波动率。

| 模型 | 开火率 | TP 率 | 池子 | 对照组 | edge | 95%CI |
|---|---:|---:|---:|---:|---:|---|
| 15m 960 | 22.7% | 26.8% | −0.029 | −0.267 | **+0.238** | [+0.056, +0.429] |
| 15m 1280 | 22.5% | 26.6% | −0.028 | −0.305 | **+0.277** | [+0.096, +0.462] |
| 5m ×1（结果标签） | 63.8% | 27.2% | −0.612 | −0.588 | **−0.024** | [−0.336, +0.309] |

### 更严的基线：模型 vs 闭眼做门放行的全部事件

| | TP 率 | 净收益 | 相对闭眼基线 |
|---|---:|---:|---:|
| 闭眼做全部 154 个 val 事件 | 28.6% | **+0.059** | — |
| 15m 960 挑过之后 | 26.8% | −0.029 | **−0.088** |
| 15m 1280 挑过之后 | 26.6% | −0.028 | **−0.087** |

**两个 15m 模型挑完都比不挑更差。**

### 5m 模型的置信度不携带信息

| 置信度分位 | n | 均值 conf | TP 率 | 净收益 |
|---|---:|---:|---:|---:|
| Q1 低 | 92 | 0.282 | 22.8% | −0.920 |
| Q2 | 92 | 0.352 | 30.4% | −0.316 |
| Q3 | 91 | 0.454 | 24.2% | −0.829 |
| Q4 高 | 92 | 0.631 | 31.5% | −0.385 |

conf vs 净收益 rho=−0.069 (p=0.19)；conf vs 是否止盈 rho=+0.066 (p=0.21)。四档净收益全负。

## 解读：每个数字变化的归因

**1. 0.912 → 0.400 的落差来自标签，不是周期也不是数据量。**

冻结筛选门要求 `post1/post2/post3/post5` 进度达标。实测 1,043 个正例：

```
post1  中位 +1.06 ATR   最小 −0.00   低于 0 的比例 0.0%
post2  中位 +1.91 ATR   最小 +1.00   低于 0 的比例 0.0%
post5  中位 +2.85 ATR   最小 +1.75   低于 0 的比例 0.0%
```

零例外，最小值精确卡在门槛上。而负例 `abs_close_progress_atr_core_plus_2 ≈ 0.0`。
**正例=已经动了 ≥1 ATR，负例=完全没动**，且模型输入窗口含核心后 2–9 根，
这个差别直接画在图上。0.91 测的是「最后两根多长」。

换成结果标签后正负例都在启动，捷径失效 → 0.400。

**2. 8 倍渲染只值 +0.05 mAP，且第 3 轮见顶。**

5m ×1 (3,812 张) mAP50 0.400；5m ×8 (29,619 张) 0.450，但最佳 epoch 从 16 提前到 3，
之后一路掉到 0.273。8 张渲染是同一形态的 8 个水平位置，**不含新信息**，只是让模型更快背完。
15m 那两个也是同样形状：40 轮里第 4–6 轮见顶，后 34 轮全是退化。

**3. +0.238 ATR 的 edge 属于筛选门。**

匹配对照组是随机时点入场，而模型的池子天生是「门放行的事件」——门保证了入场时已朝正确方向走了
中位 1.91 ATR。把对照组换成「门放行的全部事件」，模型的贡献变成 **−0.088 ATR**。

**4. 置信度不是质量排序。**

与 2026-08-28 的审计一致（episode 最高分主要反映重复检出次数，rho=0.859）。
本轮更直接：conf 与净收益、与胜率都无相关，最高分那档净收益仍是 −0.385 ATR。

## 匹配随机对照组（方向性实验必备）

已在上表给出。补充设计说明：

- 对照抽样在**同一源文件**内进行，同年同月、同 ATR 三分位、同方向，避免跨币/跨时段比较
- 每个真实提案配 20 次随机入场，取其均值配对，再对配对差做 4,000 次 bootstrap
- 强制 **ATR/price ≥ 1bp** 下限。首次运行未设下限时，停牌冻结报价的 bar 使 ATR≈0，
  报出 edge = 2.9e30 ATR——那是除法产物不是交易

## 风险与诚实声明

1. **5m ×8 只跑了 19/40 轮就被 owner 终止**，其最佳 epoch 3 / mAP50 0.4498 是前 19 轮内的最优。
   剩余 21 轮理论上仍可能翻盘，但曲线已连续下滑 16 轮（0.450→0.273），翻盘概率低。**不是完整实验。**

2. **15m 和 5m 不是同一个可交易对象。** ATR、障碍、确认窗都基于「一根 K」，
   5m 的一根只有 15m 的三分之一。两者的 mAP 不可直接比较；本报告只在**标签变量**上做对照。

3. **5m 的 edge = 0 是「未能证明有 edge」，不是「证明没有 edge」。** n=367 个提案，
   CI [−0.336, +0.309] 足够宽，容得下一个小的真实 edge。要收窄需要更多样本或更强的检验。

4. **本轮未消耗 holdout。** 所有窗口 < 2026-05-04，抽查 40 个正例窗口越界数为 0。
   未训练/调参/改阈值/promote/部署/下单。

5. **数据集里发现并删除了矛盾样本**：20 组逐字节相同的图，其中 3 组同一张图既被标成正例又被标成负例
   （相邻核心检测落在同一窗口，结果一个止盈一个止损）。矛盾组整组删除而非二选一。

6. **净收益评估用的是各模型自己的 val split**，不是共同数据。15m 两个模型共用同一 val（可比），
   5m 是另一个 val（与 15m 不可比）。

## 下一步选项

1. **停止在这条形态上加数据/调框/换分辨率** —— 本轮证明这三条都不是瓶颈。（不需要决策）
2. **补 5m ×8 的剩余 21 轮**，把「8 张渲染无用」从「高度可能」变成「已验证」。（约 3 小时 GPU）
3. **换特征而非换模型**：既然纯视觉压缩形态不携带成败信息，可测试加入非视觉特征
   （币层波动状态、资金费率、盘口）能否分开成败。**需要 owner 决策方向。**
4. **重新审视形态定义本身**：owner 口径的「六线密集启动」目前由自动门近似，
   而门与 owner 眼睛的一致性从未逐样本验证过。**需要 owner 决策是否投入人工标注。**
