# V12：ONE 三次突破说明与紧凑样式

实验 `exp-spike-v12-compact-bk-20260920-v1`，2026-09-20，Owner 要求解释三次提示、突破简称 bk、回踩用英文缩写。本轮仅显示整理，不改信号、报警、联合、止损或成本。

## 为什么有三个

原冻结 ONE 15m 辅助族记录（Luna Max 也核对当前 1499 根 reference）：

| 突破确认收盘（北京） | A | B | C | 原参考 uid |
|---|---|---|---|---:|
| 09-17 02:30 | 09-15 12:45 | 09-16 17:45 | 09-16 19:30 | 15 |
| 09-17 03:30 | 09-15 23:00 | 同上 | 同上 | 16 |
| 09-17 04:30 | 09-16 08:00 | 同上 | 09-17 02:00 | 17 |

上述辅助参考三者全部 raw，不是 raw/soft 双轨重复；这不代表原生完整候选池每次选择同一代表，见下方原生核验。它们共用 B，但 A 不同、斜率不同，第三条 C 也不同；算法的几何同线判断不会把它们判为一条。三个突破提示不等于三笔入场。当时无 V9 多头框，不能宣称三次联合。截图里的大三角中文来自较早 V12 显示实现。

## 最终显示约定

- 普通 `bk`，联合 `bk+spike`，上级 `bk 1h` 等；局部 C 显示 `C·RT`（retest）。小字、无底色，大圆圈改微小定位点，确认背景块改小字 `ok`。
- 同本图/上级来源、完全相同 B 锚点和 B 价格、距首次事件不超过 8 根本图 K，普通突破可合并为 `bk · 数量`。联合不合并，并隔开两段显示组。参数可关闭以恢复逐条显示。
- 保留首次标记位置与首条冻结代表线，不在后来更换为更好看的线；计数只在新事件实际到达后增长。提示明确写“候选突破，不代表多笔入场”，并保留每条事件时间、A/B/C 及价格。后两条的线是被显示聚合，不是被算法删除。
- 当前候选绘图不再重复绘制已归档的突破线；普通历史代表线稍淡，ABC 用更小字号。历史组依然成组删除。

## 验证

代码先提交 `4becc98a0e` 后做原生验收。51 项原相关检查通过；与前一提交对比 V9/risk、找线引擎、持线/框配对、事件码及预警区块逐字一致。无新增行情回测或收益实验。

```bash
.venv/bin/python -m pytest -q tests/evaluation/test_spike_v12_pairing_reference.py tests/evaluation/test_spike_v12_local_touch.py tests/evaluation/test_spike_v12_pine_contract.py tests/evaluation/test_spike_v10_4.py tests/evaluation/test_spike_v11_box.py
```

输入证据：上一版 `exp-spike-v12-local-touch-20260920-v1/auxiliary_lines.csv`、`auxiliary_breaks.csv`；当前实验 `one_three_breaks.json` 保留三条原记录和来源。1499 根参考数据为既有诊断集，不是独立统计样本。零假设/反例口径：不同 B、不同来源、超过组首 8 根或跨联合不能聚合；原始事件数量与时间保持。AUC、收益、胜率和随机入场对照不适用于显示修改。

## 风险与诚实声明

显示聚合不等于几何去重，更不等于提高收益；tooltip 的组计数会随后续已发生事件增长，代表线和首次确认位置保持。需要逐线核对时关闭“合并同一 B 点附近的 bk 提示”。未改生产路径；不生成 HTML。

## 原生最终核验

TV 私有修订12在03:48:27保存编译（0 error，仅原有 second shadow warning）。源码SHA256：`aca54ec5f292fefdcbb45b496746224c619ef31411428a592ffecc358d8238ad`，导入132349字符，FNV1a f6e4e4d6与本地一致。

- 05:00回放截点（03:50:47读取提示）：一个 `bk · 3`，成员#218/#221/#220分别02:30/03:30/04:30，C·RT可见。
- 完整Pine的第二个代表是A09-15 23:45@0.0006749、C09-17 02:00@0.0006424；这与辅助参考uid16不同。完整候选池与有限辅助reference不同，不能据此宣称原生逐线parity；两者都说明同B不同A/C先后突破。
- 03:00回放截点（03:52:40读取提示）：仅 `bk`、#218、02:30及其锚点，没有后面两次记录。确认显示计数不会提前出现。
- 首次位置和代表线保留。回放已退出，当前为ONE OKX15m实时图。完整原生成员字段保存在 `native_tv_verification.json`；此为人工核查回执，不是机器导出的全事件账本。

Luna Max只读诊断指出三条不同几何不能当同线去重，已落实为明确的显示聚合政策。Notion：https://app.notion.com/p/3e08856479af813dab66c21468108a32 。
