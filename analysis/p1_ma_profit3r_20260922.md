# 3R 盈利事件扩展与两组 YOLO：进行中

Owner 于2026-09-22明确授权自动过滤、扩样、处理及3060离线训练。本轮为指定实验的训练例外，不改全局阶段和生产准入。

## 当前结果

| 阶段 | 独立事件 | 毛3R且扣费仍盈利 | 先止损 | 12h超时 |
|---|---:|---:|---:|---:|
| 原Grade-A数据集 | 1043 | 未按本口径评估 | 未评估 | 未评估 |
| 本次冻结盈利标签 | 1043 | 55 | 296 | 692 |
| 本地15m扩展与旧缓存合并、跨源4h去重后 | 1176 | 60 | 342 | 774 |
| 首五周期合并、跨源/跨周期4h去重后 | 4281 | 334 | 1780 | 2159 |

55个合格事件中train51、val2、purged2。完整原池的train913、val113、test2、purged15。旧源截止较早，不能用其两个test事件作有效终审；正在扩大真实历史范围。没有训练。

本地15m扩展批次已完成663/663份源，失败0；33812条弱候选经原形态门与参考校准后保留445条Grade-A候选（尚未跨源去重，也尚未算盈利，不能称445个新赢家）。运行代码commit75c7bfd520，统计见实验目录`scan_batch_summary_local15m.json`。

该445条与旧缓存966+261条合并后，经看盈亏前的同资产/方向4h去重，得到上述1176个事件。收益联结错误0；60个保留赢家分布为train54、val3、test1、purged2。此阶段仍远低于训练门，未生成正式训练数据集，未开训；仍在继续其他周期。不得把60/1176解释成模型胜率。

高周期输入整理1117份归档，其中1112份生成30/60/240m数据。5份失败/空源完整记录在source manifests（两个0G空时间字段、US100/US500/XOM空源），没有造数据替代；32/54/241份对应周期的历史不足1200根，后续仍按warmup门隔离。

扩展批次随后完成：5m为652/652份源、148294806行、182279条弱候选、2277条严格Grade-A；30m为1112/1112份源、32060335行、36413条弱候选、601条严格Grade-A。两批源失败均0。这里只是看盈亏前候选，尚未跨周期去重和打收益标签；紧凑摘要为`scan_batch_summary_local_5m.json`与`scan_batch_summary_high_30m.json`，均包含原master与规则依赖SHA。

首批15m赢家按稳定事件哈希选取多空各3个作人工例览：`review_local15m/rendered/contact_sheet.png`为6行×3列因果A/B1/B2，`future_review/`单独保存48根连续未来bar核验图。共24图，SHA全部核对，三视图右端一致，无YOLO标签；已实看总览和BAN未来图。此例览不参与容量计数，不是完整训练数据集或胜率证据。

中枢记录：[研究实验（运行中）](https://app.notion.com/p/3e28856479af81a295a6ff91f565d9ef)，唯一键为本实验ID；后续按同一记录补充，不能自动升级为已验证或生产。

## 冻结口径

首五周期（5/15/30/60/240m）共有3797条新严格候选，加旧966+261条后为5024条；看盈利前的4h去重剔除743条，冻结4281事件，标签联结错误0。表中另有UNKNOWN4（未来缺口2、未来长度不足2）和INVALID4（空头目标价非正），未伪装成失败负例。334个TP分布为train300、val26、test5、purged3；完整池train3447、val538、test205、purged91。train赢家按周期为5m244、15m54、30m1、60m1、240m0。正式selection容量门关闭，dataset ledger为空；没有开训。

1m官方扩展首批按冻结字母序25币导入34488599行，gzip与解压后CSV各自SHA通过；普通CSV逻辑2827328238字节，APFS物理755953664字节。透明压缩只影响物理存储，原扫描器按相同CSV内容读取。源清单`sources_archive_1m_batch01.json`已先于正式扫描提交，批次只控制传输/内存，不按盈亏挑选标的。

完整信号时点是核心后第5根收盘；下一根open入场。多头止损为核心最低low，空头为最高high；R是入场到止损的价格距离。12h内先触发3R目标、没有更早止损，且扣0.2%往返成本仍盈利才保留。同bar双触发记止损；止损跳空按open，止盈跳空按目标保守计价。缺失未来为unknown。

这里3R是价格风险单位的毛R，55个赢家净R范围2.775690至2.984110。不能称净3R，也不能把TIMEOUT统一称亏损。该止损/期限是执行前向Owner说明的默认研究假设；原15m清单没有已冻结的经济止损定义。

A组每事件1图，B组每事件2图，仅改变更早上下文，右端均为c+5。原图2至9根后文的做法停止沿用；两组重新渲染，原文件不覆盖。训练遵照仅保留赢家的要求，完整失败事件留给共同val/test。两组同40epochs时B有更多曝光，属于完整配方对比，不能仅归因于位置变化。

`training_contract.json`进一步明确容量口径：3000–5000是训练集的独立赢家数，后续val/test完整已解析候选池额外保留，不用其样本补训练数量。该澄清未修改正在运行的发现/收益`plan.json`及其SHA。训练前必须通过容量与跨venue/timeframe的4h去重检查；超过5000时仅按稳定事件哈希裁选。原19个参考与50个认可参考涉及的69个时间锚点单独冻结，val/test同币±4h邻域排除，避免校准参考进入独立评估。

## 复现

```bash
.venv/bin/python -m pytest tests/test_ma_profit_filter.py -q
.venv/bin/python -m yoyo.datasets.ma_profit_pipeline prepare-legacy --plan experiments/active/exp-ma-profit3r-20260922-v1/plan.json
# 原输入冻结commit706df01ae4；已有产物拒绝覆盖，重放需指定新的out目录。
.venv/bin/python -m yoyo.datasets.ma_profit_pipeline label --plan experiments/active/exp-ma-profit3r-20260922-v1/plan.json --events experiments/active/exp-ma-profit3r-20260922-v1/legacy_events.jsonl --sources experiments/active/exp-ma-profit3r-20260922-v1/legacy_sources.json --out experiments/active/exp-ma-profit3r-20260922-v1/legacy_outcomes
.venv/bin/python -m yoyo.data.ma_profit_sources freeze-local --plan experiments/active/exp-ma-profit3r-20260922-v1/plan.json
.venv/bin/python -m yoyo.data.ma_profit_sources fetch-3m --plan experiments/active/exp-ma-profit3r-20260922-v1/plan.json --workers 8
.venv/bin/python -m yoyo.datasets.ma_profit_miner --plan experiments/active/exp-ma-profit3r-20260922-v1/plan.json --sources experiments/active/exp-ma-profit3r-20260922-v1/sources_local_15m.json --workers 4
.venv/bin/python -m yoyo.datasets.ma_profit_miner --plan experiments/active/exp-ma-profit3r-20260922-v1/plan.json --sources experiments/active/exp-ma-profit3r-20260922-v1/sources_local_5m.json --workers 4
.venv/bin/python -m yoyo.data.ma_profit_high_sources --plan experiments/active/exp-ma-profit3r-20260922-v1/plan.json --workers 2
# 高周期三个已冻结批次按30、60、240顺序执行同一矿工（各workers2）。
.venv/bin/python -m yoyo.datasets.ma_profit_miner --plan experiments/active/exp-ma-profit3r-20260922-v1/plan.json --sources experiments/active/exp-ma-profit3r-20260922-v1/sources_high_30m.json --workers 2
.venv/bin/python -m yoyo.datasets.ma_profit_cohort collect --plan experiments/active/exp-ma-profit3r-20260922-v1/plan.json --sources experiments/active/exp-ma-profit3r-20260922-v1/sources_local_15m.json --out experiments/active/exp-ma-profit3r-20260922-v1/cohort_local15m
# 队列冻结commit532c21f83e后执行：
.venv/bin/python -m yoyo.datasets.ma_profit_pipeline label --plan experiments/active/exp-ma-profit3r-20260922-v1/plan.json --events experiments/active/exp-ma-profit3r-20260922-v1/cohort_local15m/frozen_events.jsonl --sources experiments/active/exp-ma-profit3r-20260922-v1/cohort_local15m/frozen_sources.json --out experiments/active/exp-ma-profit3r-20260922-v1/outcomes_local15m
.venv/bin/python -m yoyo.datasets.ma_profit_review build --selection experiments/active/exp-ma-profit3r-20260922-v1/review_local15m/selection.json --out experiments/active/exp-ma-profit3r-20260922-v1/review_local15m/rendered
```

## 验证与待完成指标

后续大批队列可使用`collect --compact-events`：只在原NMS完成后投影掉冗余发现特征，保留完整源身份、核心坐标、质量分数与cluster成员，加存完整原行规范JSON SHA；详细原记录仍保存在SHA固定的发现产物中。13项cohort/dataset测试通过，包括精简前后三种视图PNG及YOLO标签逐字节一致，旧清单不覆盖。层间边界71项通过。空随机控制队列也已修复为输出可核验的零计数回执，相关空输入+收益解析器10项通过，非空收益逻辑未变。

盈利解析器9项行为测试通过。源499份SHA冻结，1043事件核心时间逐一联结，lineage错误0。3060 LAN可达、GPU空闲、关键版本一致。

新增渲染监督隔离对照：同一因果行情窗口，把结算标签从TP改成SL或TIMEOUT并改变R收益后，PNG和核心几何逐字节/数值不变，只有YOLO标签从正框变为空标签。数据集渲染相关9项测试通过。这直接检查未来盈利信息不会经“正负样本绘制样式”泄漏到像素，未改动正在执行的矿工、标签器或渲染器。

队列6项、因果渲染6项、Windows训练入口7项测试通过；包含真实解析器TP/SL/TIMEOUT接入、2999容量拒绝、5001稳定裁选、未来修改像素不变、Windows文件名、损坏图片/越界框/未审计根目录拒绝。层间守门通过。3060已在实验专用目录收到与Mac SHA一致的YOLO11s初始权重；新入口已真实导入并核对torch2.8.0+cu126、ultralytics8.4.89、numpy2.0.2、pandas2.3.3，详见`remote_staging_receipt.json`。没有安装或变更依赖。

目前只是监督标签筛选，不是模型或交易有效性评估；val AUC、置换p、top-decile收益和匹配随机对照尚无模型输出，不能编造。对应当前阶段的严格控制是先SL/后TP、同bar、gap、非RangeIndex、无未来、费用不盈利拒绝等行为用例。模型完成后在完整后段候选池上报告所需指标和同币/时间/波动控制，禁止只在55个赢家中算胜率。

`evaluation_contract.json`已在训练与模型分数出现前冻结。共同评估入口保留conf>=0.001全框，固定1280尺寸、NMS0.7、CUDA0；检测阈值0.25、IoU0.5。事件排序用候选已知方向的最大预测置信度，不以仅正例有的真值IoU过滤分数。所有未匹配预测框均计误报，包括赢家图的多余框；val/test分开，常数score的top10明确为任意平局裁决。6项最小测试验证完整负例池、漏图拒绝、方向/位置错误、重复框、身份/几何异常及混split拒绝。尚未执行真实权重推理。

匹配控制入口已通过7项最小测试，尚未正式抽样：以核心末根UTC ISO周为时间块，同source/asset/周期/方向/因果TR14波动桶，在看控制收益前按稳定哈希取最多5个；全候选及原参考±4h保护，不足即报缺。正式命令为`.venv/bin/python -m yoyo.evaluation.ma_profit_matched_controls --plan <plan.json> --contract <evaluation_contract.json> --events <完整outcomes.jsonl> --target-events <最终dataset_ledger.jsonl> --sources <frozen_sources.json> --reference-exclusion <reference_exclusion.json> --out <新控制目录>`。控制清单再次提交冻结后，才调用同一个`ma_profit_pipeline label`；跨目标复用必须披露并以每事件对照均值报告。

## 风险与诚实声明

3m官方归档抓取收尾：655个快照币种中654个有可用数据，共284032323行、19783个月归档完成、28个月缺失；GAIBUSDT的10个请求月份均无数据，单列为no_data，不能声称655币历史全覆盖。55份源包含100处内部时间缺口，不补造蜡烛。相关source manifest与coverage先冻结于ef3e5cfad4，再启动扫描。

因此后续标签的输入资格增加明确的连续性审计：从核心起点前11+1200根至c+5，时间戳必须逐根按周期连续，失败留作`purged/known_input_gap`；渲染侧同区间再次拒绝。连续性检查、渲染和原收益解析器共26项测试通过，包括多空标签处理之外的正/负样本输入、warmup/pre/confirmation缺口、区间外缺口不误杀。它不修改止损、目标、期限或形态规则；上述首五周期300个train赢家是追加此输入审计前的阶段记录，后续统一重算并报告，不回写旧结果。

匹配收益汇总模块已建立：先从全部事件排序top10，再在该固定集合内对每事件随机对照取均值；没有有效对照的事件单列，不补抽、不用对照行数改变事件权重。实际执行命令为`.venv/bin/python -m yoyo.evaluation.ma_profit_control_metrics --evaluation <某组完整评估目录> --ledger <最终dataset_ledger.jsonl> --controls <冻结随机控制目录> --outcomes <控制label目录> --out <新比较目录>`。输入SHA、完整控制集合、收益/身份、费用和复用一致性均核验；此处尚无训练权重，不能填入实际收益对照结果。

首批1176个15m独立事件的标签几何检查：扣费后净盈利501个，其中TP60个；TIMEOUT774个中441个仍净盈利，不能将全部超时称为亏损。风险距离`risk_price/entry_price`的25/50/75分位为2.294%/3.301%/4.881%，因此中位毛3R目标距离为9.904%。这揭示当前“确认5根后入场、核心高低点止损”的目标较远，属于描述性诊断；尚未与更早入场做单变量比较，不能声称已证明确认延迟的因果损害。扩样不改变这套已冻结口径。

上述数字从`outcomes_local15m/outcomes.jsonl`全1176行直接计算：`sum(row['profit']['net_r'] > 0)`；对`row['profit']['risk_price'] / row['profit']['entry_price']`取NumPy默认线性分位数；3R距离为风险距离的3倍。它们不是训练后的模型指标，也不是独立实盘验证。

- 当前远未达到3000–5000独立事件；不能通过图片变体或放宽原形态门凑数。
- 规则候选与盈利标签都不是Owner逐张确认Gold。
- 3R事后筛选本身不证明图片足以预测赢家；失败事件必须保留评估。
- 跨周期使用原15m形态门是扩展研究假设，评估按周期分层，不先称同分布。
- 1m扩展另冻`extension_1m.json`，保持同655币快照及原经济规则；12h标签在1m对应720根、在4h对应3根，小周期占比变化可能主导结果，必须单报周期分层。
- 当前exchange-info快照只能保证当次可枚举标的；不能声称覆盖所有历史退市市场。全局质量优先4h去重是研究抽样，不是逐时实盘发信号规则。
- 不改ACTIVE、不部署、不交易。完整实验仍进行中。

## 下一步

持久队列 `yoyo/datasets/ma_profit_queue.py` 顺序观察已启动的3m与1m首批扫描，然后处理1m固定批次02–27；每批追加后重新跨周期去重、计算完整收益与输入资格、冻结容量结果。扫描完成与筛选轮分别保存，恢复时补齐缺失的下游轮；到达3000独立train赢家才输出`queue_ready.json`。每阶段提交仅包含明确路径，不会带入其他任务暂存的改动。队列测试包含真实Git仓集成与伪造、自哈希、split漂移和重复事件拒绝；保持原有形态、收益和训练口径。

1m前五批已经导入125个币种，批次清单与receipt保存在`imports_1m/`及`sources_archive_1m_batchXX.json`；它们仍只是输入覆盖，不能称125币均有赢家。正式训练仍待容量、渲染与审计通过。

后续导入控制器为`yoyo/data/ma_profit_import_queue.py`，从固定batch06继续：远端审计和run binding核验、等待完整批次、SCP gzip与SHA校验、冻结manifest后调用原importer、核对本批完整CSV与发布清单、冻结回执后才清理本地staging。失败/no_data或磁盘低于20GiB明确停止；没有自动删除远端数据。8项测试包含真实Git边界、清理回执中断恢复、传输字节、部分下载等待、终态失败、源集合/CSV漂移及已完成导入恢复；真实只读SSH已确认batch06的25份审计全部完成。该调度不修改原行情抓取或盈利规则。

导入命令：`caffeinate -i .venv/bin/python -u -m yoyo.data.ma_profit_import_queue --plan experiments/active/exp-ma-profit3r-20260922-v1/import_batches_1m.json --from-batch batch_06`。Mac防休眠断言有效；3060当前AC/DC自动睡眠秒数均为0，未修改系统电源配置。

复现队列命令：`caffeinate -i .venv/bin/python -u -m yoyo.datasets.ma_profit_queue`。它要求上游所有输入先冻结，只读取原miner和规则，不重复启动外部仍在运行的3m进程。完成扩大真实事件与审计后，在3060按已冻结A/B方案训练并在共同后段完整候选池评估。

## 1m 第十批可用性裁决与续跑

自动导入在第十批 GAIBUSDT 的 `no_data` 终态停止，原失败日志和错误回执保留。十个月请求归档均缺失的原始审计、源审计 SHA 和可用性裁决先冻结于 `29626e0b6bbca3d1732ff6120d264cee2c461d7c`。原请求仍为25币；本批仅处理有完整审计的24币，没有替换币种或把缺失行情计为零信号。

第十批已完成30319872行导入，24份CSV逐一重哈希；发布清单SHA为 `cc536723826d0c5e63e2e73ec8c17edde2a21e86389fd5b4d8f1f21d56020b6c`，与导入回执一致。`batch_10_gzip_sources.json`保留原请求集合、排除记录及裁决SHA，原importer、CSV字节核验和清理回执流程未变。累计前十批249个可用币种只是输入覆盖，不是合格事件数。

恢复原队列从未处理的第十一批开始：

```bash
caffeinate -i .venv/bin/python -u -m yoyo.data.ma_profit_import_queue --plan experiments/active/exp-ma-profit3r-20260922-v1/import_batches_1m.json --from-batch batch_11
```

重放第十批使用已冻结的`imports_1m/batch_10_gzip_sources.json`，按其中`upstream_gzip_path`传输并核对gzip SHA，再调用`ma_profit_archive_import`；不得重新将原25币请求称作全覆盖。此次可用性处理未修改形态、收益、时间切分或3000个独立训练赢家门槛。

## 全3m加1m首批后的完整筛选

3m扫描654/654个可用币种，3529个严格候选，源扫描失败0。追加完整3m和1m首批后，`cohort_queue_round_001`冻结8084个跨周期去重事件。原labeler完整重算耗时约1352秒（目录birthtime到summary mtime），lineage错误0；TP1032、SL4256、TIMEOUT2787、UNKNOWN5、INVALID4。未解析结果不计负例。

| 阶段 | 独立事件 | TP | train赢家 | val赢家 | test赢家 | purged赢家 |
|---|---:|---:|---:|---:|---:|---:|
| 首五周期，追加连续性审计之前的历史记录 | 4281 | 334 | 300 | 26 | 5 | 3 |
| queue round001，全3m和1m首批，含连续性审计 | 8084 | 1032 | 857 | 102 | 68 | 5 |

本轮完整池按split为train6384、val964、test625、purged111；purged原因为warmup不足94、已知输入缺口3、跨时间切点14。857个train赢家分布为1m59、3m499、5m243、15m54、30m1、60m1。两行数据同时改变了输入覆盖和资格审计，不能把全部增量归因于单一周期。

容量门仍关闭，正式dataset ledger仍为空，没有训练。val/test数是完整收益池的时间分组，后续共同评估还要遵守参考事件排除；不能把102+68个未来赢家挪进训练凑3000。

原完整标注约22.5分钟，因此后续队列接入增量计算：每轮仍重哈希事件所用的全部行情源，只复用源字节、核心语义、plan和原reader/resolver/pipeline全部一致的旧计算字段；新NMS/cluster元数据仍来自新队列。模块31项相关测试通过，接入队列23项通过。旧first5缓存因连续性实现不一致被拒绝；本轮完整结果作为新的基准，另执行真实全量字节一致性核验。该优化不改变任何收益和入组标准。

导入第十一批遇到短暂Git索引锁竞争，失败记录保留；锁释放后，逐字段重建比对待冻结manifest与snapshot一致，再冻结并续跑，未删除索引锁。第十一、十二批各25份源已完成；此前299个可用币种只是行情覆盖。

真实缓存核验已完成并冻结于`d563aee89f2fd91687767bc1a80c541adfd777e4`：8084次命中、0次未命中，outcomes和lineage_errors与本轮原labeler输出逐字节一致，outcomes SHA为`c71d380b5a60c4e22af885944663009f26c91054c0b68d97e41ea56d5fcd68e6`。日志文件birthtime至summary mtime约22.73秒，包含重哈希当前事件使用的源文件；这是全命中核验耗时，不能拿它保证新增来源批次也只需23秒。

迁移旧state和停止回执先冻结于`1bc55a635613779c5f30929c021c30726167896c`，新控制器`e9f5aee2ebae9a719b0d7bbfe613a2c31929b124`通过23项队列测试；新旧state/binding、允许的代码变化、逐字节核验SHA和不变的规则/plan/contract记录在`queue_incremental_migration_receipt.json`（冻结commit`04be3dcee4a636401c64bd4b05d8579d30971643`）。没有静默改旧state的绑定。

迁移中原batch02子进程也在父进程退出后结束，未形成master，不能声称它不间断运行。独立检查无活动miner后，2份完整源receipt的binding和文件SHA通过，剩余23份未完成来源由原矿工续算；`queue_incremental_child_exit_recovery.json`保留实际退出和恢复证据。恢复命令仍为`caffeinate -i .venv/bin/python -u -m yoyo.datasets.ma_profit_queue`，新的日志为`queue_run_incremental_resumed.log`。


## 增量队列第二、三轮

| 阶段 | 独立事件 | TP | train赢家 | val赢家 | test赢家 | purged赢家 |
|---|---:|---:|---:|---:|---:|---:|
| round001 | 8084 | 1032 | 857 | 102 | 68 | 5 |
| round002，追加1m第二批 | 8322 | 1089 | 899 | 107 | 78 | 5 |
| round003，追加1m第三批 | 8597 | 1152 | 953 | 110 | 84 | 5 |

第三轮完整结果另有SL4637、TIMEOUT2799、INVALID4、UNKNOWN5，lineage错误0；完整池train6795、val1010、test681、purged111。增量缓存8321命中、276次新算，前轮有1个代表被重新NMS替换，不能把缓存命中率当样本独立性。主任务再次执行队列的label/selection校验，第三轮四文件缓存链与冻结选择回执通过；容量仍不足，dataset ledger为空，未启动训练。新增胜者只是监督标签，不是预测或实盘胜率。

第三轮收益SHA为`94982814cb1fca6aec988e4434c304dc2c222e4b7019b5c3e6fbe1e6fa6ce842`；collection/outcome/selection分别冻结于`41315ceb578f22d1572f03f1c37ce1fe6bb378a8`、`4df7d2c993b09d9ca76f221df6676842ca6ee84a`、`f9396cf10f823ded9e6828e8ced1cfd0e39ff83c`。1m导入前16批共399个可用币种；第16批source manifest SHA为`2c4db29db9e4decc38311825b9f07582464784a4bbd3380fbbac4599903cb1b3`。导入覆盖不等于扫描完成或合格事件数量。
