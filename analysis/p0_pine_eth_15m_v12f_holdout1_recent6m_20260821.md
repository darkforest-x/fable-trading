# ETH 15m Pine V12F：最近半年 holdout 第 1 次正式验收（2026-08-21）

> **最终判定：V12F 相对冻结 V9 有改善，但没有通过项目的严格收益门，不能 promote 或实盘。**
> 用户要求的完整最近半年 `2026-02-21` 至 `2026-08-21` 中，V12F 为 **-3.46%**、
> 最大回撤 **24.74%**，优于 V9 的 **-6.93% / 27.45%**，但两者都亏损。纯受保护期
> `2026-05-04` 至 `2026-08-21` fresh-start 中，V12F 为 **+15.77% / 18.74% DD**，
> 优于 V9 的 **+11.60% / 21.69% DD**；然而 V12F 对匹配随机入场的周区块检验
> `p=0.2566`，top-decile 置换 `p=0.1838`，均远高于项目要求的 `p<0.01`。

> **这次是已冻结 V12F 配置第 1 次、也是本轮唯一一次正式 holdout 消耗。** 没有读取或评价
> V12E、TBSL、LR/LightGBM，也没有在结果出来后改 W8、阈值、止损、成本或任何超参。

## 一、直接回答

1. **Pine 优化确实落地了。** V12F 把六条均线的 12 对方向性交叉、最近 8 根净交叉数和
   full-state gate 写进 Pine；本次评估的 Pine SHA-256 是
   `9e03c2959e403632a8db06c66ee43487d7388e0dfdaf31abe5ae32218c7567de`。
2. **它比 V9 好，但不是“半年已经赚钱”。** 最近半年只少亏 `3.47` 个百分点；纯受保护段
   多赚 `4.16` 个百分点且回撤少 `2.95` 个百分点。
3. **改善机制是少做几笔坏交易，而不是创造新赢家。** 两版本共有 56 笔完整半年交易，
   单笔净收益完全一致。V12F 相比 V9 少了 5 笔、因状态变化新增 2 笔；这 7 笔全部亏损。
4. **低胜率是趋势策略结构，不是统计错误。** 受保护段 V12F 30 笔仅 4 笔盈利，平均赢家
   `+11.82%`、平均输家 `-1.41%`，盈亏幅约 `8.38:1`。代价是收益高度依赖极少数大趋势。
5. **严格验收仍失败。** 半年绝对收益为负；匹配对照和排序检验均不显著；官方 TradingView
   逐笔 parity 仍未通过。因此 V12F 只能保留为 paper research candidate。

## 二、批准与一次性消费记录

| 项目 | 记录 |
|---|---|
| Owner 原话 | `批准` |
| 批准范围 | `V12F 配置第 1 次消耗 holdout，区间 2026-02-21 至 2026-08-21` |
| 唯一候选 | V12F full-state W8 gate |
| 唯一对照 | 冻结 V9 |
| 消费编号 | 1 |
| 冻结配置 SHA-256 | `97cb40d4b8dfd792771f000fbd0356922dac9c136e45b79219e85548f1b42eb4` |
| 运行代码 commit | `7b859dabe07f27c1e1f2523c4652622b975f381d`，main，运行时代码 committed + clean |
| 正式开始 / 完成 | `2026-08-21T06:49:33Z` / `2026-08-21T06:53:07Z` |
| 打开的 protected rows | 10,464 根 15m bar |
| 账本状态 | `completed`；9 个物化产物 SHA-256 已记录并复核 |

第一次 shell 命令使用直接文件入口，因项目根目录不在 Python import path 而在 import 阶段报
`ModuleNotFoundError`。当时消费账本、配置和任何结果文件均不存在，数据 loader 尚未导入或执行，
所以不构成 holdout 消耗。随后使用同一份冻结代码的模块入口完成唯一一次正式消费。这个操作失误
在此保留，不把它静默抹掉。

正式完成后运行 `--verify-existing`，得到：`verified 9 artifacts; holdout data not reopened`。

## 三、冻结策略和特征

共同条件：OKX `ETH-USDT-SWAP` 15m 研究代理、本金 500、每单按止损距离承担 1% 资金风险、
13x 杠杆上限、ATR14 × 4 初始止损、3% 最大止损距离、`+1.5%` 触发 break-even、原 cooldown、
确认 bar 后下一根开盘成交、往返成本 0.20%。没有资金费、额外滑点、最小下单量或强平模拟。

V12F 只在 V9 上增加一个冻结 gate：

- 六条线全部只使用收盘价：SMA20、EMA20、SMA60、EMA60、SMA120、EMA120；
- 计算 20×60、20×120、60×120 三个快慢周期组的 SMA/EMA 组合，共 12 对；
- 多头统计最近 `[t-7,t]` 的金叉数减死叉数，空头反向；
- 方向净交叉数 `>=0` 才通过；拒绝的 guarded signal 不反手、不平仓、不消耗 cooldown；
- 所有特征只用信号 bar `t` 及之前数据，未来价格只供冻结的执行回放和标签结果使用。

阈值 0 的准确含义是“最近 8 根中反方向交叉不多于同方向交叉”。零交叉也会通过，所以它是
**方向冲突过滤器**，不是“必须至少发生 N 个交叉”的严格密集度计数。最近半年 94 个 guarded
候选中，V12F 通过 83、拒绝 11；纯受保护期 59 个候选中通过 49、拒绝 10。

## 四、完整最近半年主结果

区间为左闭右开 `[2026-02-21T00:00:00Z, 2026-08-21T00:00:00Z)`。收益是 500 本金的
策略资金曲线复利收益；DD 是 15m mark-to-market 最大回撤。

| 版本 | 交易 | 收益 | DD | 胜率 | PF | 净 bp/笔 | 相对 V9 收益 | 相对 V9 DD |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| V9 frozen | 61 | **-6.93%** | 27.45% | 8.20% | 0.835 | -45.44 | — | — |
| V12F W8 full | 58 | **-3.46%** | **24.74%** | 8.62% | 0.912 | -41.39 | **+3.47pp** | **-2.71pp** |

这是“相对改善、绝对失败”。PF 仍小于 1、每笔净期望仍为负，不能因为 V12F 比更差的基线好
就宣称已经获得可交易收益。

### 月度复利分解

| 月份 | V9 | V12F | V12F−V9 | 说明 |
|---|---:|---:|---:|---|
| 2026-02（部分） | -2.40% | -2.40% | 0.00pp | 三笔相同亏损 |
| 2026-03 | -7.00% | -7.00% | 0.00pp | 路径相同 |
| 2026-04 | -6.98% | -6.98% | 0.00pp | 路径相同 |
| 2026-05 | +0.10% | +0.07% | -0.03pp | V12F 略差 |
| 2026-06 | +0.69% | +0.67% | -0.01pp | V12F 略差 |
| 2026-07 | -6.16% | -5.06% | +1.10pp | 少做坏启动开始体现 |
| 2026-08（部分） | +16.54% | +19.54% | +3.00pp | 共享大趋势，V12F 以更高剩余本金参与 |

![V9 与 V12F 资金曲线和月度收益](../experiments/active/exp-pine-eth-15m-v1/results/charts/v12f_holdout1_recent6m_equity_monthly.png)

该图是完整半年诊断图，不是独立 fresh-start 图。每个 arm/period 的 equity 原表在终点
`2026-08-20 23:45` 有两条状态记录；所有数值汇总和月度表已按时间保留最后一条，图表绘制没有
先去重，所以最右端可能出现轻微垂直跳变。这个可视化瑕疵不改变表中收益/DD，但图不能作为逐点
parity 证据。

## 五、纯受保护期结果

repository holdout 从 `2026-05-04T00:00:00Z` 开始。主判定使用独立 fresh-start replay，避免
把 2–4 月的旧仓位、权益或 cooldown 状态混进受保护段。

| 版本 | 交易 | 收益 | DD | 胜率 | PF | 净 bp/笔 | 相对 V9 收益 | 相对 V9 DD |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| V9 frozen | 33 | +11.60% | 21.69% | 12.12% | 1.417 | +20.83 | — | — |
| V12F W8 full | 30 | **+15.77%** | **18.74%** | **13.33%** | **1.642** | **+35.31** | **+4.16pp** | **-2.95pp** |

连续状态诊断得到同样的段收益和 DD，因为 `2026-05-04` 没有跨界持仓，两个版本在切点的权益
倍数也恰好同为 `0.833900`。这次相等是数据事实，不代表以后 fresh-start 和 continuous-state
可以混用；报告仍以 fresh-start 作为独立验收口径。

### 低胜率与长尾依赖

| 版本 | 赢家 / 总笔数 | 平均赢家 | 平均输家 | 平均盈亏幅 | 最大赢家 | 最大输家 |
|---|---:|---:|---:|---:|---:|---:|
| V9 | 4 / 33 | +11.82% | -1.39% | 8.48× | +21.27% | -3.20% |
| V12F | 4 / 30 | +11.82% | -1.41% | 8.38× | +21.27% | -3.20% |

V12F 的 30 笔里有 26 笔止损、3 笔反转退出、1 笔期末退出。两个最大单笔净收益是
`+21.27%` 和 `+17.77%`；按同一已实现账本机械移除最大一笔 PnL、且不重放后续仓位规模，
V12F 的受保护段会从 `+15.77%` 变为约 `-8.70%`。这不是另一个策略回测，但足以说明结果仍
高度依赖极少数 runner。胜率低本身不是应被机械优化的目标；真正目标是少做震荡假启动，同时
不能砍掉这些大趋势。

## 六、V12F 实际删掉了什么

完整半年中，两版本共有 56 笔交易，它们的方向、信号时间和单笔净收益完全一致。差异只来自
full-state gate 改变后续状态：

- V9 独有 5 笔，全部亏损，`project_net_return` 合计 `-5.05%`；
- V12F 独有 2 笔，全部亏损，合计 `-1.34%`；
- 净效果是把 5 个较差入场换成 2 个较小亏损入场，保留更多本金参加 8 月共享趋势。

这支持“W8 能过滤部分假启动”的机制解释，但没有证据表明 W8 找到了新的大趋势赢家。换句话说，
它改善了损失管理和状态路径，不是提高了大趋势命中率。

## 七、匹配随机对照

每笔候选配 3 个不复用随机入场，匹配同一 ETH、同 UTC 时间块、同波动桶、同方向、同持仓
时长、同障碍和同 20 bp 成本。主表使用固定 anchor assignment；另跑 32 个预定 assignment
seeds，避免挑选有利随机种子。周区块 sign-flip 使用 20,000 次重采样。

| 区间 | 版本 | 候选净 bp/笔 | anchor 对照 bp/笔 | 候选−对照 | 周区块 p | 32-seed 中位超额 | p05～p95 | 正超额种子占比 |
|---|---|---:|---:|---:|---:|---:|---:|---:|
| 完整半年 | V9 | -45.44 | +1.03 | -46.47 | 0.4204 | -40.26 | -68.86～-14.40 | 0% |
| 完整半年 | V12F | -41.39 | -10.76 | -30.63 | 0.3730 | -36.89 | -68.08～-13.71 | 0% |
| 纯受保护期 | V9 | +20.83 | -32.04 | +52.87 | 0.2460 | +26.49 | -3.47～+60.64 | 90.63% |
| 纯受保护期 | V12F | +35.31 | -6.42 | +41.73 | 0.2566 | +34.16 | -2.84～+63.45 | 93.75% |

受保护段点估计有利，但 `p05` 仍略为负，周区块 p 也未接近 0.01。完整半年在全部 32 个对照
分配中都为负超额。因此匹配随机对照不支持“V12F 已有稳定 alpha”的说法。

## 八、AUC、top-decile 与单特征基线

本轮没有模型。统一指标使用 V9 振荡器绝对值作为单特征排序诊断，冻结 V9 是策略基线；它不
等于 W8 净交叉分数，不能拿 AUC 给 W8 gate 背书。

| 区间 | 版本 | AUC | top 10% 笔数 | top 10% 净 bp/笔 | top 10% 胜率 | 置换 p |
|---|---|---:|---:|---:|---:|---:|
| 完整半年 | V9 | 0.475 | 7 | +115.94 | 14.29% | 0.1957 |
| 完整半年 | V12F | 0.487 | 6 | +136.93 | 16.67% | 0.1876 |
| 纯受保护期 | V9 | 0.388 | 4 | +339.85 | 25.00% | 0.1870 |
| 纯受保护期 | V12F | 0.404 | 3 | +456.46 | 33.33% | 0.1838 |

所有 AUC 都低于 0.5，所有置换 p 都远高于 0.01。top-decile 的正收益只是少量大赢家落入顶部，
不是可靠排序能力。项目“top-decile 扣成本为正且置换 `p<0.01`”的严格门没有通过。

## 九、与冻结前证据对照

| 区间 | V9 收益 / DD | V12F 收益 / DD | 结论 |
|---|---|---|---|
| 2023 | +70.50% / 26.48% | +117.84% / 22.55% | 开发证据有利 |
| 2024 | +109.26% / 12.22% | +110.61% / 10.49% | 时间外开发证据有利 |
| 2025-01～2026-02 | +22.82% / 20.03% | +31.41% / 18.22% | final-preholdout 点估计有利，p=0.3100 |
| 2026-02-21～2026-08-21 | -6.93% / 27.45% | -3.46% / 24.74% | **绝对失败，相对改善** |
| 2026-05-04～2026-08-21 | +11.60% / 21.69% | +15.77% / 18.74% | **点估计有利，统计门失败** |

前半段 2–4 月连续亏损，后半段主要靠 8 月大趋势修复。V12F 没有消除 regime dependence，只是
在某些震荡段少亏。把受保护段单独的 `+15.77%` 当成“最近半年收益”会是错误陈述。

## 十、数据质量与无前视审计

- 有界数据共 162,274 根，`2022-01-03T15:30:00Z` 至
  `2026-08-20T23:45:00Z`；源帧 SHA-256
  `7bccd1fc8ef264a8c474d7b7d8e442dbe53bfe24531995da0efc0197c4b5cfb7`。
- 本地只读前缀 158,499 根；缺少的 3,775 根通过 OKX history-candles 从冻结终点向前分页，
  38 次请求、只在内存合并，没有写入 K 线文件。
- API 返回时间必须严格早于 `after` cursor 和批准终点；未确认 K 线、单页重复、跨页重复均
  fail-closed。实际越界、重复、未确认、空值、非 15m gap、OHLC 约束错误全部为 0。
- 请求区间前有 144,898 根 warmup，超过冻结的最少 400 根要求。
- `entry at open[t+1]`；所有信号和 W8 特征只使用 `t` 及以前。没有随机时间切分或未来特征。
- OKX 官方 `after` 语义说明见 [OKX API guide](https://app.okx.com/docs-v5/en/)。

## 十一、测试与 Luna Max 双重审计

本地相关测试为 `52 passed`，覆盖冻结身份、仅 V9/V12F 可达、终点边界、API 多页 cursor、
重复时间、未确认 candle、400 根 warmup、原子 `O_EXCL` ledger、精确结果集合以及 Pine/Python
状态语义。

可见 Codex 任务使用 `gpt-5.6-luna`、`thinking=max` 做消费前只读终审。它确认 runtime
provenance、重复值 fail-closed、终点与 warmup、API tail、原子账本、fresh-start/continuous
分离均成立，裁决 **GO**。正式产物完成后又发起结果审计；其结论只用于核对完整性和防止夸大，
不允许据 holdout 提参数建议或选择新版本。结果审计独立复算 9 个 artifact hashes、182 trades、
546 controls、182 pairs、128 个 sensitivity rows、22 个 monthly rows，以及汇总、ranking p 和
20,000 次周区块 sign-flip，完整性判定 **PASS**，经济项目门判定 **FAIL / REJECTED**。

## 十二、风险与诚实声明

- 完整半年绝对收益为负，V12F 只是在失败基线上少亏。
- 纯受保护段只有 30 笔，4 个赢家；去掉最大已实现 PnL 后同账本收益转负，长尾依赖严重。
- 匹配对照 `p=0.2566`、排序置换 `p=0.1838`，严格项目门失败。
- W8 阈值 0 允许零交叉通过，不是严格“六线必须密集交叉 N 次”。
- 研究源是 OKX swap proxy，不保证与 TradingView 上具体 venue 的 `ETHUSDT.P` bar 相同。
- `official_tradingview_parity_passed=false`；Python stop-first、订单时序尚无官方逐笔导出对账。
- 完整半年与 fresh-start 是重叠窗口的两套独立 control assignments；各自内部不复用，但跨口径
  有少量重复，禁止合并成独立样本。CSV 也没有单独落候选端 ATR quintile，无法只靠结果表完整
  重建该匹配字段，必须结合冻结 runner 审计。
- equity CSV 的每个 arm/period 在终点有两条状态记录。汇总已去重、图未去重，因此现有资金曲线
  仅作诊断图，不能用于逐点 TradingView parity。
- 已扣固定 20 bp 往返成本，但未计资金费、额外滑点、最小下单量、流动性冲击和强平。
- 这次 holdout 已经消费，不能再用于 W8 窗口、阈值、TBSL、LR 特征或选择版本。
- 没有训练/评分 LR 或 LightGBM，没有 promote、deploy、改 `models/ACTIVE`、写 forward log 或
  触碰真金账户；所有 eligibility 继续为 false。

## 十三、结论与允许的下一步

V12F 可以继续作为**唯一的 15m paper 候选**，因为它在 preholdout 和本次受保护段都相对 V9
改善收益与回撤；但它没有资格成为生产策略，因为完整半年仍亏损且统计门失败。

本次结果出来后，不再在这个 holdout 上继续优化。下一步如果继续，只能是：

1. 冻结 V12F 不变做真正新的前向 paper collection；
2. 在不读取本次 holdout 作为训练/选择集的前提下，等 P0/P1 允许后另立 Pine 专用判断层协议；
3. 先完成官方 TradingView 编译和逐笔 trade-export parity；
4. 任一新 W8、交叉次数、TBSL 或 LR 版本必须使用新的开发/前向数据，不能回头从本页挑参数。

## 十四、产物与复核命令

主产物：

- `experiments/active/exp-pine-eth-15m-v1/results/v12f_holdout1_recent6m_trades.csv`：
  182 行，含两个版本的完整半年与 fresh-start；完整半年去重后为 V9 61 + V12F 58 = 119 笔；
- `v12f_holdout1_recent6m_controls.csv`：每笔 3 个 anchor controls；
- `v12f_holdout1_recent6m_pairs.csv`：逐笔候选与 control mean；
- `v12f_holdout1_recent6m_control_sensitivity.csv`：32 个 assignment seeds；
- `v12f_holdout1_recent6m_monthly.csv`、`v12f_holdout1_recent6m_equity.csv`；
- `v12f_holdout1_recent6m_summary.json`：冻结配置、数据质量和全部汇总；
- `v12f_holdout1_consumption.json`：第 1 次消费账本和 9 个 artifact hashes。

唯一允许的现有产物复核不会打开 holdout：

```bash
cd /Users/zhangzc/fable-trading
python3 -m scripts.backtest_pine_eth_15m_v12f_holdout1 --verify-existing
```

历史上的一次性正式命令是下面这条，**已经消费完成，不得再次运行**；任何新的数据读取必须重新
获得 owner 明确批准并登记新的消费编号：

```bash
python3 -m scripts.backtest_pine_eth_15m_v12f_holdout1
```

报告生成与非数据复核：

```bash
python3 scripts/md_to_html.py \
  analysis/p0_pine_eth_15m_v12f_holdout1_recent6m_20260821.md \
  --out-dir analysis/html

python3 -m pytest -q \
  tests/test_backtest_pine_eth_15m_v12f_holdout1.py \
  tests/test_backtest_pine_eth_15m_v12_preholdout.py \
  tests/test_generate_pine_eth_15m_optimized_variants.py \
  tests/test_pine_allin_v7_backtest.py \
  tests/test_pine_cross_features.py \
  tests/test_research_pine_eth_15m.py
```
