# V9 report contract

Audience: technical, as requested by the owner's detailed rule/backtest/failure
analysis workflow. Delivery: portable HTML in this Codex file workspace, using
one canonical Data Analytics artifact. Historical reports remain unchanged.

Required structure mapping: title; technical summary; definitions before results;
fixed-population and matched-control comparisons; dedicated experimental clock
rules; paired distribution with adjacent interpretation; loss transitions;
four chronological folds; serial intentions; robustness and uncertainty; next
steps plus unresolved questions. Reproduction retained because repo explicitly
requires complete commands. Each peer section has its own native markdown block.

Chart contract: one full-width distribution bar chart over exactly286 paired
requests, 12 fixed signed numeric bins including a zero atom, both open tails
and unknown. Single blue measure, no redundant legend, direct count labels,
count-axis zero. Count is not density: unequal bin widths explicitly disclosed.
Retain bin limits, means, extrema, sum, full denominator and unknown count.
Native bar renderer and semantic fallback only; no custom JS/SVG or installed
browser. Other quantitative sections use exact-lookup tables for multi-metric
rules, gates, win/loss and fold/control reconciliations; no four-point trend line.

Source metadata: authored MD, reviewed summary, saved case_delta CSV and Python
builders, root-relative canonical identities; actual SQLite count query reused
from tested V8 helper but all feature/clock definitions explicitly V9. Markdown
SOURCE directives only for blocks fully supported by one facts source. No
unverified browser/phone claims: disclose structural_only if packaged verifier
lacks a compatible browser. Canonical HTML is sole reader-facing delivery mode.
