# V12：双向时序配对与可追溯绘图

Owner 2026-09-20 明确要求七项修订：简洁无底色 break+spike、每次突破对应线、突破后持续在线上再出 V9 也配对、默认 BB 密集区、退出虚线与 R、HYPE/BTC Bybit 漏线核对、分周期结构参数。本轮是经 owner 授权的功能组合，不是多参数收益实验。

保留已核验的主点/局部 C 两族及逐 bar ATR 快照，不修改 V9 入场、风险障碍、追踪退出、成本或生产扫描器。15m/30m/1h/4h 独立输入默认维持同一已审几何；每个 request.security 上下文读取自身周期，不声称最优参数。

新增持线证据：突破确认后每根收盘严格在线上，收盘等于/低于线、断档、非正线价或超过最长存活即永久撤销。只在当根 V9 最终多头确认且有效未消费框内完成配对；不能恢复已撤销证据，不能回填。上级只使用已收盘输出，并从本图第一根可见收盘开始检查。相同机会的一批待配对线共同消费，避免下一框复用。

绘图：普通 break 与联合共用有限历史组，线和标记原子清理；仅联合模式的新框当根可见，旧框同根退出不得被新框的联合揭示。退出使用已结束框自己的冻结 entry/risk/side 与 exitReferencePrice，标毛 R，tooltip另列成本后 R。图形组上限缩小以控制 Pine 对象回收。

验收：原 V9 代码契约（仅豁免两个 BB 显示默认值）、原找线/无前视/HTF契约，合成迟到 V9、线上等值、回线后反弹、断档、过期、多框去重、HTF可见延迟与前缀不变性；原生编译与实际图表。截图漏线需真实 OHLC 与锚点诊断，不能凭画面宣布已修复。先提交 builder，再运行市场案例。

风险与诚实声明：本轮不做盈利优化；收益/AUC/随机入场对照不适用，改用确定状态序列反例与前缀不变性；画线成功不等于交易优势。原生编译/截图案例验证以最终回执为准。
