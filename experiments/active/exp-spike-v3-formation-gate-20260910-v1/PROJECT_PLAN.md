# V3 第四项独立假设：突破之前六均线带的收拢过程

## 授权与顺序

沿用 Owner 2026-09-10 开启目标模式、尽可能减少信号且保留正确启动的授权，以及此前任何日期均可研究的授权。仅对原版 SPIKE V1 与当前 V3 作比较；不把中间版本作基准，不改线上监控、Bark、TG、ACTIVE、订单或其他会话任务。

13项单门、A/B两项结构过滤和C确认后抑制均未解决目标。D为第四项固定结构探索，不是盲测，不是已找到的最优参数。此前绝对密集门本身仅保留944/1463及时正例，更严格的该门子集不能满足90%；D考察先后收拢过程，与绝对宽度和交织快照不同。本计划、源码、测试、预审先提交，再运行D市场检测与评分。本固定配置第1次消耗授权holdout，历史已被多轮查看，不能称独立样本外。

## 唯一改变

使用原来的 SMA/EMA 20、60、120 六条均线，令 W[j]=max(6MA[j])−min(6MA[j])，单位为该币价格，不除以当根ATR。

在原V3候选根i，formed[i] = median(W[i−6:i]) <= median(W[i−12:i−6])。

- 12沿用原V3历史窗口；6为等长拆半；相等保留，不搜索阈值或窗口。
- 仅使用此前12根完整有限数据，任何一个历史W缺失都不跳过；检查这12根到当前根的小时连续性。
- 当前根W或ATR不会改变formed；当前W缺失会使随后包含它的12根形成过程不合格。原V3 ready等条件仍须满足。
- 保持V3完整raw条件上升沿。early = raw_edge AND 原accepted-only12根冷却 AND formed。被拒绝的edge仍消耗本次raw上升沿，但不重写接受信号冷却时间。不能等formed后来变True时补发同一个edge。
- 原父0..3根确认仅附属于真实接受父。确认阈值、时钟不变；无A/B/C占用抑制，无第二新过滤。
- 不改变退出、风险、成本或交易规模。

## 固定评价

沿用同一认证278个OKX历史1H合约（不含BTC/ETH），UTC [2026-07-10,2026-09-09) 61天。原14904行完整标签及8046研究锚点保持逐字节一致，1660正例、6240负例、146未知，947个大幅正例子组不改。不按当前涨幅榜选币，不重命名原正例使结果过关。

四项必要门：相对于原V3的12042个同根合并标注时点减少至少50%（<=6021）；保留原1463个及时命中中的至少90%（>=1317）；原947个大幅正例及时召回>=80%（>=758）；匹配随机净超额>0且Holm校正p<.01。及时仍要求在原锚点t..t+1有真实新父预警。已有持仓/参考、后续确认、提前预警不得补进该分子。

报告原V1历史结果、原V3与D同表，并链接A/B/C失败结果。分别列独立父数、确认升级数、同币同根合并标注数，不能把显示合并称为删除假启动。同根V3父子已经合并，不能把2153次再算一次新增优化。

prepare全局冻结检测与共同随机对照后再evaluate；同币×UTC周×因果ATR桶最多3个控制，统一排除V3/D当根及过去12根事件，不排除未来信号。执行原次根开盘、过去5根结构低点减0.2ATR或2ATR最小风险取宽、向外tick、收盘2R激活4ATR跟踪且次根生效、20bp往返成本，无固定止盈。确认升级不重复算交易。执行缓存必须源文件/tick/位置/截止/执行契约一致。

报告全期、前31天/后30天、逐周和固定案例夜；匹配随机超额、净胜率、毛净均值、资产×周置换、至少A/B/C/D四个固定假设Holm。明确此前13门探索未被这局部校正覆盖；不因检验失败改参数。无训练，val AUC不适用；量比/TR为描述性单特征对照，不用于选择门。事件可重叠，不伪称账户收益或组合最大回撤。

## 测试与交付

测试水平宽度、持续扩散、同一宽度集合前后反转、当前ATR暴涨不产生收拢、当前W不进入形成过程、缺失历史、不连续重新预热、raw edge拒绝/冷却与确认父身份、未来追加不改历史。另一个实现独立审核源码和实际输出。

HYPE/NEAR/PEPE固定96根全局图保留原V1和V3，标真实确认时间、零轴、六MA、量能；失败案例不删。Markdown立即转HTML，注册所有结果。若D达不到目标，按失败记录，不调半窗/中位数/方向；不能把少标文本的显示改进冒充正确率改善。未通过不替换当前指标。
