# SPIKE V1 盈亏框显示修复

Owner 2026-09-10追问：强劲爆发V1为什么没有止盈止损框。

原因：原显示仅有保护/入场线和94%透明度的初始风险fill，没有独立盈亏双框；线用前根已生效状态，信号当根尚无风险填充。

本次只新增状态消费者：信号当根红色初始风险框、绿色3R观察框，确认峰值超过3R时扩展，保护触及后固定历史框。保留组数默认8，上限20，time坐标避免长期持有的bar_index距离限制。3R不作为退出；所有价格仍为信号收盘参考，不是交易回执。副图与六均线不改。

冻结回测源文件 `yoyo/evaluation/pine/spike_burst_v1.pine` 不改；当前TV显示发行源码为 `yoyo/evaluation/pine/spike_burst_v1_display.pine`，同名私有指标更新。移除显式显示块后必须与原文件逐字节一致，显示层不得写原信号/风险状态。

验收：两项源码边界契约；TradingView原生编译、ETH4H历史框截图、保存版本确认。首个编译遇到Pine保留字text，改为caption后重试；记录真实结果，不把Python源码检查冒充编译。

不新增参数搜索或收益评分。Owner此前允许任意历史时段；仅看已讨论的ETH/USELESS等显示案例，无新收益holdout消耗或模型训练。上线通知、执行、Bark不改，不下单。
