# V3 第五项固定假设：已收盘4H相对动能否决1H弱启动

## 授权与顺序

沿用 Owner 要求目标模式降低噪音、保留正确启动、多周期背景及任何日期可研究的授权。原版SPIKE V1与V3保持固定对照，不改其他任务、线上Pine、监控、Bark/TG、ACTIVE或交易执行。13个单门及A/B/C/D结构实验均未同时达到目标；E是第五个顺序探索假设，不能称盲样本外或最优参数。每固定新配置第1次使用授权holdout，不再重跑旧配置。

本轮先完整提交计划、detector、runner、测试和实际落盘的独立预审收据，再prepare市场。收到PASS消息不等于JSON存在或已进提交，必须实际核验HEAD内容。prepare冻结所有事件和共同随机日程后才evaluate，report builder也先提交再渲染。

## 唯一新门

对原V3的1H父预警：仅当背景已就绪且4H md<sb时拒绝。4H md>=sb保留，双零中性也保留；md和sb都为负但md>=sb同样可通过，不能称4H多头或刚金叉。

4H不足341根完整bar或数据无效时为unknown，保留原V3候选但明确不是背景通过。绝不能把数据不足过滤掉当降噪。341沿用原bar_index>=340稳态预热，不用刚能计算34/9的更短历史替换。时间覆盖审查已认证278份历史features，但尚未计算E信号或收益：259在研究起点就绪，269在期末前就绪，其余保留unknown。这些是覆盖信息，不是绩效。

每根的因果时序：

1. 原1H连续segment内，每4根完整的OHLCV按UTC00/04/08/12/16/20开盘分组，O首/H最大/L最小/C末/V求和；首尾不完整组丢弃。不把1H MD取均值。
2. 在4H OHLC上重新计算原SMMA34(high/low)、ZLEMA34(hlc3)、MD及SMA9信号线；使用原数学初始化，gap后递推和计数重新预热。无新ATR过滤或阈值。
3. 1H decision=open+1h，只取同一1H连续segment内最后一个4H close<=decision的完整组，允许恰好同一收盘。11:00开盘的1H在12:00确认可用08–12闭合4H；11:00确认不能用该尚未闭合组。
4. 断档后第一根即unknown，不得因新完整4H尚未产生而继承gap前旧ready背景。记录htf_close/age小时/count/ready/md/sb/status；正常完整映射age0..3。
5. 原完整raw条件的上升沿与accepted-only12根冷却不变；仅接受父时加E门。拒绝消费rawedge但不推进接受冷却，不在4H后来好转时补发旧edge。原0..3确认只升级已接受父，不重新检查E，确认绝不回填父根。

## 不变评价与交付

沿用D研究认证输入：278个历史OKX合约1H多头，不含BTC/ETH，UTC [2026-07-10,2026-09-09) 61天，14904原标签文件逐字节保持；研究8046锚点=1660正例/6240负例/146未知，947大幅正例。不得重定义标签或按当前榜单换池。未知4H只作诊断分层，全原分母不变。

四项必要门仍为：原V3同根合并12042标签至少减少50%即<=6021；原1463及时命中保留>=90%即>=1317；947大事件及时召回>=80%即>=758；匹配净超额>0且五假设Holm p<.01。及时仍要求锚点t..t+1有真实新父，已有跟踪/提前预警/后续确认不补进分子。

同币×UTC周×因果ATR桶最多3随机入场，统一排除V3/E当根及过去12根父子事件，不看未来。经济只算父，次根开盘，旧5根结构低点−0.2ATR/2ATR风险下限取宽，向外tick，收盘2R激活4ATR跟踪下根有效，20bp往返成本，无固定止盈。D缓存须所有源/tick/位置/截止/执行契约匹配；未缓存位置用同执行器。

原V1历史对照和原V3/E同表，分别报告独立父、升级、同根标签、4Hunknown/known/opposed数量与拒绝数；全期、前31/后30天、周、固定夜，报告相同标签召回、净胜率/毛净均值/匹配超额/资产周置换。A/B/C/D冻结p与E共同Holm5；此前13个门和顺序探索仍披露，局部校正不变成盲测。无训练，val AUC不适用，RV/TR描述单特征AUC/top10%仅作对照不调参；独立重叠事件不冒充账户NAV/最大回撤。

测试exact4H收盘、未闭合尾、头部不完整、341根边界、同segment映射、gap立即unknown且重新预热、双零/双负但md>=sb、unknown保留、拒绝不推冷却/不补edge、children真实父及前缀稳定。严格预审收据先提交再跑。原V1/V3/E的HYPE/NEAR/PEPE固定96根全局图保留全部真实标记、零轴和六MA，标清背景与当前信号不是同一级别。

报告MD立即转HTML，QA与注册完整。E可能阻挡领先4H反转的1H真启动，少信号不等于少假启动；失败不改341/34/9/不调md门槛追结果，不替换用户当前指标。
