# V8 six-source filters: prospective definitions on reused historical data

## Owner authorization and scope

Owner explicitly requests testing the supplied six-batch V1 observations on V8, and has already permitted any historical dates. Use the existing authenticated Binance/OKX/Gate 3531-stream30m/1H/4H pool, 2024-09-10 inclusive through2026-09-10 exclusive UTC; split2025-09-10. Every sample/full run is another exposure to previously studied data, never fresh blind validation. No live, Pine, notification, model or account changes. No new branch/worktree or dependency installation.

## Preregistered independent interventions

Keep frozen V8 admission, raw reverse exits, original stops/trail and nominal cost0.002. Do not combine gates and do not import the previous BE/tier exit experiments.

1. Reject signal-close relative volume>50; source denominator must match frozen feature.
2. Reject actual initial risk / pre-entry open quote>0.30 (30 percent, not0.3percent). An execution gate evaluated at the opening quote before filling, not a future-label selector. Never shrink the stop.
3. Reject USDC underlying assets only, not every symbol settled inUSDC.
4. Reject sourced stock-linked underlyings; freeze a documented taxonomy before inspecting policy outcomes. Gold and ambiguous crypto tickers are not automatically equities; unknown classifications are explicit.
5. Reject new confirmations/executions at hour00 UTC (source correction; do not call it Beijing midnight).
6. Reject Sunday on the same UTC decision clock.
7. Source-corrected joint RV>50 AND TR/priorATR>10, separate fromrule1. The quoted Bonferroni p0.0001 refers to this joint zone, not standaloneRV.

Sensitivity only: rule5/rule6 onAsia/Shanghai, retaining both outcomes without selecting the better clock. Nine fixed gates in total;27 gate-by-timeframe comparisons define the multiple-comparison family. Monthly/week resampling and asset contributions are descriptive, not further parameter searches.

## Holding-duration claim

Realized holding<4h is a future outcome and cannot filter entry. First reproduce that slice as explicitly noncausal attribution. Separately test one causal operational alternative: at the first completed bar where an open trade reaches4h, if cumulative observed MFE<1R AND current gross closeR<=0, schedule exit at next open. Old stop/gap and raw reverse exits retain priority. Previously stopped trades remain losses; there is no4h guaranteed-hold rule. This is one new fixed exit rule, not an admission gate, not the claimed349R recovery.

## Evaluation and controls

Show baseline and each gate separately for full period, pre/post year, month, timeframe, side; trade/signal counts, unknown/censored coverage, sum/mean netR, net win rate, PF, eventR drawdown, original10R winner retention, saved losses and sacrificed winners. Fixed-event subtraction is labelled as such; actual serial replay must expose new or lost subsequent entries and leave raw reverse feeds untouched. EventR is not account return and current surviving catalogues are not a delisting-complete universe.

Baseline must match saved authenticated V8 serial_baseline from precedingBE05 full_v3 by identity, entry, risk, exit and return. SHA receipts bind inputs/outputs. Test same-bar stop priority, excluded opposite still closes, time-zone day roll,30percent units, USDC quote versus base, new serial entry after rejection, and missing features. Do not infer missing equals pass without an explicit unknown policy.

Use a matched random-deletion null on frozen baseline entries to distinguish selective rejection from simply reducing exposure, matching venue,timeframe,side,calendar month and volatility bucket where observable. This controls entry selection within the signal pool, not full strategy alpha versus random market entries. Asset-wide exclusions cannot be tested by within-same-asset deletion; their interpretation and concentration must be explicit. Report corrected exploratory p and right-tail retention, not just gross recoveredR. Statistical/algorithmic evidence does not prove the proposed FOMO/session mechanism.

## Ownership

Parent owns this plan, registries, synthesis, controls, HTML andNotion. TerraHigh v1_v8_be05 owns yoyo/evaluation/spike_v8_six_filters.py, matchingtests andadmission/. LunaMax filter_claims_audit isread-only on claim provenance. A separate exit implementation, if delegated, owns exit-only module/tests/exit/ and must not alter the admission engine.
