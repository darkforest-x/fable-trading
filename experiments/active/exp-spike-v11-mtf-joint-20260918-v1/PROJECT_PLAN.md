# exp-spike-v11-mtf-joint-20260918-v1

## Owner 指令（原文）

2026-09-18：「再写一版本v11 逻辑调整 15min级别可以看1h级别的趋势线突破信号 … 1h级别可以看4h级别突破信号」；
随后「回测一下」。Pine：`yoyo/evaluation/pine/spike_burst_v11.pine`（4e387d4f2c，未在 TV 编译）。

## 是什么

V11 = V10.4 的联合规则 + 第二个突破来源：上级周期（15m←1h、1h←4h）的下降线收盘突破。
SPIKE 只来自本图；两事件间隔 ≤6 根本图 K、第二事件当根全部门槛、一次 SPIKE 只消费一次（两来源共用）。
上级突破只在上级 K 收盘后的第一根本图 K 可见（Pine `request.security(..., f[1], lookahead_on)`）；
那根本图 K 缺失则该突破不用。上级引擎：同一套找线/校验/突破，已突破的线立即离池。

## 事前登记的臂（全部上表）

| 臂 | 定义 | 作用 |
|---|---|---|
| `v9_long` | 所有 V9 多头 | 基线 |
| `v10_4` | V10.4 联合（仅本周期线） | 必须逐笔复现六周期报告的 15m/1h 联合结果 |
| `v11_htf_only` | V11「仅上级周期」 | 上级来源单独的贡献 |
| `v11_both` | V11 默认「本周期+上级周期」 | **Owner 要的 V11（主臂）** |

出场（V9 fixed-entry：次根开盘、5 根低点−0.2ATR/收盘−2ATR、2R 后 4ATR 追踪、原始 V9 空头平仓）、0.2% 往返、
每币×周期一笔、随机对照（种子 91918）全部与 V10.4 六周期回测相同。不调参、不搜窗口。

## 数据与窗口

原 638 个 Binance 永续 5m 档案，本图与上级 K 都由同一份 5m 聚合；信号收盘 [2024-09-10, 2026-05-01)，前后段分界 2025-09-10；
预热 1500 根（各自周期）。批量计算前检查所有文件没有 ≥2026-05-01 的行。**holdout 消耗 0。**
Owner 截图里的 HYPE 2026-09-17 在 holdout 期内，本地没有也不读，本轮无法核对该例。

## 判定（沿用 V9/V10/V10.4，每个本图周期单独判 `v11_both`）

全期与后段每笔净 R > 0、相对匹配随机的配对超额 > 0 且月块 p < 0.01。
另报 `v11_both − v10_4`、`v11_both − v9_long` 的每笔净 R 差与月块共同重抽样 95% 区间（种子 91509，2,000 次；描述性）。

## 事前预期

**预期不通过。** V10.4 在 15m/1h 的后段每笔均为负（−0.25R / −0.08R），V9 多头后段也为负；
上级来源只是多了一类入场，同一段市场下预计仍为负。这 20 个月的 15m/1h 已被多次读过，**不是盲测**。

## 纪律

不训练、不 promote、不改仓、不动真金、不 push、不读 holdout。
