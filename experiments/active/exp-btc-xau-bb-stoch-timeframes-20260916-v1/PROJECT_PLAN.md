# BB × Stoch 换市场换周期：BTC 5m/1m、XAU 1m

## 授权与唯一变量

Owner 2026-09-16：「测试 btcusdt.p 1min 5min呢」「还有 xauusdt.p 1min级别」。
**规则一个字不改**——BB200±2σ 总体标准差、原 Stoch5/3/3 极区箭头、整根含影线严格带外、
3% 初始止损、对侧触轨平 50%、尾仓移到保本、完整反向组合退出、下一根开盘成交、
每边 0.1% 费用、固定 1 单位归一化，全部沿用 ETH 冻结值。
**唯一变量是「哪个市场 + 哪个周期」。不做任何参数搜索。**

3% 止损与 BB200 是为 ETH 5m 定的。放到 1m 上，3% 相对 1m 波动是极宽的止损、
BB200 只覆盖 200 分钟——**这是照搬的后果，本轮如实测量，不替 Owner 先行调参**。
若结果差，不能据此说「参数没调好」，也不能顺手调；要调是另一轮，需 Owner 指定并单独冻结。

## 数据

| 组合 | 源 | 说明 |
|---|---|---|
| BTC 5m | `data/kline_deep_5m/okx_BTC_USDT_SWAP_5m_*.csv` | OKX 官方月度 1m 档案聚合 |
| BTC 1m | `data/kline_deep_1m/okx_BTC_USDT_SWAP_1m_*.csv` | 档案原生分辨率 |
| XAU 1m | `data/kline_deep_1m/okx_XAU_USDT_SWAP_1m_*.csv` | OKX 黄金永续；XAUT 无档案数据 |

请求区间 2024-01 → 2026-04，`--archive-max-exclusive 2026-05-04T00:00:00Z`；
实际覆盖以各自 receipt 为准（XAU 上市较晚，缺月如实记录，不回填不外推）。
**holdout 消耗 0**：读价经冻结读取器，其 `end > 2026-05-01` 硬门未放宽。
这三段历史此前都没被本策略用过，**本轮看过即已花掉**，故全程零参数搜索。

## 组

每个 (市场, 周期) 跑三组，各自只比参照多一个变化：
`v2_baseline`（保本价=入场价）、`be_cost`（参照，保本含 0.2% 往返成本，Owner 已批）、
`be_cost_rsi_line`（＋RSI14 线区域门，多需 <30、空需 >70，同根）。
门只过滤新入场；反向退出一律用未过滤信号。主成交路径固定 TV 近端极值优先。

## 必报与对照

每笔序贯入场提前抽 5 个同品种同方向同 UTC 月同因果波动桶的随机入场，先冻结抽样再读结果，
不因亏损或未平仓重抽；按 UTC 月块符号翻转做单侧检验。另按 ETH 那轮的口径给出
**0/2/5/10 bp 每边的精确重算**与盈亏平衡费率 `f* = Σ毛盈亏 / Σ成交名义`。
必报：完整笔数、毛/净 R、每笔净 R、净胜率、PF、已实现回撤、最大连亏、退出构成、
配对随机超额与 p。无排序模型，AUC / top-decile 不适用，不编造。

**跨市场跨周期不可直接比总额**：笔数和每笔风险口径不同，比较只看每笔净 R 与 f*。

## 交付

先提交 builder/config/计划/测试再读价。报告 `analysis/p1_btc_xau_bb_stoch_timeframes_20260916.md`
立即转 HTML。不训练、不 promote、不改仓、不动真金；本轮不产生任何实盘资格。
