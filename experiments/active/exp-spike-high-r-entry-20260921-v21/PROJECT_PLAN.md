# SPIKE High-R Entry V2.1：按过去风险距离分布筛选

Owner授权目标仍是提高普通胜率、优先捕获最终净R>10。2026-09-21，V2的20根突破单变量回放失败：全期胜率29.33%→30.21%，10R比例0.88%→0.82%；后段平均净bp仍为负。V2原始版本和失败结果完整保留。

## 新假设的来源与暴露

V2预先列出的单特征诊断中，负的信号参考风险比例全期AUC约0.608、后段约0.619，排序前10%的10R比例约为原均值两倍。这是已经看过全历史后的探索线索，选择该方向本身受已暴露数据影响，不能宣称前后切分是盲验证，也不能直接把全批次排名前10%当可执行规则。本计划在V2.1阈值构建与市场回放前固定，不继续搜索百分位或窗口。

## 唯一变量：多头准入

- 原V9多头信号的参考风险比例，使用信号收盘价、当时ATR和原5bar极值计算；不读实际次开价，不缩小止损或改变R分母。
- 每个UTC月份、每个周期（30m/1h/4h）预先计算一个阈值：此前3个完整日历月中所有原V9多头候选的正有限参考风险比例的第10百分位（线性分位数）。当前月份和之后的数据不参与。最低100个候选，并要求整个历史月窗处于原数据时间范围内；不足时unknown拒绝，不向后借数据。
- 新月份阈值在月初即可计算，按信号收盘时刻所属UTC月份取值。允许条件为reference_risk_fraction<=cutoff。阈值是无标签统计量，没有拟合预测模型。
- 只改变多头允许入场；空头许可、原始反向退出、初始止损、2R收盘启用4ATR保护、20bp成本和串行状态全部保持原V9。不结合V2突破条件或V1高点退出。

## 数据、对照与验收

仍为原V9冻结3,531流全部候选，包括亏单、空流、删失；2024-09-10至2026-09-10，2025-09-10切分，跨界单列。阈值先由已认证候选表生成、哈希绑定并提交，再读原cache完整串行回放。保留全部阈值历史窗、样本数、原始候选引用与原因码。

主要验收仍为后段：普通净胜率及最终净R>10比例均提高，月块bootstrap差值95%下界均>0，平均净bp>0，匹配随机超额>0且月块符号置换p<0.01。保留数量/召回率、漏掉的原高R、假阳性、总笔数、收益与成本同表，不把低频少亏说成单位交易能力提升。前段早期阈值预热导致不交易需显式统计；后段不受初始预热缺失影响。

随机沿用原同stream/方向/月/ATR桶、seed91509、同退出，失败不重抽；保存控制实际桶/月/方向。连续分数=-reference_risk_fraction，AUC和前10%排序诊断仍仅用于解释，不能事后换参数。RV是单特征参照。

## 交付

独立Python V2.1源码、测试、计划先提交，校准产物再提交，之后全量回放；失败也记录。保存Markdown/CSV/receipt并更新Notion，不改Pine/TV、监控、ACTIVE、训练、仓位或账户。研究结果不是实盘保证，10R是结果标签，最多两次加仓偏好不变；不输出100U滚仓资金推测。
