# Six-MA density decomposition

Owner asks to test what actually recognizes MA density. Use the same29-asset frozen archive as the prior SMA study,15m/1h/4h, with unchanged boundaries. This is geometric detection research, not a profitability optimization or a new live strategy. Do not stack the HTF gate, train, modify Pine, replace defaults, produce HTML or send messages externally.

Freeze individual ablations before scanning: baseline prior12 mean(span/ATR)<=3 and pair flips>=2; change width alone to0.5/1/1.5/2; replace mean by max at3; change denominator alone to median ATR from before the core; replace flip count alone by distinct pair count or cross-period-group connectivity; add prior12 coverage10/12, prior256 relative-price P20, or20% contraction separately. Compare recent-density memory3/6/12 independently, without changing core gates. None of the variants is labeled optimal or accepted from its retention rate. A combined candidate is only a proposed next hypothesis, not evaluated as though single-variable.

Report shared eligible bar coverage, density bar/episode counts, overlap, mechanism flags and current V9 candidate diagnostics by timeframe/time split. Full scan is not a random sample; adjacent bars/overlapping windows are not independent trials. Existing V9 raw fields are preserved and independently checked outside gaps. New comparisons require340 valid post-gap bars and full quantile/reference windows; log excluded coverage. Existing OHLC aggregation partial buckets are counted, not silently repaired.

Synthetic adversarial geometry tests expose same-pair churn, separated groups, ATR denominator inflation and tight parallel lines; these are mathematical input examples, not market labels or physically simulated OHLC. Null: fixed-seed per-bar line-identity permutations preserve span but destroy cross identity, separating proximity from crossings. No AUC, net returns, cost tuning or random entry test is appropriate to this non-directional study.

Luna Max audits earlier owner-approved and owner-rejected geometry examples. Only directly attributable individual owner judgments qualify as anchor examples; old auto-generated10000/8000 sets or post-start outcome labels are not truth. Preserve attachment/sample identity and core boundaries. If anchors are small or use different semantics, explicitly decline overall precision/recall claims. No new user-upload is necessary while local data exist.

Visuals: static scientific candlestick+six-line figures in PNG, source-backed, same colors and80-bar past-only context,12-bar core highlighted and actualUTC date/time at right. Preserve full-price y-axis extent needed to see long-line outliers; use percent-from-last-close if comparing assets. Distinguish SMA solid/EMA dashed and20/60/120 color groups. Mechanism examples selected deterministically without future prices/results; disclose purposeful selection. Export a small gallery and inspect actual files before delivery. No HTML.

Commit builders/config/tests before market outputs. Record source/code hashes, completeness, null seed, tests, report, learnings and Notion experiment as unverified/inconclusive. Updating derived report text does not authorize altering old versions or production.
