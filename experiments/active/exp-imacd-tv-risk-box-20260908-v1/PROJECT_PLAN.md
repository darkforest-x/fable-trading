# TradingView 启动 K 线盈亏比框

Owner 要求出信号的 K 线像之前一样加多头／空头盈亏比框，并明确位置为 TV。范围仅当前保存的 IMACD Pine 指标；TG、Bark、Mac 服务不修改、不重启、不重放通知。

沿用历史红绿紧凑框画法；当前 IMACD V2.3 没有既定止损或目标价。已向 Owner 明示本次显示默认假设：信号收盘参考入场，信号低点（多）／高点（空）参考止损，3R 参考目标，右延 12 根。均作为可调图形参考，不能记作 Owner 单独确认过这套交易参数。提供下一根开盘参考选项，但图形直到该根出现才建立，不回画未来开盘价。它不改变信号、出场规则、成本或任何实盘仓位。

只绑定 showFocus 且已确认 focusRelease；普通启动、回踩与退出不生成框。默认最多 8 组、上限 20 组，各组对象先删除再创建。多空镜像、零风险、方向错误和非正目标价严格处理；所有新对象 force_overlay=true。副图、零轴、六均线、警报及信号核心原样保留。

源码先提交，再在 TV 编译更新并核对：设置快照不漂移；可见信号对应框；多空颜色与价格正确；保存脚本和综合过滤布局。单独保留 V2.3 原文件与哈希。工程对照采用剥离显示增量后原可执行代码一致、算术边界和实际 UI 编译，不做收益评估。沿用全日期授权，本配置第 1 次 live-observation 使用。
