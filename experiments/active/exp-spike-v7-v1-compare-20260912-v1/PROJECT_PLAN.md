# V7 BB-background admission versus archived V1

Owner authorizes V7 logic and a like-for-like V1 backtest, not production monitor changes.

Freeze B admission before new outcomes: BB200/2 population SD, prior500 bandwidth P10, full run3 in the preceding12 bars; no current expansion, RSI or future waiting. Require712 contiguous bars. Raw opposite V6 events still close holdings; admission filters new entries only. Plot visibility independent from admission toggle.

The B choice follows a previously inspected 8-market study, so overlapping data is reused research, not blind validation. No retuning after new outcomes.

First map archived V1 source receipts and time/venue coverage. The historical long-only native ledger is separate from a common-execution long-only comparison; bidirectional V6/V7 is a separate table. Count cold/gapped/missing streams and never call a current-catalog incomplete pool all-ever-listed coverage. Dates and streams must be frozen before running.

Use unchanged costs and risk. Report candidate/accepted/censored counts, win rate, PF, netR versus MFE, realized>=10R retention, annual/venue/period splits, and drawdown only under the model actually calculated. Do not sum event returns and call them account equity. Include matching random controls where performance conclusions are drawn; no directional economic success claims from descriptive coverage alone.

Commit builder before outcome generation; record code/data hashes and commands. Deliver ledger, reproducible report MD+HTML, and honest limitations. No monitor/Bark/TG/orders or other-task changes.

## Frozen replay scope and common execution

Default selected after the owner was offered the two archived studies: the original two-year Binance/OKX/Gate covered pool, 30m/1H/4H, 3534 evaluated of5043 current-catalog cells. Evaluation confirmations in [2024-09-10,2026-09-10), with authenticated prehistory for indicators only and no pre-window positions. All five global source receipts and per-cell input hashes are checked before scoring.

The native archived V1 long ledger stays unchanged. The V1 common long arm borrows raw V6 shorts solely as reversal exits so entry comparisons have identical protection, cost and reverse-exit rules. It must not be represented as the native V1 strategy. Common-ready V1/V6 baselines separately remove V7 history-warmup differences.

Matched controls: seed0; same venue/symbol/timeframe/month/causal-prior120 volatility quartile/direction. To bound Mac runtime, before viewing new outcomes freeze up to16 realized entries per stream/variant/side selected by SHA256 entry identity; all realized trades remain in performance statistics. Same-entry outcomes are verified before sharing control paths. BB-ready arms apply the same readiness restriction to control candidates. Unresolved/missing matches are reported, no replacement until a favorable match appears. These controls test paired event return, not account equity; this previously inspected period is not blind holdout.

Same-bar V1 long/V6 short conflicts: exit priority suppresses the common-arm long admission and records conflicts explicitly; native V1 ledger is unchanged. Random candidate controls likewise exclude an opposite raw confirmation on their entry-decision bar.
