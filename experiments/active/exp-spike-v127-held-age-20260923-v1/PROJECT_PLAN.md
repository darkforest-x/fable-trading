# V12.7 候选：先突破后V9的持线证据加时限

Owner授权：2026-09-23 回复“先回测第1条时限，再做V12.7”。上一轮只读诊断（V12.6 run_v2 账本）显示：
“先突破、后V9”占联合72%，突破确认到V9中位34根15m（75%分位92根），入场距越线收盘中位追高0.74R；
这是“突破+spike 有延迟”的主要来源。唯一键 `spike-v127-held-age-20260923-v1`。

## 唯一变量

V12.6 的持线证据（held break）寿命：600 根本图K → **8 根本图K（15m 即 2 小时）**。

- 年龄按 `pair_events` 原有时钟：本周期突破从确认根 `break_i` 起算，上级突破从首次可见的本图根 `visible_i` 起算；
  V9 当根要求 1 ≤ 年龄 ≤ 8（原规则 1 ≤ 年龄 ≤ 600），其余“每根收盘严格在冻结线上方、断档清空、24 条上限、每框一次、HTF 同根优先”不变。
- 证据过期只是不再可用，不消耗V9框；框仍开着时后来的新突破照旧按“先框后突破”配对。这是原状态机的自然结果，不另加补单规则。
- 8 根是 2026-09-22 向 Owner 口头提出的值，运行前固定，不做网格或邻域搜索。与 V10.4 窗口模式的 6 根不同，本轮不比较两者。
- 不改：V9 准入（BB200、3ATR、H1 SMA60、USDC/量比/周日）、线几何（主三点+局部C、raw/soft）、先框后突破分支、
  初始止损（5根极值+0.2ATR、至少2ATR）、收盘2R启动4ATR追踪、原始反向次开退出、20bp往返成本。无BTC门、无RSI7、无模型。

## 输入与一致性

- 沿用 V12.6 run_v2 冻结的 638 币 Binance 永续 5m 归档与元数据，逐文件 SHA 必须等于父 identity；不补采、不挑币。
  2024-09-10 至 2026-05-01 前，2025-09-10 切前后。历史已被多轮研究看过，后段不是盲测。
- **基线臂必须逐笔复现 run_v2 的 baseline**（trades 与 controls 同 trade_key 数值一致），否则结果作废先查因。
- 两臂共用同一次线计算与V9事实，只在配对时传不同寿命；各自独立串行占仓（拒绝释放占仓可产生新成交，全部入账）。
- 原生 TradingView pivot 平局语义仍未对账；本轮是 Python 研究回放，不称原生 parity。

## 输出、指标与零假设

每币保存 decisions/trades/statuses/controls/reference_boxes/breaks（两臂），decisions 增加越线起点
（突破确认前连续收在线上方的第一根，本周期或上级时钟）及其收盘时刻/价格，用于延迟度量。先提交 builder、测试、计划与配置再跑行情。

- 主指标：扣费胜率（净R>0）、每笔净R与净bp、总净R、PF、平均赢亏、退出时点事件R回撤、>5R/>10R 笔数与比率、event_key 保留/丢失/新增。
- 延迟指标（描述性）：越线起点收盘到联合信号收盘的15m根数中位/分位；入场价相对越线收盘的追高（R 与 ATR）。
- 匹配随机：同币×同UTC月×同前后段×同因果波动桶随机多头，原退出/成本，两臂同一随机池（本变量不改变入场许可，不给随机加门）。
- 月块 paired bootstrap 2000 次（保留零交易月）；后段周块符号翻转 10000 次，四项家族（胜率差、每笔净R差、处理臂相对随机净R/净bp超额）Holm 校正。
- 无模型或排序分数：AUC/top-decile/单特征基线不适用；零假设 = 不改寿命的同源基线 + 同币同月同波动随机入场。

## V12.7 默认值规则（运行前固定）

V12.7 一律加入“先突破后V9最长间隔（根）”输入。**默认开启 8 根**当且仅当：处理臂全期与后段每笔净R均不低于基线，
且全期 >5R 占已平仓比率不低于基线。否则 V12.7 保留该开关但默认关闭（=原600根行为），并如实报告。
无论哪种，都不是盈利验证，不改生产/ACTIVE/训练资格；后段p<0.01与正净收益仍是策略准入必要条件。
