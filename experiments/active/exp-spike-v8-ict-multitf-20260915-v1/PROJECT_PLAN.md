# 原V8 × ICT六周期：冻结研究计划

- Owner于2026-09-15问所有周期只在此前ICT时段交易。Owner随后明确去掉翻身指标退出；在任何本轮结果产生前收敛为仅原V8退出，只改开仓时段。
- 时段来自此前图表记录，见`exp-spike-eth3m-ict-sessions-20260914-v1/PROJECT_PLAN.md`：纽约London[02,05)、lunch[05,08)、NewYork[08,11)，合并[02,11)。当前夏令时图表曾核对；IANA纽约冬季同钟面仍是未在原生受保护脚本验证的假设。
- 只比较全天all与三段合并union；不新增择优小时、周末过滤或日期筛选。周末包括，按下一根实际开盘的纽约本地时钟判定。时段外信号丢弃、不延后排队；持仓可跨时段，初始止损、2R收盘激活的4ATR跟踪、原V6反向退出仍全时有效。没有每日债务清零。
- ETH-USDT-SWAP OKX，3、5、15、30、60、240分钟。入口、初始止损5bar/.2ATR/2ATR下限、退出公式、0.2%名义往返成本不变。多周期各自回放，不将六个账户相加为组合。
- 复用上轮哈希固定的全部入场机会结果，先剔除时段外机会，再重建单仓准入。不能直接从旧已成交清单删行，因为被剔除的旧仓不应再阻挡后续信号。
- 仅用2026-05-01前完整行情，源前缀加载和聚合沿用已冻结代码；旧结果清单及每个机会文件读取前验SHA，源若变更拒绝。5m原生OKX，不从3m重采样；30m/1h/4h使用完整15m UTC桶。
- 两窗口：共同2026-01-01至2026-05-01 UTC；较长可用历史按上轮各周期实际起点。两窗有重叠，不充作独立验证。Holdout使用0次；本轮不读2026-05-04及之后价格，也不使用既有9月交易调参。
- 每个周期原V8退出×2时段×2窗口，24组统计、48组1000U/1U基础价格风险/10倍容量固定与亏后翻倍账户。翻倍净回本后才重置；无六次止损上限伪造、无真实标记价格强平模型。
- 同时段匹配随机入场5次/自然交易：同币、周期、实际入场UTC月份、纽约小时、纽约周末状态、当前闭合信号ATR/close相对前120bar的三分位桶，同方向同退出同成本。仅用该窗口内允许开仓时点；不以交易未来盈亏选桶。随机结果是独立交易对照，不冒充可执行随机账户。删失重抽及缺失逐笔记录。
- 月块符号置换9999次；每个窗口12项Holm校正。描述性研究不选冠军、不promote。AUC/top-decile无事前预测排序而不适用，以全天同规则及匹配随机入场为零假设对照。
- 必报自然笔数、净胜率、PF、毛/净R、初始SL与净亏最长连续次数、实现毛/净3R、固定与翻倍现金、容量拒绝/停机/余债。核对全天12组与旧逐笔完全一致；union原V8另与原引擎逐笔parity；强制所有union计划入场属于[02,11)。
- 新代码/配置/计划先提交，再评分。测试DST边界、按入场而非信号bar开盘筛选、筛选先于单仓准入、随机控制无跨窗及空样本。报告MD立即转HTML，附可读逐笔CSV与完整结果哈希。
- 原生TV逐笔对齐未完成，不将Python回放称为TV原生策略测试器结果。本轮环境若仍缺Xcode目标，使用现有CLT Python3.9.6和原.venv包路径，不安装/降级依赖。
