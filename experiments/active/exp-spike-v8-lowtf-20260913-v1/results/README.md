# Result runs

- `run_20260913_v4` is the canonical report run. It reuses the hash-pinned
  primary files from `run_20260913_v3` and reruns only the pre-holdout ranking
  cohort after the midnight-latency correction.
- `run_20260913_v3` is the pinned primary source (formal holdout exposure #3).
- `run_20260913_v2` completed before review but allowed a 23:55 signal to fill
  at the same 00:00 instant when the prior-day rank became available; do not cite it.
- `run_20260913_v1` stopped after primary while building post-hoc coverage; do not cite it.
