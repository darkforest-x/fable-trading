# Result runs

- `run_20260913_v2` is the completed canonical run used by the report.
- `run_20260913_v1` is an interrupted preflight run: it completed the primary
  streams but failed while building the post-hoc coverage table, before writing
  leaderboard outputs.  It is retained for provenance and must not be cited.
