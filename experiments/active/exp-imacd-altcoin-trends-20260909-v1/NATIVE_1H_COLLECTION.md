# 原生1H OI/taker采集：只读验收回执

验收时间：2026-09-08 18:17:17 UTC（北京时间09-09 02:17）；采集2026-09-08 17:48:59～18:04:09 UTC。

**近期数据覆盖通过；原采集状态仍保留2个分页停止错误，不得改称零错误。** 本验收未重拉、覆盖或修订数据，没有信号收益评分。

## 数据范围与覆盖

源目录：`data/altcoin_trends_20260909_v1/native1h_snapshot`；日志：`/tmp/spike-altcoin-native1h-fetch.log`。

- 56个固定合约 × OI/taker = **112流**，112个规范CSV、1792个原始成功响应页，共161,036行。
- 请求区间为`[2026-07-10 12:00, 2026-09-08 16:00)`UTC，理论每流1444个1H桶；所有实际末桶均为09-08 15:00，名义可用时点为16:00。`ts>=end`和不完整桶没有进入CSV。
- 92流各1438行，从07-10 18:00开始，左缺6桶；20流各1437行，从07-10 19:00开始，左缺7桶。**全部流内部连续、无重复/缺桶**；请求全区间的`full_period_coverage`均为false。
- 固定近期`[07-12 00:00, 09-08 16:00)`的112流均恰有**1408桶**，合计157,696行；没有补零或插值。

每种行数唯一对应上述首时点，所有末时点相同。逐合约分布如下（简写均为`-USDT-SWAP`）：

| OI行数 | taker行数 | 合约 |
|---:|---:|---|
| 1438 | 1438 | AAVE、ADA、APE、APT、ARB、ATOM、AVAX、BCH、BNB、BTC、CFX、CHZ、CRV、DOGE、DOT、EIGEN、ENA、ETC、ETH、ETHFI、FIL、GALA、GRT、HBAR、HYPE、ICP、INJ、JTO、LINK、LTC、NEAR、OKB、ONDO、OP、ORDI、PENGU、PEPE、PI、POL、SAND、SHIB、SOL |
| 1437 | 1438 | SOPH、SUI、TIA、TRUMP、TRX、UNI、USELESS、WLD |
| 1437 | 1437 | VIRTUAL、XLM、XRP、ZEC、ZK、ZRO |

该左边界差异与采集跨整点时两端点各自更新的约1440条滚动保留期相符；端点缓存更新延迟没有单独测定。已冻结页保留各请求时实际可得的数据，不应强制补成共同更早起点。

## 原始页重放与边缘上下文

逐页核对了合约、endpoint、1H参数、taker的`unit=0`、HTTP 200/code=0以及请求参数SHA文件名。用纯`normalize_pages`重放全部1792页，与112份CSV**逐字段一致**；每份单流coverage也与总coverage对应项一致。检查首尾、排序、唯一性、整数小时网格、有限非负值、`available_at_nominal=ts+1H`及端点过滤通过。验收前后所有源文件SHA和文件集合保持一致。

若未来另行采用假定的`ts+2H`可用时点，07-12 00:00决策需当前OI桶07-11 22:00、4H参照07-11 18:00、24H参照07-10 22:00。这三个端点56币全部存在且OI币本位值为正；同一当前taker桶56币均存在且买卖合计大于零。**这是上下文可算性检查，不是时点特征已经上线或历史发布时间得到验证。**

## 两个停止错误的定位

`coverage.json`的`errors=2`，UNI与USELESS的taker均标记`Pagination made no backward progress`。两者最后一页请求`end=1783706400000`（07-10 18:00），都返回恰好一条该时点记录；前页最老记录为07-10 19:00。新页实际向过去推进了一小时，但采集器以`oldest>=cursor`拒绝了包含边界等值的新记录。

新记录已经被保留并参与规范化，因此两份CSV的近期区间和左侧24H计算上下文均完整。原始错误状态没有删除或改写；验收也不推定请求区间左侧缺失可以恢复。此停止规则的修复属于后续代码变更，不能反写本次回执为原始零错。

## SHA256快照

以下是本次首次登记的内容校验，不冒充不存在的旧文件哈希清单。数据一致性证据来自上述全量原始页重放。

| 对象 | SHA256 |
|---|---|
| manifest.json | `a27e5725f517d0505602becd32f19f507d7573d3031c9d6e05f650279d261b1c` |
| coverage.json | `4c84ff791af4b9d8060052e22a202829b3435bc404e3e57f21d921c555937084` |
| 112个normalized CSV清单 | `70b033d4304107e3be5f09cbf8307fa0d53e05e82a35fd64da3c1b09a3c4bcc3` |
| 源目录全部2018个文件清单 | `17c8c96e3a8df4d245ac3744214200afc9e03d2efd834c2da71f0a8a7bdbad1b` |

清单哈希定义：按相对源目录的路径字典序排列，每行UTF-8为`relative_path + TAB + SHA256(file_bytes) + NEWLINE`，对连接结果再算SHA256；全目录总大小49,583,071字节。本说明位于源目录外，不参与该哈希。

## 限制

原生1H首次发布时间、后续修订稳定性未验证；1H与旧4H的taker聚合不严格可加，见同目录`NATIVE_1H_FEASIBILITY.md`。没有评估本批数据是否减少假启动、改善净收益或适合生产，不应据此改变指标、通知、ACTIVE或执行配置。
