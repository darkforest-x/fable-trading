# IMACD → YOLO：固定规则扩大至54币、2026年上半年

Owner在1H／4H小样本后明确要求“多测试一点数据啊”。本轮扩大评估覆盖，沿用既有54币缓存池，起止2026-01-01至2026-07-01 UTC（右开），不按结果或当前涨幅挑币。币池扩展和历史扩展属于本次明确要求的覆盖扩展包；模型、阈值、检测与确认协议都不变。

## 冻结规则与授权

直接复用上轮`imacd_yolo_timeframes.py`的推理、渲染、候选联结、失效、最早确认及下一开盘价格观察。Grade-A close-source YOLO11s full40，SHA256 `862705b999594355c1133640acc540f4de19b561889e89d9e050ddad5c6db838`，原生训练15m；native-chart 1H／4H迁移仍属研究。

conf0.25、IoU0.70、imgsz1280、batch16、MPS float32、rect=True、max_det300、非跨类NMS；1280×742白底K线与close SMA/EMA20/60/120。W18/W19、core4/5、post2..9；IMACD34/9、focus12、band0.10。每个已收盘箭头p冻结原蓄势区，[focus_start,p]与检测核心至少有一根实际K线重合；同币、同周期、同方向，完整图窗右端p..p+9。md回零／反向／未知永久取消候选，同根取消优先，最早合法确认优先。同端点confidence降序、window_len升序、检测ID升序。1H最多等9小时，4H36小时。不重新优化等待长度。

Owner此前明确“任何时间段数据都可以使用不要有任何限制”，沿用授权。**这是相同1H配置第2次、相同4H配置第2次消耗holdout；本次扩大评估各1个pass**。旧BTC／ETH May4–Jul1共14候选属于重复接触，不能把全部数量称新增独立证据。新实验ID不把历史消耗次数重置为1。无训练、阈值搜索或模型切换。

## 数据与时间分段

universe.json逐字列明旧formation-memory V2 manifest中的54个source path／整文件SHA。运行先核对源文件字节，再读取原始15m自最早历史至Jul1之前；完整聚合UTC1H／4H，拒绝缺口、重复、非法OHLCV或未覆盖Jun30末根的源文件，禁止补齐或替换。记录每个来源前缀SHA、聚合首末、热身、有效评估根数和被丢弃的不完整边缘组。

两个提前固定的时间组：

- pre_holdout：Jan1–May4，模型验证期已接触，不能称盲样本；每个完整检测窗和p+10下一开盘均须在May4之前。
- holdout_review：May4–Jul1，该时期项目已有历史接触；同样只用Jul1之前完整随访。

分组按**箭头收盘可用时间**，不是开盘、模型确认或未来高低点。所有均线和IMACD从完整历史因果播种，不先截到Jan1。pre组输入用截止May4的prefix，推理不能见后组K线；保存trace也不跨分界。为保留与旧协议一致的离线随访口径，缺完整9根等待及下一开盘的整事件标censored，即使可能早已出现确认。它不是在线通知状态机。

54币是既有缓存池，并非当时全OKX币池。ZEC历史较短，其4H在Jan1初仍在热身；零箭头、数据不足、模型零框必须分开记录。模型训练可见时间至2025-11-29，val至2026-05-03；因此不把Jan–May分组写为训练外独立验证。本轮不延伸到2025来堆叠已训练过的行情。

## 预定输出与对照

每币×周期×时间组共216个shard，均落盘候选、原始模型proposal（包括结构未过）、决策、p..p+10 trace；无候选也保存空schema及receipt。每个shard结束后才写带SHA的完成回执；同source manifest／同原始prefix才可resume，已完成片不重推理，半写入片fail-closed。整轮只运行这一预定配置，不见到某个方向或月份少就自动扩大窗口。

主表1H／4H分时间组、月份、方向和全54币：原箭头、完整随访、确认、失效、过期、截断、确认率、0等待／延迟确认、等待小时、正向追价和反向改善。报告全部币，包括0信号，不用确认率冒充去噪率。原IMACD全部箭头是唯一信号基线；旧BTCETH14候选逐事件对齐报告，重复与新增分开。

四个时间组×周期的固定币／月份内方向置换，各10000次seed20260908，四项一起Holm校正；候选原方向形成的md有效时间池固定，只打乱方向关联。它仅检验条件方向关联，不是无条件去噪、趋势真实性或交易超额。聚合全半年结果仅描述，不另增主检验。

没有新的价格退出、TP／SL或持仓合同，也无人工真假启动金标；**AUC、真实胜率、top-decile净收益、最大回撤、经济匹配随机入场不适用**。不捏造。相同IMACD箭头基线与条件方向置换是本次工程过滤审计的对照。若需盈利裁决，必须另立有收益退出和匹配入场对照的实验，不能由“少发多少”推出“更赚钱”。

## 验证与交付

源码／计划／universe／manifest提交main后才读新增评估行情和推理。新增测试重点是分段边界、完整随访、旧候选等价、断点回执防混用；复用原模型门与周期测试。独立saved-ledger验证器不调用选入代码，核对每个事件真实trace内的取消、最早确认、价格、身份和汇总，以及4项置换/Holm；如有分歧先查再出结论。

报告`analysis/p1_imacd_yolo_expanded_20260908.md`完成后立即转换`analysis/html/p1_imacd_yolo_expanded_20260908.html`交付。图表基于全部保存台账，不挑未来赢家。记录源版本、完整复现命令、风险与诚实声明。training_eligible=false，production_eligible=false。实时TradingView、spike、Mac扫描、TG/Bark、ACTIVE／frozen／forward及实盘执行配置均不在本实验修改范围。
