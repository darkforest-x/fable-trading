# exp-spike-v10-trendline-gate-20260918-v1

## Owner 指令（原文）

1. 「把这个趋势线突破 加入我们的spike v9 写一个v10」（附 Pine v6 源码
   「主下降趋势线 · 关键高点 V1」）
2. 「然后对v10做回测 要求v9出了信号 同时 或者信号处于刚好趋势线突破 或者突破了一会的」

## 问题

V9 确认信号加上「本根或最近若干根内发生过趋势线突破」这道门之后，净 R / PF /
配对随机超额是变好还是变差？

## 单变量

只改入场掩码。出场、0.2% 往返成本、原始反向确认退出、随机对照抽样规则与种子
全部沿用已发布的 V9 全量回测。趋势线参数逐字采用 Owner 脚本的默认值，不调参。

## 事前登记（在读任何价格之前写下）

- 参评臂固定为 5 个：`v9`（对照）、`v10_now`（突破当根）、`v10_a12`、`v10_a48`、
  `v10_a200`（突破后 ≤12 / ≤48 / ≤200 根）。这是对 Owner「刚好突破 / 突破了一会」
  两种说法的三个量级读数，不是择优搜索；全部如实上表，不挑最好的那个宣布结论。
- 多头用下降压力线向上突破，空头用上升支撑线向下突破（镜像），与 V9 自身的
  多空镜像结构一致。
- **holdout 消耗 0**：每条流在 2026-05-04T00:00Z 之前截断，开盘时间 ≥ 该时刻的
  K 线一根都不读入。2026-05-04 → 2026-09-10 这 4.2 个月留作未来最终验收的盲样本。
  跨界需要 `docs/HOLDOUT_LEDGER.md` 新编号条目 + Owner 对话批准，runner 默认拒绝。
- 预期：不知道。趋势线突破是形态过滤，先验上既可能提高胜率也可能只是减少样本。
  V9 后一年为负的病因已归因为 30m 毛收益贴近零 + 固定成本，本门未针对该病因设计。

## 完整性校验

截断不得改变截断点之前的任何决策。每条流把 `v9` 臂与已发布的 V9 账本逐笔对照：
所有在截断点之前平仓的交易必须完全一致（signal_i / entry_i / side / exit_i /
exit_reason / 价格 / net_r）。不一致即 raise，不出结果。

## 纪律

不训练、不 promote、不改仓、不动真金、不接实盘。本实验的任何正结果都不构成
实盘资格；`production_eligible` 与 `training_eligible` 保持 false。
