# IMACD Mac 全市场监控交付

Owner 2026-09-08 明确要求：这台 Mac 监控 OKX 合约全部币种，1h/4h 信号通知 Telegram，提供简洁美观前后端，开启目标模式并自主完成。既有全日期使用授权继续有效。本轮是软件交付与实时指标观察，无收益优化、模型训练、ACTIVE 修改或下单。

## 固定范围

- 所有 OKX `instType=SWAP,state=live` 永续合约，不按涨幅/成交量筛币；包括 USDT 和币本位。
- 当前 V2.2 原默认密集启动规则；近零12根释放、影线回踩单独记录。HTF 1h→4h，4h→1Dutc，仅使用本周期 OPEN 已可知的已收盘高周期，标注许可，不悄悄改变默认入场过滤。
- 仅公共 GET 行情。confirmed=1 且收盘时间不晚于交易所时间；不制造缺失 K 线。
- 数据只在 RAM；与 VPS 的 K 线缓存/forward_log 完全独立。SQLite 仅保存指标事件、状态、扫描元数据、TG outbox。
- 扫描完成后120秒再次检查；全局公共请求上限8次/秒，8工作线程。首次每周期最多720根预热，运行期间保持历史起点。重启历史预热的边缘值/状态与无限 Pine 历史可能不同，不宣称无条件逐点一致。
- 新鲜窗口30分钟：扫描入队、TG发送、前端新鲜标记共用常量。TG通知 release/entry/exit；回踩作为前端观察。历史回填7天仅显示，不刷屏。
- Mac 本地127.0.0.1:8766；LaunchAgent负责常驻、自恢复；caffeinate防止接电空闲睡眠，不承诺关机/合盖/断网仍工作。

## 交付与验证

1. 源码与测试先提交；实际扫描后记录真实交易对数、收盘时间、耗时和错误。
2. 测试 Pine 关键公式/因果、输入质量、重复扫描/重启去重、失败与过期通知。
3. 使用已有 TG 配置；真实通知链路验证通过回执，不输出 token/chat ID；测试消息明确标识服务启动测试，不伪装交易信号。
4. 实际前端检查桌面/手机、筛选/选择/图表、空/错状态，保存截图。
5. 最终交付运行页面、报告、运行和停止命令、已知限制。

## 延迟预算

1h/4h 收盘→检查最坏120秒（从上一轮结束计）＋正常增量全市场请求预算180秒＋网络重试余量120秒，约7分钟；目标小于共享新鲜窗口30分钟。首次474以内合约×3周期×3页 /8rps约9分钟，另加计算/网络。实际耗时必须报告；超30分钟历史事件仅留前端。

经济AUC/收益/随机入场对照不适用：本轮不评估收益。等强工程对照为合成行情未来扰动、HTF边界、重复并发入队和故障注入，断言已确认历史不可改变、不可重复推送、不可用未收盘报价发信号。
