# V32 validation and delivery

## Overall assessment: Share with caveats

Economic continuation is rejected (not_supported); report and saved arithmetic are reproducible.
This is exploratory reused2023–2024 fixed-clock diagnosis, not executable profit.

## Executed checks

- Source-first core/config/plan/tests/audit commit4cbcc93;99 synthetic tests passed71.19s before real run.
- V31 metadata/schema smoke passed before any subgroup labels; historical metadata left unchanged.
- Real economic runner ran once under system Python NumPy2.0.2/pandas2.3.3 and imported the pinned assumption utility. Config/input/source hashes verified before and after.
- Root independent saved-number audit passed3980 endpoint labels,1004mother×H rows,all horizon group descriptions,4primary inferences and Holm. It also recomputed gate timestamps/states.
- Core-author second check independently aggregated final halves and decision and replayed all policy cells;REVIEW.md states reviewer identity and scope.
- Report builder3ea0bb2 committed before actual SQLite queries.2 report tests passed0.27s.
- Project .venv boundaries + V31support/research/audit + V32report:245 passed24.66s,13existing pyparsing deprecation warnings. No contract dependency upgrades.
- Narrative/support docs bc93cfa committed before artifact packaging.14 markdown sections,15 blocks,1native chart48rows,24months×2series.
- validate_artifact passed. Canonical portable delivery:validation passed/package passed/verification structural_only.
- No installed compatible Chromium; browser/mobile/light-dark/source-dialog/source-interaction NOT verified. No browser installed. Semantic chart tables and exact canonical payload remain in self-contained HTML.

## Notebook companion

verification.ipynb contains4 executed code cells; outputs are actual saved-number results.
No nbformat/nbclient/ipykernel/nbconvert was installed in either system or bundled Python.
No dependencies installed. A sequential standard-library replay executed all pure-Python cells in one
shared namespace and checked nbformat4.5/cell keys/unique ids, with stdout captured into outputs.
This is not a Jupyter kernel/UI compatibility certification. Exact read-only replay:

```bash
python3 - <<'PY'
import json
from pathlib import Path
path=Path('experiments/active/exp-btcusdtp-1h-volume-wave-economics-preholdout-20260907-v32/verification.ipynb')
book=json.loads(path.read_text())
assert book['nbformat']==4 and book['nbformat_minor']==5
assert len({c['id'] for c in book['cells']})==len(book['cells'])
namespace={'__name__':'__notebook__'}
for cell in book['cells']:
    if cell['cell_type']=='code':
        exec(compile(''.join(cell['source']),str(path),'exec'),namespace)
PY
```

For native Jupyter validation later, use an already compatible Jupyter environment with the repository
numeric contract and run nbconvert --execute --to notebook --output verification-jupyter.ipynb
against verification.ipynb. Native-kernel verification is optional presentation QA, not missing economic arithmetic.

## Calculation checks and caveats

- Same248 opportunities:11.4697973584712bp improvement =11.9354838709677bp costs −0.4656865124965bp gross.
- All100 accepted compare ALL300 original controls,not158 filtered controls.
- All251 mothers retained;3unknown remain missing policy,not zero.55wrong-direction+7cost-erased+38above-cost=100.
- Four accepted half means negative;10/24positive monthly means;no filtering based on these descriptions.
- No raw-market authenticity, intrabar fills, funding, sizing, overlap, dynamic exits, Pine runtime or forward profitability validated.
- No2025+/holdout/new prices/TradingView/production/training/ACTIVE changes. This configuration holdout use0.
- Statistical-analysis guided assumption/effect/CI reporting; experimental-design froze contrasts before labels;
  report/visualization/validation skills separated source-backed evidence from executable-profit claims.
