# V12.6 联合入场重新检查本币 H1 SMA60

Owner授权：2026-09-22对上一轮建议回复“ok 按照你的建议去做”。主目标为扣费盈利胜率提高，同时保留大盈利机会。唯一键 `spike-v126-htf-recheck-20260922-v1`。

## 固定范围

首轮只测建议中的15m：重建V12.6默认线性双轨、主三点＋局部C合并池、框内与先突破后V9两种配对。V9首次确认本已要求完成H1 SMA60方向；新臂在每框首次联合出现时再次检查当前15m close严格高于其开盘时已知的完成H1 SMA60。相等、未知、缺口或陈旧拒绝。拒绝消耗该候选，不等待下一突破补单。不得将基于rawSignalSide的旧门直接用作联合门。

输入沿用现成638币Binance永续5m归档与元数据，不补采、不按结果选币。2024-09-10至2026-05-01前，2025-09-10切前后；每币同源聚合15m/H1，仅完整有效桶，缺桶形成时间缺口。历史已暴露，不是盲测。旧文件名里的preholdout不代表当前禁读规则；以后历史全可研究，本轮截止只为了冻结已有范围。5m的EMA120条件未纳入本轮收益结论，30m/1h/4h亦不外推。

## 一致性先行

- 新port包括两族共享几何去重、各kind/source/span独立两槽、先监测后建线、HTF只取同根最佳突破、chart/HTF各自24条held、两种时钟、每框一次、严格持续在线上。
- 默认V9风险参考按收盘起算，只生成配对状态；经济账本从联合后次开独立起算。两者分账并记录来源/几何/首次可知/联合/参考/可成交时间。历史回放没有实际发现时间，不捏造通知及时性。
- 复用旧固定price退出：原五根极值＋0.2ATR、至少2ATR，收盘2R启动4ATR追踪，原始未过滤反向触发次开退出，20bp成本。两臂相同；不加入RSI7、均线止损、BTC门、止盈或模型。
- Pine volume array和ready在缺口时重置，递归MA/ATR仍按chart观察值延续；新研究helpers实现此语义，旧实验实现不改。
- 共享实际成交的入场、退出、成本、风险R逐笔相同；门控后重算串行占仓；不直接删除旧成交表。
- 目前原生TV pivot平局语义尚无全量导出验证，研究暂沿用显式strict约定并记录tie数量。不得把单元测试说成原生Pine parity；未完成原生对账不升级默认或交付V12.7。

## 输出与零假设

两臂每币保存decisions/trades/statuses/controls/reference_boxes/breaks，含未成交原因和删失。输入/代码/配置/输出SHA闭环，先提交builder再跑。完整实验注册，资格false。

主指标：普通胜率定义最终扣费净R>0；净R/bp均值与总数、PF、平均赢亏、退出时点结算回撤、净>5R/>10R笔数和event_key原赢家保留/误删/新增。R求和不是账户净值。按full/earlier/later和月份披露，跨切点持仓不进入earlier成熟标签，未闭合不作输赢。

每笔配同币×同UTC月×同前后段×同因果ATR/price波动桶的随机多头，原障碍/成本/退出不变；新臂随机入场也过同H1门。随机不重抽规避亏损或删失。基线随机无需在当前时点重检H1，否则偷加处理条件。

月块paired bootstrap 2000次，保留零交易月份；周块符号置换10000次。主后段检验家族四项：胜率质量改善、每笔净R改善、相对随机净R超额、相对随机净bp超额，Holm校正。无参数选择，无随机切分；p<0.01与正净收益是必要条件。尾部保留全部披露，未由Owner指定可牺牲比例，不事后挑一个比例宣布通过。原生一致性未完成则最终状态至少inconclusive，不能仅凭正点估计上线。

无训练或概率排序，AUC/top-decile/分类单特征基线不适用；同币随机和不重检臂为等价严格零假设。完整负面结果保留。滑点/资金费/组合资金约束未新增，沿用旧20bp限制。

## 构建核对记录（不改变实验变量）

首版9102b22819仅ETHUSDT smoke_v1完成，复核发现回放误用了BB20；实际Pine默认BB200。该试跑失效，只留作失败证据，不纳入收益结论。修正为200/700/712并加非空连续常量序列和旧BB200诊断逐bar对照后，完整研究从run_v2重新开始。线对象joined未回传几何池是内部trace差异；可用期本已受usable保护，失效后spike同步清空，独立复核未找到它改变候选的路径，仍不宣称完整原生parity。
