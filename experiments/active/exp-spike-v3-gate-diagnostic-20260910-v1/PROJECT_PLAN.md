# V3噪音：条件放宽与单项恢复诊断

2026-09-10 Owner纠正以最新V3为对象，要求解释放开了什么、为什么信号多、有哪些降噪方法。本轮只诊断V3，不再用V2去重代替。沿用明确的所有日期与优化授权；本轮各固定过滤配置首次消耗holdout，边界仍2026-05-04，已见历史不能称独立样本外。

## 先冻结问题与方法

Owner随后明确“忽略v2，应该和原版本对比”：唯一版本比较基准为最初SPIKE强劲爆发V1，不采用V2规则或其去重结果。本修订在任何新评分前完成。V3现有10386个预警、3809个确认，同一278个OKX1H/61日池。复用原V3已认证信号、实际交易结果及同币×UTC周×因果波动桶随机控制；不重新生成候选、父子时序、冷却、风险、止损和费用。对既有事件做可部署为显示层的单项布尔过滤，不能声称重新定义了完整检测器。恢复一条门槛后不会释放旧冷却，也不会在确认窗口中寻找后来的替代确认。

- early独立恢复7项原版组成条件：近期密集、六线全上、单根量比>=4、TR/前根ATR>=3、正向实体/全振幅>=0.55（同时要求阳线）、收盘位置>=0.75、MD>=0。
- confirmed独立恢复其中6项（近期密集本来已要求，不重复测试）。MD>=0只恢复原版两条路线共有的最低方向约束，原版释放确认的MD>SB/MD递增未被这单项完整代替。
- 近零12根与整段蓄势箱体是原版状态机，不能用同根单项tag伪装完整恢复，本轮列为需要单独重建的结构门。现有recent_density仅原密集定义，不能说它包含近零资格。
- 13条只各自对各自原阶段加一个条件，不叠加，不网格，不按看到结果调整阈值；保留失败。两阶段基线是对照；13个全期新过滤的资产块置换p统一Holm修正。
- 逐项缺失比例会重叠，不能相加为“各条件解释新增信号的贡献”；这里是单项过滤诊断，不是各改动因果效应分解。V1/V3不同状态机也不能把总差额逐门硬摊。

## 输出与验收

原1660正例分母保持，+1/+6命中按保留信号重新匹配；标签没有密集要求，不伪称真正密集金标。所有/前31日/后30日/手选当夜分开；报告每日期待负担、原锚点召回、24根+4ATR先于-2ATR比例、自然平仓净胜率、峰值>=5R/10R捕获率、事件净收益与原匹配随机超额。次根开盘、20bp往返成本与原结果逐项保持，不是账户净值。

单特征量比/TR AUC和top-decile作为描述，训练val不适用；不根据历史最优单项宣称找到赚钱参数。标签与交易结果先前已评估过，复用减少重复计算，不恢复盲性。

## 交付范围

先提交builder再生成新汇总。报告源Markdown并按仓库命令转换HTML（owner指定格式优先通用插件报告包装）；精确代码条件表、单项统计、风险与诚实声明、复现命令。对本轮新增结果独立计算复核。初始报告不改TV/监控/通知/订单；若额外修复显示，明确只改变预警的视觉和参考归属，不能当盈利优化。

报告面向owner：中文结果摘要→V3条件差异→量化证据与分期→优先降噪方向→未解决问题→方法/诚实声明。表格用于13项精确查找，无需额外趋势图；诊断没有新时间序列。保留源文件与哈希作为证据。
