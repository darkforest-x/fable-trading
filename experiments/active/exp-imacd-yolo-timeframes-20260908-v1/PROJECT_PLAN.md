# IMACD → YOLO：同一冻结规则迁移至1H／4H

Owner本轮明确要求“1h 4h也试试”。这授权了本次跨周期研究，不代表重新训练、promote或替换实时通知。本轮只改变K线周期；沿用上一轮BTC／ETH、固定日期、权重、按根数定义的形态、失效与等待规则。不读取本轮结果后选择等待上限或阈值。

## 固定比较

上一轮15m台账保持原样，只读取已保存summary作参考，不重复模型推理。新配置分别为native-chart 1H、native-chart 4H：把同一冻结的15m训练模型用于1H／4H图像，**不是**1H箭头后再跑15m模型的跨周期确认方案。模型原生训练周期仍为15m，本轮不冒称已有高周期训练或经济验证。

固定Grade-A close-source YOLO11s full40 native1280，SHA256 `862705b999594355c1133640acc540f4de19b561889e89d9e050ddad5c6db838`；conf0.25、NMS IoU0.70、imgsz1280、batch16、MPS float32、rect=True、max_det300、非跨类NMS。1280×742、同源白底K线与close SMA/EMA20/60/120，不把TV界面截图直接输入模型；不增加旧语义门。

每个本周期IMACD focusRelease（34/9、focus12、band0.10）收盘p建立候选，冻结原蓄势段[focus_start,p-1]。检测窗口W18/W19，完整可见端点p..p+9（共10个检测机会，最多等9根）。1H等待预算9小时；4H等待预算36小时。core4/5和post2..9也均为当前K线周期的根数。保持根数意味着自然时间和波动尺度改变，这是本轮迁移本身，不声称135分钟或9根最优。

模型核心与原蓄势段＋箭头[focus_start,p]至少共用一根实际K线、同币同方向才允许确认。md在p到确认时只要回零、反向或未知，候选永久失效；同根失效优先。最早确认端点优先，同端点confidence降序、window_len升序、检测ID升序，不事后挑更好价格。每个候选仅确认一次。

原箭头时间、模型实际可用时间和两次下一根开盘价分别记录；正的side×(后入场价/原入场价−1)表示追价。无收益退出、无固定3R、无新增止损或仓位合同。

## 数据与时钟

固定OKX BTC／ETH USDT永续，原始来源与上一轮完全一致，候选收盘时间2026-05-04至2026-07-01 UTC（右开）。从各原始CSV最早行读取到2026-07-01前，不解析之后OHLCV；bounded原始OHLCV摘要必须等于上一轮。

先检查15m输入连续、UTC对齐，再用完整4根／16根聚合1H／4H，丢弃首尾不完整组并记录数量。全部均线与IMACD在聚合后的完整历史重新播种，然后筛选评估期；不聚合15m指标、不先截断到5月。模型输入和候选ID、联结、置换组都带周期，禁止同币跨周期串框；所有available_at=当前周期bar开盘＋当前周期长度。

末尾不够完整9根等待和下一根开盘的候选保持censored规则，分别报告分母。它是离线队列筛选，不直接当在线状态机；不能将缺未来随访误称不出信号。

Owner此前明确“任何时间段数据都可以使用不要有任何限制”，本次继承授权。**1H配置第1次、4H配置第1次消耗holdout**；15m本轮不再消耗。该时期在项目已有历史接触，不称全新盲样本。只做固定配置评价，不以本次数据训练/调参。

## 审计与交付

新增合成测试覆盖1H／4H实际时钟、9根边界、整组聚合、候选收盘截止、跨周期拒绝、未来像素扰动、原15m决策等价与假模型批次接口。源码先提交main，再运行市场推理。

新增仅供审计的事件trace：p..p+9的md与时间、直到p+10的下一开盘记录，保存first_invalid_i；不改变入选规则。独立脚本只读保存台账，可完整核对等待期间失效、最早同向确认、方向池、后续开盘位移与各周期分母；不重复模型推理。

主表列15m旧结果与1H／4H新结果，各币原箭头、完整随访、确认、过期、失效、截断、保留率、等待与追价。沿用固定符号/月内方向置换10000次seed20260908，在每个周期独立执行，对两个新周期p值做Holm；只诊断条件化方向关联，不是盈利、假信号率或经济超额检验。

本轮无人工真假形态金标，无收益退出合同：AUC、胜率、最大回撤、top-decile与经济匹配随机入场不适用；不捏造。指标单一基线是全部原IMACD箭头，新增变量只有对应周期模型确认；对应工程零假设是上述方向置换。

代码：yoyo/evaluation/imacd_yolo_timeframes*.py；产物：本实验results＋data/imacd_yolo_timeframes_20260908_v1。报告analysis/p1_imacd_yolo_timeframes_20260908.md完成后立即转换HTML。每个新周期仅按时间选择最早2个确认展示，无未来行情挑赢家；模型输入复现需像素SHA一致。

training_eligible=false，production_eligible=false。不改TradingView、spike、Mac扫描、TG/Bark、ACTIVE/frozen/forward，不下单。不因小样本或零确认自动延长数据期、扩大币池、更换权重或改变规则。
