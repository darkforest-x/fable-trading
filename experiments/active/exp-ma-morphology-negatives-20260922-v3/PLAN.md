# 形态负例纠正与离线 A/B 重训

Owner 2026-09-22“去做吧 开始目标模式”授权继续：保留原1506个3R精选训练正事件，普通背景必须有可见形态依据；收益非赢家不作为空标签。验证与测试一起纠正。旧v4、v5失败结果与已产权重保留，不续训v5。

## 冻结数据口径

- 正例保留v4的原PNG/TXT和事件时间切分：train1506、val175、test160。它们是规则候选再经3R筛选，并非逐样本Owner金标。类别改为dense_launch_long/short；3R是正例选择属性，不是负例依据。
- 每正事件软目标匹配1个背景：同源文件、币种、周期、半年和split，前后7天寻找；不够则记录缺样。保护全部已知规则候选（含SL/TIMEOUT/UNKNOWN）、69参考与221人工视窗±4小时；禁止负例互相重叠和跨源复用。
- 先使用明确非密集背景：沿用历史easy六MA宽度1.6ATR界限，强化为最宽可见窗每根K线的CLOSE和HL2六MA宽度均不低于全可见窗最大ATR的1.6倍。core+5以后的OHLC和收益不参与。这个条件是保守筛选证明，不冒充人工金标。
- 相似密集但未启动、不同裁剪协议的旧人工NO均放审核队列，不强行混入；不设难负例硬比例。旧收益非赢家的形态继续未判定，不批量反向改成正例。
- A前9/后5、B前7或11/后5；核心4或5。原1280×742、HL2六MA、1200预热、visible_range_v1，训练图无框提示。B每事件只两张，不增加后文。
- 按UTC2026-01-01/2026-05-01切分。负图最宽可见区间及保守12h安全区间均不跨切点；相同事件及相关保护区不散落两侧。

## 执行与验收

1. 先提交plan、屏蔽函数、builder与测试，再构建20事件先导；实际查看正负配对原图和框审核图。
2. 数值测试覆盖未来数据变化不影响判断、可见窗边缘密集段被拒、缺口/短预热被拒、所有收益类型都受形态保护。复核失败则修正代码并新建先导输出，旧输出保留。
3. 扩展匹配1841正事件的背景；逐文件SHA、尺寸、正例字节保真、真实label与loader、事件及时间split、背景证据审计。缺样如实交付，不降低标准。
4. RTX3060同基础YOLO11s、各40轮、原冻结超参和全部增强关闭。跨机环境及代码/数据SHA通过后才开训；A/B均从base开始，不续训被否决权重。
5. 检测验收与收益研究分开：同一修正val/test比对定位、召回、背景误报和mAP；该精选正例/明确背景集合不能代表所有形态召回。旧全候选池只作独立收益评分，非赢家不按“检测错报”统计；原20bp成本、匹配随机对照及失败记录保留。
6. 保留原人工与tip金标的协议差异，不声称生产准入。没有部署/promote/ACTIVE变更。

本轮修正标签语义及其关联的数据与评估口径，是Owner明确同意的整体返工；不伪称只改变负例数量的单变量实验。B曝光翻倍仍需单独披露。

## 运行与节省额度

轻量子代理为Luna Max映射、Terra High单模块实现。训练与评估用普通进程衔接；不反复唤醒模型打印未变化的训练状态。当前旧目标在应用仍paused，工具不支持恢复且拒绝创建第二个未完成目标，已请Owner操作目标卡；不因此阻止当前轮数据修复工作。
