# V38 report and chart plan

Mode HTML as explicitly required by owner. Audience product stakeholders,
executive-report specification. Title and Executive Summary; definitions then
findings with evidence and implications; next steps/open economic questions;
risks/assumptions; requested reproduction appendix. No KPI-strip dashboard.

One full-width grouped category bar compares mutually exclusive status shares
for case/control, with explicit counts and cohort denominators. Actual SQLite
query on main.audit_events groups by cohort/status and retains count, denominator,
share, immediate/delayed counts, average observed waiting minutes. BlueGold hard
two-root palette, visible cohort legend and exact adjacent table. Four halves,
source/key/unknown quality and boundary rules use exact tables instead of more
charts: they are audit lookups, not continuous trends or profits. Do not invent
nonzero categories or scatter just for a renderer. If fewer than4actual statuses,
use cohort/status grouped bars plus four-halfyear lookup; do not widen data.

Chart source metadata records executed SQL, actual table/CSV, dimensions and
metric definitions. Raw request IDs belong in the detailed CSV/notebook, not
executive headings. Major sections remain separate markdown blocks. Prepare
the final narrative, independent review and SQL source BEFORE one canonical
package. No alternate chart runtime. Structural_only, if returned by portable
builder, is disclosed; no browser install or invented mobile QA.

Companion notebook: saved-output audit only, no new raw fetch or strategy replay;
tl;dr/Context & Methods/Data/Results/Takeaways, source hashes, keys, grouped counts,
prefix proof. Existing environment has no nbformat/nbclient/ipykernel. Use a
small validated raw JSON scaffold and sequential code-cell execution with
captured outputs, explicitly not Jupyter-kernel certification. No installs.
