# exp-spike-v10-long-break7-20260918-v1

## Owner 指令（原文，2026-09-18）

「v10去掉 重新做
 用spike v9 和我这个趋势线指标 只有当我的趋势线指标出现 突破信号  然后v9也有信号才行
 支持先突破 然后最多7根k出现v9信号  我们v10才做多  不需要做空信号」

第一版 V10（`exp-spike-v10-trendline-gate-20260918-v1`，多空双向、突破年龄 0/12/48/200
四臂）按 Owner 口径撤回。它的报告、账本与代码按仓库纪律原样保留为历史，不再叫 V10。

## 规则（逐字落地，不调参）

- 突破 = Owner「主下降趋势线 · 关键高点 V1」指标的向上收盘突破，参数全部用 Owner 默认值。
- V9 多头确认出现在第 s 根，最近一次突破在第 b 根，**0 ≤ s − b ≤ 7** 才开多。
  突破当根（年龄 0）也算；V9 先出、突破后到的不算（那一刻突破尚未确认）。
- 不开空。V9 空头确认仍按 V9 原规则结束已有多单（反向确认退出属于出场，改它是第二个变量）。
- 数据缺口清空突破记录：缺口另一侧的突破不算「7 根之内」。

## 事前登记的臂（读价格之前写下，全部上表）

| 臂 | 定义 | 作用 |
|---|---|---|
| `v9` | 已发布 V9，多空双向 | 截断完整性校验（逐笔对账已发布 V9 账本） |
| `v9_long` | V9 只做多 | 把「只做多」这一步单独量出来 |
| `v10` | `v9_long` ∩ 突破后 0–7 根 | Owner 的 V10 |

V9 → V10 同时改了两件事（去空头、加突破门），所以必须拆成 v9 → v9_long → v10 两步归因，
否则「V10 比 V9 好/差」说不清是哪一步造成的。

## 事前预期（写在跑之前）

**预期不通过。** 理由：撤回的 V10 已经在同一段数据上按方向切过——多头在后段
2025-09→2026-05 是 V9 −0.2102 R/笔、突破门 ≤12 根 −0.2395 R/笔、配对超额 −0.114。
新规则的 0–7 根窗口落在已看过的「当根」与「≤12 根」之间，所以这次**不是盲测**，
结果的方向在很大程度上已被旧表决定。本轮的价值是给 Owner 一个精确按其口径落地的
账本和可粘贴的 TV 脚本，而不是新证据。

## 判定口径（沿用 V9/V10）

- 必报：全期 / 前段 2024-09-10→2025-09-10 / 后段 2025-09-10→2026-05-04 的每笔净 R、PF、
  胜率、总净 R、事件回撤；匹配随机对照（同流 × 同方向 × 同月 × 同 ATR 桶，同出场同成本）
  的配对超额与月块符号翻转 p。
- 「有效」的最低要求：全期与后段每笔净 R > 0、配对超额 > 0 且月块 p < 0.01。任一不满足即未过。

## holdout

**消耗 0。** 每条流在 2026-05-04T00:00Z 之前截断，开盘 ≥ 该时刻的 K 线一根不读；
截断用 V10 第一版同一个 `truncate`，并逐笔对账 V9 臂与已发布 V9 账本。
跨界需要 `docs/HOLDOUT_LEDGER.md` 新编号条目 + Owner 对话批准，runner 默认拒绝。

## 纪律

不训练、不 promote、不改仓、不动真金、不接实盘。`production_eligible` 与
`training_eligible` 保持 false。
