# Owner选定29币交易数量盘点

Owner于2026-09-19列出30个名称，随后明确「先不用」ARY；保留29币，PEPE映射归档1000PEPEUSDT。

- 只统计当前V11.2框内首次突破后独立做多；不混入V9、加仓、V10.4六根配对或其他参数版本。
- 15m与1h直接使用已绑定SHA的execution/run_v2 baseline账本。
- 原V11框内回放只覆盖15m←1h、1h←4h；本次复用原box.run_pair，补充4h←1d，映射与V11.2 Pine一致。参数、退出、串行占仓、20bp成本与日期均不变。
- 评估信号收盘2024-09-10 UTC至2026-05-01 UTC之前。沿用既有Binance5m归档，尝试向前读取1500根日线的预热长度，但如归档历史不足不捏造，逐币披露实际历史根数。新币上市较晚，样本覆盖不会强行等长。
- 报告已平仓、数据边界未平仓与全部入场三个数量，不把缺回测记为零，也不把1h的上级4h突破计作4h入场。
- BTC 1h用同一适配器复放，与既有账本逐笔身份和成交字段对齐，作为连接验证。
- 仅做数量盘点；不按本轮收益作策略或币种选择。不做新特征、参数搜索、训练或生产修改。原runner附带的其他参考臂和匹配控制留档但不混入主计数。
- 复现：`.venv/bin/python -W ignore -m yoyo.evaluation.spike_v112_selected_counts`。先提交适配器和计划再运行，输出counts.csv、selected_trades.csv、coverage.csv及逐币回放证据；无HTML。
