# Bark 作为当前 IMACD 可见启动的独立通知通道

Owner于2026-09-08提供自己的Bark推送地址并要求“信号推送到我的bark”。沿用现有仅实际可见focusRelease/tv_start、1H/4H、全OKX在交易永续、收盘与30分钟新鲜度契约，不调信号参数。TG保留；不恢复已经暂停的Codex AI巡检。

## 固定实现范围

- 同一新事件在一个事务中生成TG/Bark独立outbox；通道队列、回执、重试和首次启用时间互不消费。Bark启用前历史不补发，已有事件不重排队。
- 设备密钥只保存本机monitor运行目录bark.json，权限600；不改其他项目Bark/TG配置，前端、SQL和错误均不含密钥。
- 使用官方POST /push，禁用重定向，通知字段为币种/周期/方向/收盘点位/近零根数/标记与确认时间/TradingView链接。只认可HTTP200、code200、server timestamp的服务接受结果，不伪称手机已显示或已读。
- 独立后台发送线程；不确定发送不盲目重试。收盘可见标记不合格、历史或过期都不发。不发送启动、示例或连通性测试消息。
- 前端显示两个通道状态；Bark接入不触碰图表、筛选、均线、Pine源码、交易执行、VPS数据或模型。

## 验收

提交源码和collector后再正式取before/after。合成测试验证通道原子性、并发与重启去重、未来/历史/过期拒绝、切换时钟、限流与未知回执、私有配置权限和脱敏。真实服务重启后检查全市场健康、Bark已配置、cutover持久化、旧TG回执保持、无历史补发、前端双通道显示和密钥不出API。网络仅做不携带密钥的公开ping，无真实新信号则明确尚无新Bark推送回执。

沿用Owner全日期授权，本次通道配置第1次live-observation日期使用；没有收益计算/调参/训练，经济AUC/p/top-decile/胜率/随机入场不适用，以前述故障注入和分通道对照验收。交付HTML报告，不重写历史报告。
