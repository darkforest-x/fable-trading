# V25 analysis validation

## Overall Assessment: Share with caveats

Ready for the limited decision to stop this sparse configuration before any
outcome join. Not evidence that the trading system earns money, not a negative
profitability estimate for these nine cases, and not a deployable strategy.

## Methodology and independent calculations

- b1cca1e strategy/config/plan/tests committed before the actual support run;
  original251 cases/744 randomized controls remain, own clocks,3or0 membership.
- Actual accepted9/abstain241/unknown1; controls3/741/0. Four halves1/2/4/2,
  eight active months; support thresholds80/12perhalf/12months/3monthsperhalf
  all fail. No threshold was revised after observing the counts.
- One unknown is no_confirmed_break, not a missing price or observed abstention.
  All248 original matched groups known; all9 accepted cases have original
  triples. Their controls are1accepted/26abstain/0unknown; controls need not
  pass the new gate for the group to be observed.
- 48511b4 independent stdlib auditor was committed before real execution;
  actual status passed in independent_audit.json.18222savedhour rows,
  995contexts,62count rows,251mother rows,8strategy+30historicalsource pins,
  13input+4output hashes verified without importing the research functions.
- V24 previously published figures are historical context, not newly joined
  V25 outcomes. No raw5 archive,2025+price,holdout,labels or economic evaluation.
- 130new synthetic tests;447joint tests passed. Negative tests break causal
  clocks, exact joins, group membership, unknown semantics and source stability.
- Wider registry/boundary check with system Python reported177passed/3failed/
  2skipped:fastapi0.128.0 vs0.128.8,opencv4.12.0.88 vs5.0.0.93,pyyaml5.3.1 vs6.0.3.
  Both interpreters have numpy2.0.2/pandas2.3.3 used by this support run. Without
  changing dependencies, the project .venv rerun passed182registry/boundary
  tests (13legacy warnings), and the130new synthetic tests also passed there.
  Commands: `.venv/bin/python -m pytest -q tests/contracts/test_registries.py
  tests/boundaries`; `.venv/bin/python -m pytest -q
  tests/test_hourly_impulse_structure_event_research.py
  tests/test_hourly_impulse_structure_event_support.py`.
- Learning notes record event/known separation, source hashes bracketing work,
  and the difference between current reversal and persistent trend state.

## Visual / artifact review

- Technical narrative has13sections,14blocks,1native monthly bar chart.
  Chart map in artifact_build_receipt.json; all24UTCmonths including16zero
  months, original251 count,accepted9; singleblue root, exact labels, no
  redundant color-series grouping. Adjacent tables allow exact denominator
  and practical threshold lookup. No extra plots for isolated counts.
- Canonical artifact datasets retain total/known/accepted/abstain/unknown,
  not onlyx/y. Actual SQLite SELECT over frozen counts is recorded, not an
  invented SQL claim. No raw prices or outcome calculation in report builder.
- First official packaging attempt failed because a native chart source lacked
  an actual SQL query. The repair executes a real in-memory SQLite projection
  and supplies that exact query. Builder fix was committed48511b4 before use;
  strategy, support tables and the nine-event result were not altered.
- Official validation/package subsequently passed; verification structural_only.
  Headless-shell executable unavailable; no browser download or substitute
  bespoke HTML/chart renderer was installed. Browser/source-dialog/mobile
  visual/interaction checks remain unverified. Do not claim those passed.
- MD converted immediately using repository md_to_html; canonical package
  replaces that intermediate with the user-facing offline report.
- No extra notebook: reproducible source-first CLI,synthetic tests,frozenCSV,
  stdlib independent auditor and source-linked technicalHTML serve that role.

## Required caveats and scope

Reused2023–2024 development, not an independent validation period. Frozen
10/10 tie-inclusive Python approximation, not verified Pine builtin parity.
Saved-hour replay is not raw5 aggregation, exchange authenticity, live latency,
randomization replay or economic validation. Two hash passes do not protect
against transient changes subsequently restored. All opportunities and unknowns
remain; no winners were selected. AUC/PF/net/p/top-decile are not applicable to
this support-only stage; causal/identity corruption tests are the negative
controls. Goal stays active/unmet, with no promotion or TradingView overwrite.

## Handoff blockers vs caveats

No blocker to sharing the support-insufficient finding. Incomplete visual
browser/mobile/source-dialog QA is a delivery caveat. The insufficient support
blocks only this configuration's economic stage, not the broader research goal.
