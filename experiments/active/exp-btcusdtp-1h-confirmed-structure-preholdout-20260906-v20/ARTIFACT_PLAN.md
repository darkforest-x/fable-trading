# V20 report contract and QA scope

Audience: technical experiment/validation, matching the owner's detailed
strategy/failure-analysis request. One portable HTML delivery mode; no website.
Required structure maps to title, conclusion, metric definitions, temporal
support, trade quality, matching evidence, per-exit failure mechanisms, source
clock/design, uncertainty, next step/open questions, reproduction. Definitions
precede comparative findings so differing denominators cannot be misread.

Chart map: one full-width native bar chart, four fixed original halfyears,
own case accepted count, visible12 minimum reference, single blue palette with
labels; no line interpolation over sparse anchors. Chart SQL actually groups
the713 saved contexts in SQLite; extra total/abstain/unknown fields retained.
Source timestamp reflects query execution, not the older summary timestamp.
Other evidence uses exact multi-metric comparison/audit tables: different
denominators and paired accounting are the point, not another decorative plot.

Canonical artifact builder preserves all12 original report sections; content
comes from reviewed source MD. All quantitative narrative references that MD;
original context, summary and independent verification are also exposed as
canonical source metadata. Repo-required MD-to-HTML runs immediately after MD
creation/edit, then the official portable builder replaces the same HTML path.

QA:300 targeted tests pass. Independent stdlib verifier passes all18222 saved
hourly-state rows,713 requests,62 support rows and154 original triplets; it does
not independently audit raw5 aggregation or economics. A separate read-only
reviewer independently reconstructs all four serial masks, accepted old fields,
40 metric rows/1240 scalars and22 outcomeCSV hashes; no accounting-helper call,
no raw price read, no independent p/CI computation. Its findings are preserved
in the report; this is a review, not a new standalone fill-replay artifact.

Original failure and both outcome-access receipts remain. Frozen context
SHA checkpoint unchanged on resume; no market/feature rerun. Do not suppress
the initial serialization bug or label the resumed run a new configuration.

Portable packaging receipt records exact embedded-artifact and native runtime
verification. If browser QA is structural_only, disclose it; no browser install
or alternative endpoint workaround. The earlier local report preview was
blocked by URL policy, which is not bypassed through a local server or browser.

Actual final packaging:validation passed,package passed,verification structural_only;
13 blocks,1 native chart,exact embedded artifact verified. No compatible
headless Chromium installed; source dialog/interaction and desktop/mobile
viewports not verified. No install or URL-policy workaround attempted.
