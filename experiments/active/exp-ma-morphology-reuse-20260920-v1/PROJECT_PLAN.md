# Reuse feasibility: human morphology target, not legacy return target

Owner asks which old YOLO/L2 logic can identify their six-MA density and whether feature engineering from historical true regions is feasible. Scope is repository evidence reconciliation plus original221 reviewed-row OHLC/index/clock/image lineage inventory and176 starred-image availability. No training, label correction, source replacement, production or Pine changes.

Luna Max independently maps old L2 features, targets and negative experiment results. Parent audits stronger human sources and options: compact numerical morphology classifier, past-only multivariate templates, hybrid YOLO proposal plus geometry checks. Existing return-model weights cannot be renamed morphology probability. Auto-generated8000/10000 positives remain weak labels. Rejected/unknown/core boundary status must survive joins.

Use the reviewed221 manifest as-is; verify original source path exists, referenced first/last timestamps match, within-window15m clock continuity, core bounds and original image hash. Record present-source hashes without claiming historical OHLC parity when no old hash is available. Missing sources are excluded, never replaced with another exchange or a guessed index. All image/clock evidence remains read-only. Starred-image availability is separate from numeric OHLC linkage.

Deliver source inventory and a compact research design with target, candidate feature families, hard negatives, event-level time split, prefix detection latency and geometry-specific validation metrics. No accuracy from old proposal labels, no new fitted classifier or net-return claim. No HTML by Owner instruction. Commit builder before materializing inventory.

Recovery v2 after inventory: preserve v1's stale-path findings. Resolve relocated images only through originalSHA under archive/consolidated/yoyo-trading. Explicitly offer numeric timestamp rebinding to current files of the exact same OKX symbol and15m interval; require original start/end timestamps, complete equal-length15m grid and agreement among overlapping current-source windows. Derive core timestamps only from original within-window offsets. Do not claim original OHLC byte parity without its expected hash. Missing/conflicting windows remain unresolved; original manifests and eligibility stay unchanged.
