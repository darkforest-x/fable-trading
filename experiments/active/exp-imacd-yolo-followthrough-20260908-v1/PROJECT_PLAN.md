# IMACD＋YOLO：同收盘共振与延迟确认后的走势检验

Owner本轮明确要求“如何验证……给一点图，最好全局图……同时出现信号，以及后续走势是不是真的按照预期走出来”。本轮是对已冻结箭头/YOLO确认的后续走势标签审计，补完上轮只做筛选工程检查的缺口。所有原始1341事件和425确认保留，不重新推理、不改联结或选入规则。

## 授权与冻结范围

沿用Owner“任何时间段数据都可以使用不要有任何限制”，且当前明确请求后续走势。本轮是相同1H配置第3次、相同4H配置第3次消耗holdout；新增的是未来结果标签，不把旧样本称新盲测。前段Jan1–May4与模型原验证期重叠；后段May4–Jul1是再次复核。源为exp-imacd-yolo-expanded-20260908-v1已校验的54币原缓存和decisions，逐文件SHA校验，原预测和模型输入逐字不动。

## 观察合同：看方向兑现，不虚构一套持仓策略

唯一主要观察窗为24根；6/72根为提前列定的短/长描述窗。1H对应6h/24h/72h；4H对应24h/96h/288h。模型/指标锚点j都从下一根open[j+1]至close[j+H]，只在这H根high/low中计算MFE/MAE，排除信号本根；沿side取正向终点涨幅。MFE和MAE非负，ATR分母只取锚点已知值。没有固定止盈、止损、杠杆或组合仓位，MFE不是可落袋利润，MAE不是资金曲线最大回撤。

保存所有事件原IMACD锚点p的结果；对425确认另存真实模型锚点e的结果。同根e=p才是可在该收盘知道的即时共振。延迟组e>p从e后次开盘观察；原p处按最终确认/未确认分类只是事后诊断，不能冒充当时可执行筛选。16个原检测窗口截断保持censored未知，不归入模型拒绝。确认之后md失效仍按固定H保留，不事后删掉失败行情。

每个确认另以相同close[e+H]计算原p+1入场参考位移，提供同事件同终点的等待对照。原/确认入场价与displacement分开保留，不把追价百分比机械等同少赚的百分点。毛位移减20bp（沿用0.2%往返成本）只作静态摩擦敏感度，未建资金费/成交滑点/仓位/退出模型，因此不汇报实盘净值、策略PF或账户最大回撤。

## 对照与统计

全IMACD原箭头为信号基线。对每个事件/锚点另随机固定三份同币、同周期、同锚点收盘UTC月份、同因果波动五分位控制；方向同case，窗口和20bp成本相同。波动桶=(ATR/close)在此前240根（含已收盘锚点）的滚动百分位，不用全月事后rank。控制锚点当前及此前9根不可有IMACD释放，不能按后续走势/后续信号排除。每case内部不重复，跨case允许重复并披露依赖。

三份控制按event_id和anchor_kind的SHA派生固定seed20260908，在读标签之前选定，6/24/72使用同一组三个。某窗控制不完整时记录缺失，不补选；完整3控才计算控制均值和配对超额。所有case本身的完整率与匹配覆盖率单独展示，不只报匹配池均值。每组观察和图形在本fold_end截止，后续不足H根不填收益，不跨May4/Jul1边界；缺失控制不能当负结果。

后段24根的1H/4H×即时/延迟四组匹配超额作探索性ISO周聚类符号检验，所有币种同周共享符号，以周sum保持事件加权均值。≤18周穷举，其余10000次plus-one；四项Holm，缺失组不缩小家族。跨周持有窗重叠与控制复用仍存在依赖，因此不是随机试验或严格alpha认证。前段和6/72根仅描述，不挑最高收益周期升级。不对置信度新建排序器，不做top-decile调参。

## 图形选择与未来隔离

所有新图只放results/future_only，全部注明事后观察、未来未进模型。模型原始输入不覆盖，不生成训练labels。固定先选后段TF×方向最早的即时共振、要求72根完整（缺组据实缺图）；再选每TF最早延迟，另每TF×24根静态净正/非正各取最早即时，重复去重，最多10例。后四例明确是按事后结果分组的说明图，不当独立验证；总体统计始终包含全样本。

每幅图是前100根及确认后72根的宽双面板：价格/六均线、IMACD/零轴；标出原蓄势、模型核心、箭头与确认位置；同根则合并标记，未来区明显着色。计时为UTC，首屏和图注说明1H/4H不同自然时间。保留过去全局结构，不能仅截取上涨段。

## 验证与交付

计划/主构建程序/合成时钟测试先提交main，再执行一次冻结评价。报告构建程序先提交再生成。独立复算器从保存上下文和台账检查所有终点、MFE/MAE、原/确认时钟、三控身份与完整性、同终点等待对照、汇总和四项检验；图截断与输入哈希另查。

AUC/人工真假形态精度N/A：没有新金标或新监督任务；top-decile N/A：固定全体组比较，没有冻结的排序候选。报告给全组毛/静态净均值、中位数、方向兑现率、MFE/MAE与匹配随机，不以此冒充持仓策略收益。MD完成立即转HTML交付，图中可看未来和模型当时可见区物理分开。记录失败、风险与后续选项。无训练、promotion、TV、spike、通知、仓位或订单变更。
