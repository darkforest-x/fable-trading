# V37 — opposite state versus a genuinely new opposite edge

Frozen before V37 outcome replay. Origin: V36's63-event mechanism diagnostic,
not a blind discovery. One variable only, on reused development data.

## Question and immutable scope

Does existing native5 `transition_colour` improve realised net returns and
matched excess over V36 native5 `colour`, with the flow gate OFF in both arms?
Use original63 V36 cases,108 assigned controls,36 complete three-control groups;
keep27 unmatched cases in full-cohort results. Do not redraw assignments or
select only the38 early-opposite-continuation events. No new entry generation.

Exact same Binance USD-M BTCUSDT2023-01-01..2025-01-01-exclusive monthly inputs,
hourly K1K2 requests, stop at each original K2 extreme, actual next5m open,
native5 SMA40(HL2), .002 assumed round-trip cost,72h maximum hold and original
four-halfyear/exclude-final72h clock rules. No new TP, trailing stop, break-even,
partial, fee/risk threshold, cooldown, source, model or flow filter.

## Precise intervention

The unchanged engine seeds transition state from the complete[E−5m,E) bar.
If this pre-entry bar is aligned, it is armed at entryE, earliest exitE+5m.
If opposite/unknown, it first needs a valid aligned completed bar, followed by
an opposite bar. Only adjacent valid same-segment colours form an edge. Missing
management observation resets the edge; missing raw price censors the path.
Priority: gap hard stop, closed-bar market exit, timeout, current intrabar stop.
An aligned seed need not wait for an additional post-entry aligned bar.

## Estimands, dependency and progression

Primary, each original matched case: (transition net−state net) minus mean of
the same change in all its3original controls. Report both absolute matched
excesses and gross/cost decomposition. Identical complete fills imply equal
assumed fees, so improvement should not be merely fewer fees. Never replace
unknown path outcomes with cash0; only observed invalid-risk refusals are0.
Any missing member keeps the original pair unresolved, not silently removed.

Same V36 exploratory24calendar-month sign-flip,9999draws,seed20260906, one-sided
alpha.01;95% bootstrap of intact month groups within each fixed halfyear.
Month errors need symmetry/approximate independence; reuse, repeatedK1,
overlapping price paths and cross-month holding limit inference. Report monthly
lag1 correlation, skew/outlier concentration, median/SD/range, counts/missing.
No IID trade t-test, no observed-power claim, no alternative significance search.
Sample size is fixed by inherited63 requests, already below80 progression gate.
All old min12/fold, four positive folds, PF1.1,12active months,3/fold,90%matched,
positive mean/excess, p<.01 and positive CI gates stay unchanged. Passing is NOT
deployment or independent validation. Only one primary test; other inference
and subgroup statistics are descriptive, not separately selected discoveries.

## Full failure analysis and verification

- Replay BOTH arms; demand baseline exact identity and numeric parity (tolerance
  1e-12 for CSV roundtrip arithmetic) against saved V36 case AND control trades.
- Save immutable input receipt before paths, compare same decisions/stops/opens
  in both arms. Keep entry return metadata out of request inputs.
- Describe all63 and all108, every halfyear, case-only paired changes, matched
  changes, old38 early-continuation group and other25 WITH their assigned
  controls, lost/recovered winners, longer losses, stop/edge/timeout/censored,
  arming time and never-armed outcomes. Groups are labels, never entry features.
- Recompute per-arm single-position case ledgers; report blocked opportunity
  counts and closed returns. No account compounding/leveraged drawdown claim.
- No ML validation: valAUC N/A; fixed K1-body ranking AUC/topceil10% gross/net/
  wins in both arms is descriptive on reused development observations only.
- New synthetic entry/opposite/armed/stop/gap/unknown/mirror/scale tests; preserve
  existing engine and old V6 narrower-cohort transition-exit failures.
- Commit exact runner/config/plan/tests BEFORE real replay; strict source hashes
  and timestamp-only preflight, new registered output, no overwrite. MD then
  immediate HTML; disclose all failures and coverage limits.

V36 had two incidental old-holdout text exposures, already recorded; those old
texts are not new V37 market inputs. V37 only reads explicit code paths and
approved2023-2024 outputs/sources. No broad report/index/registry diff content.
No dependency installs, TV, ACTIVE, training, production or real-money changes.
