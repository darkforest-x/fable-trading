# V16 — Qualified fast partial realization

## Frozen question and single switch

Owner's continuing profitable-system goal is not a profitability promise.
V15 native15 true-transition exit lost16.6624bp per original intention: slower
management expanded winners but also deepened losses. Test one predeclared
policy switch on the SAME original251 cases/462 own controls: native15 baseline
versus a one-time50% realization on a qualified completed native5 true edge.
No entry gate, re-matching, MA/length search, stop movement,1R/2R threshold,
deadline change or optimized partial fraction. V15's final path is the anchor.

Qualification: consecutive valid native5 SMA40(HL2) bars change aligned to
opposite; latest fully completed native15 SMA40 is still aligned; directional
gross at the next executable5m OPEN is strictly greater than0.002. Decimal
quote-price algebra makes equality fail. Realize exactly half original notional
once, then manage remaining half exactly as native15 baseline. A failed edge is
consumed, never latched until price later recovers. A genuinely new later edge
can qualify. Initial opposite/unknown must first align. Both streams reset on
invalid/noncontiguous observations, without fast state changing slow state.

Priority: missing source/invalid open; gap-open K1 hard stop; original slow full
exit;72h deadline; fast half at open; current intrabar HLC and remaining-stop.
The current open is observable but its later HLC cannot cancel an already-filled
half. Unknown remainder keeps full-trade net unknown, not realized partial alone.
Same-open observation/fill assumes zero added execution delay; include this limit.

Each exit portion pays its own proportional20bp roundtrip cost; total closed
weights1 means20bp overall, not40bp for two exits. Strict20bp qualification
guarantees only the HALF's net positive, not the full trade.30bp stress re-costs
the same path; it does not alter qualification or re-run with a different gate.

## Bounded deduplication and support

V1 partial_colour used opposite-state on one stream then a second confirmation,
not true5m event while native15 remains aligned. V2 used a111-entry slope cohort
and1R/2R staged policies, not this original251/no-R fixed dual-clock test.
This is bounded source deduplication, not proof no related experiment exists.

Keep251 original intentions,462 fixed nonreused controls,154 triples and97
unmatched. Raw5m stop clock, direct hourly K1 extreme stop,72h,20bp unchanged.
Control own stop scales case risk/ATR to control ATR; controls use own colours.
No borrowed case context, no random time split, no excluded losers/unknown zeros.
Use only2023--2024 development price prefix; outside-prefix timestamp-only
boundary audit is not price access. No2025+ score/audit/holdout; holdout count0.

## Preregistered order and mechanical invariants

1. Commit builder, engine, config, plan and synthetic tests before real replay.
2. Pin V4 source mothers/assignment and V5 pre-entry context, not old outcomes.
3. Regenerate original hourly entries and5m entry context; require all-old-field
   parity. Freeze native15 initial states for both arms1426 rows and fast713
   observations with hashes before reading/hashing any parent outcome in run.
4. Pin V15 candidate six ledgers plus summary. Reproduce every existing field
   of all six baseline ledgers before new candidate outcomes are interpreted.
5. Run candidate in the same folds; independent fast/slow initial state parity.
   All713 final prices/times/reasons/hold/MFE/MAE/slow triggers unchanged.
   Same serial occupancy/selection. No-partial returns identical. Weighted
   partial return equals half old return plus half actual fast exit return.
   At20bp a baseline winner cannot turn into a loss; partial baseline losses
   must improve, but can stay negative. Violation is a bug, not an explanation.
6. Save every evaluated fast true edge and rejection reason plus source clocks,
   colours/MA/HL2/segments, actualopen, threshold and single fill diagnostics.
   Saved logs cannot independently prove raw MA or that an edge was not omitted.
7. D on all251; I on original154 with97 remaining unknown; serial all251 with
   known skips zero.9999 calendar-month bootstrap/sign flips,seed20260906.
   Distribution/outlier diagnostics retain every observation; no test shopping.
8. Independent saved arithmetic, clock/identity/weight and unknown validation;
   failure taxonomy and partial benefit versus truncated winner cost. Report
   all outcomes and exact loss/win conversion, not only partial-trigger subset.

## Unchanged success gates and honest inference

Require net positive, PF>=1.1, all4 positive halves,80 events,min12/half,
12 months,min3/half, serial net positive,>=90% matched coverage. Require
D>0 and I>0 each with month p<.01 and95%CI lower>0. Coverage154/251=61.35%
already fails90%; even favorable findings are diagnostic, not acceptance.
This repeatedly explored2023--2024 pool is NOT fresh independent validation.
Block resampling approximates dependence and does not turn observational
controls into random assignment. Report raw effects, CIs, exact exploratoryp;
no observed-power claim. No trained model: model AUC not applicable. Existing
single range/ATR feature AUC/top-decile gross/net remain descriptive, never a
replacement for aggregate and matched performance. No risk/money deployment.

## Reproduction and delivery

`.venv/bin/python -m yoyo.evaluation.hourly_impulse_dual_partial_research`

Runner refuses existing results; preserve failed attempts and explicitly record
reruns. Sources versions pandas2.3.3/numpy2.0.2/Python3.9.6 unchanged; no installs.
Technical HTML report: title/summary; definitions before evidence; aggregate,
fold/matched/partial failure cuts; all251 difference histogram; methodology,
uncertainty/validation; evidence-led next step and open questions. Exact tables
serve audits; one native full-distribution chart explains shape. Canonical
official builder only. Companion notebook reuses saved verifier (not second raw
or inferential replication). Missing browser/Jupyter QA must be disclosed.
No TradingView save, live orders, ACTIVE/promote/training or production changes.
