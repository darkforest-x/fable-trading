# 待Owner授权：直接预测最终净R>10的离线模型

本文件只是具体提案，不代表训练已获准或已经进行。AGENTS.md现有“P0/P1通过前禁止新训练与promote”仍有效。

## 为什么提出

V3从全49,207原信号构建独立标签，已结束49,070笔中448笔>10R；6960个固定规则中早段预选方案在后段没有稳定提升。V3.1逐月更新同一规则族，后段14/1680=0.833%，也未提高10R命中率。新的方向是让目标函数直接识别稀有10R结果，而不是继续堆固定指标。模型也可能失败，不能预先承诺高胜率。

## 若批准，具体执行范围

1. 仅本研究的20个信号收盘可用特征及周期。原版初始风险/费用/退出不变，目标严格最终净R>10；未知标签剔除训练但继续报告，不采样只保留赢家，不用币种名称、未来MFE或真实次开风险作特征。
2. 固定两个模型作对照：L2正则逻辑回归；小型LightGBM（7叶、深度3、150轮、学习率0.03、每叶至少200条）。参数先登记，不额外网格搜索；与低风险单特征排名及旧V9同表。
3. 按季度向前验证，首个预测季度2025Q4；只用预测季度前12个完整月、在切点前已结束且退出bar已完成的样本。季度内参数冻结。内部需要选择阈值时只用更早的时间块；禁止随机切分及跨界未成熟标签训练。
4. 报告1%、5%、10%、20%信号覆盖下的10R命中率、10R召回、PR-AUC、ROC-AUC、概率校准、净成本收益、同币同月同波动匹配随机、月块区间/置换检验。实际入场阈值必须从过去分数得到，整批排名仅作诊断。
5. 只有表现有改善才按原串行引擎复核；无法达到也完整归档。既有历史已暴露，称时间外探索复核，不称盲测/实盘验证。
6. 只批准这份研究数据和这两个模型的离线训练；不更改YOLO金标资格、全局阶段、ACTIVE、frozen、报警、Pine、部署、杠杆或订单。研究模型保存到新实验目录、production_eligible=false，不自动promote。依赖优先复用现有环境；不降级契约库。

需要Owner确认的是**本研究的离线训练例外**。完成上述授权前不启动训练，也不把现有training_eligible=false的产物改为允许训练。
