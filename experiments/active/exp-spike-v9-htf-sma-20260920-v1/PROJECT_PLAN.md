# V9 higher-timeframe SMA direction filter

Owner explicitly authorizes one additional multi-timeframe direction gate and a search for its SMA length. Use the prior29-asset universe (ARY excluded, PEPE=1000PEPE) and frozen Binance USDT perpetual5m archive; no new fetch or data-source substitution. Current question is pure V9 long/short, not a hindsight-selected joint subset. No HTML requested.

Only change admission: chart signal close above last confirmed HTF SMA for long, below for short. Compare20/30/50/60/90/120/150/200 and no filter. Map15m→1h,1h→4h,4h→UTC day. Use latest HTF already closed at chart BAR OPEN to match Pine SMA[1]+lookahead_on timing; candle closing on an HTF boundary cannot read that just-completed HTF until next chart bar. Require all underlying5m bars and contiguous SMA windows, expose unknown coverage rather than filling it.

All settings in config are frozen before market runs. Each arm fully replays one position per coin/timeframe, both directions, with raw opposite exits independent of entry filter. Stops/trailing/cost remain original. Include actual-availability scope and a separate common-SMA200-ready scope for every candidate so listing age/HTF warm-up cannot silently masquerade as direction improvement. No synthetic reconstruction of missing sources. Preserve partial-chart/gap limitations inherited from the existing V9 engine and count them.

Dates:2024-09-10 through2026-05-01 exclusive, split2025-09-10. Development excludes trades crossing the split. Select one shared long/short length per timeframe using development total net R (not validation, max PF or max per-trade mean). Baseline can win. Full-range/validation parameter ranks are descriptive and cannot change the selected value. The previously researched later period is a temporal validation, not untouched OOS.

Report closed/censored counts, signal counts/unknowns, wins, net/gross R and nominal returns, event-curve realized drawdown (not account drawdown), original realized10R retention, removed losers/winners and new reentries, by period/timeframe/direction/asset. Match random entries on symbol/direction/month/time-split/ATR-price bucket, use same exit/cost and fixed event key so common trades have identical controls. Count unavailable/censored/cross-split controls, never redraw on outcomes. Shared calendar-month block bootstrap/sign flips aggregate cross-coin dependence; adjust3 selected tests. Show selected and all candidate sensitivity without extending the search after observing validation.

Acceptance in config is deliberately separated from a historical maximum. If no candidate clears it, report the best development parameter and validation failure; don't label it optimal for live use or replace the default. A passed presentation candidate can be delivered as a NEW minor Pine file/private TV script; old versions must remain. No production/monitor/execution promotion.

Validate clock boundary, future suffix, missing5m/HTF gap, strict long/short and unknown handling, raw opposite exit preservation, final baseline parity against the original V9 adapter, and fill/fee arithmetic. Commit builder/config/tests before generating market artifacts. Register results, capture knowledge with unverified status and write a learning note.
