# V9 加入净2R含费保护：冻结对照计划

Owner 明确“加上吧。然后用这个回测一下”。本轮只增加退出保护，不调入场、成本、ATR、周期、ICT 或倍投。保留原V9作为比较基线，交付可加载的新版本Pine；原版不可变历史及监控身份保留。未授权部署、通知、订单或ACTIVE操作。

## 精确规则

初始止损仍由信号根及此前4根极值、信号收盘及ATR计算，下一根实际开盘与该止损之差固定为价格R。先检查当前有效止损；存活K线到收盘时，使用本根多头最高/空头最低，减去固定往返0.2%成本R，若达到净2R则把保护移到多头入场×1.002向上取tick、空头×0.998向下取tick。下一根生效；收盘时已回落亦不回写本根成交。保留原收盘毛2R启动4ATR跟踪，取更紧保护，原始反向V6下一开盘退出。

## 数据与时间

- OKX ETH 3m/5m/15m/30m/1H/4H：仅已有、已绑定的2026-05-01前数据。3m允许反序列化已绑定SHA的pre-only缓存；其余timestamp-first读取，30m以上由完整15m UTC分桶重建。不用Binance替代缺失OKX历史。共同区间2026-01-01至05-01，另报各周期实际可用范围。1500根预热。
- 可选原V9全量3531流：沿用冻结原始缓存与旧V9逐笔基线，2024-09-10至2026-09-10。其含holdout，必须在实现和配置提交后取得本配置明确授权才可以加载；不得因旧V9已有授权直接打开。授权前可完成合成检查与ETH pre-only回测。
- 固定两个退出版本，无参数搜索。过往ETH研究已暴露，不能将其称为盲验证。每一回放窗独立边界，边界/缺口仓位标记censored，不算自然平仓。

## 比较与验收

1. 关闭新BE逐笔对齐原串行引擎；全量范围另对齐原V9归档。对每条基线原始入场运行两退出的固定入场回放，识别救回亏损、伤害赢家和原净10R保留；同时重放真实单仓序列，容纳提前退出后增加的交易。
2. 每笔真实事件匹配同币、同方向、同月、固定因果ATR/价格桶和评估窗口的随机入场；同退出同成本。固定一次抽样，缺口/未闭合不重抽。月块置换和bootstrap，跨币同月整体采样。AUC/top-decile不适用：无预测分数、排序或训练；原版同入场对照为退出单变量基线。
3. 汇报毛/净R、严格净正率、按一个tick容差识别的近保本（独立字段、不冒充大盈利）、PF、逐笔结算累计R回撤、最长净亏/初始止损连串、触发次数、原净10R留存。多币累计R与回撤只是事件统计，不能称1000U组合权益/保证金回撤。未加倍投。
4. 合成边界覆盖同根止损优先、最高价触发与收盘价触发差异、毛净阈值、空头tick、跳空、止损不放松、反向准入独立、串行与固定路径以及前缀因果一致。
5. builder/config/tests提交后才跑；保存来源哈希、完整逐笔、fills/events、配对与随机对照、完成清单。MD立即转HTML，记录失败与适用边界、learning及Notion，禁止将效果不确定自动升级实盘。
