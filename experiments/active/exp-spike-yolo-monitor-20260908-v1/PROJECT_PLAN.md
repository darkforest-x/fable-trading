# spike IMACD → YOLO 本机通知接入

Owner于2026-09-08在已有全景图说明处明确授权：“将模型检测的逻辑也加入到信号检测中吧，接入当前的前端和信号通知中”。
范围为15m、1H、4H通知与前端，不是ACTIVE/frozen/实盘执行授权。

单一变更：通知条件从原IMACD可见箭头，改为原箭头后按既有冻结YOLO确认合同产生派生信号。
UI和双渠道outbox是同一条件接线的交付界面；模型、MA、0.25分数及等待9根等参数沿用先前报告，不做调参。

冻结输入源码8f0c1dc，已知样例重放builder4779754先提交再运行。
两方向/三周期合成负控；三个既有真实正例确认原像素与时钟；独立临时数据库及假发送器；本机真实加载/扫描/UI验收。
只复用三个既有样例，不计算收益或扩展样例筛选。这是本在线接入配置第1次消费已使用holdout的工程兼容复核；Owner此前在本对话已明确允许任意时间数据。
生产行情运行属于Owner另行明确授权的本机通知，所有时间保持因果。

通过门：未来不进入模型图，主线取消优先，推送从确认收盘计时，旧历史不补推、回执不重发，模型出错不放行原箭头，三周期持续扫描。
training_eligible=false；production_eligible=false（无实盘执行资格）。
