# V3 减少提醒且保留真实启动：固定两种结构干预

Owner 2026-09-10 明确开启目标模式“尽可能降低信号，但是保证正确的信号不丢”。授权继续离线研究与独立指标实现；此前所有日期授权沿用。本轮首次读取每个固定新配置的holdout，边界仍为2026-05-04；样本已研究过，不能称盲测。没有能保证未来零漏报的先验判别器；如实交付保留/漏报与收益边界，不回写正确标签。

## 预登记；真实评分之前冻结

唯一比较基准：原版SPIKE强劲爆发V1和当前V3。原版信号保持86条历史结果对照；当前V3重新重放核对10386预警/3809确认，所有新组使用同一278个OKX1H历史合约（不含BTC/ETH）、UTC2026-07-10至09-09前61日。冻结既有8046锚点/1660正例及其>=8%峰值子组，不改分母、不按今天赢家选币。

两种干预各自独立对照V3，不组合、不做网格搜索、不以评分决定再加第三门。

- A reference：预警条件原样，仅加入原Pine的信号收盘价参考占用状态。先按前根保护判断退出，再更新次根4ATR保护；活跃参考/退出当根不接纳新父。初始5根低点减0.2ATR与2ATR下限、2R激活、4ATR跟踪全部原值。每根更新完整raw上升沿，只有接纳父更新原12根冷却。无效风险不创建占用但保留父信号。child仍age0..3，包括参考已退出时的纯质量升级；不能复活/重置参考。缺口清状态并单列失联，末尾不人为解锁。
- B near_box：只替换结构资格。双线近零<=0.1前ATR连续12根，第12根冻结容差，箱高低继续纳入最后近零根；离开near才冻结盒，release age0..5内收盘突破完整上沿并站上SMA20/EMA20的真实上升沿可报，一盒一次；age6过期，新near序列废弃旧盒且需重新12根。原12根接纳冷却/child0..3质量不变，child父边界采用完整盒。未恢复单根4倍量/3ATR等门。readyfalse只清结构，gap全清。

## 衡量不能混淆

主保留率：固定V3及时命中的正事件中，新规则仍在t..t+1出现**实际新预警**的比例。并报1660全正及时召回、>=8%子组召回/保留、V3漏掉但新捕获、原时点保留/删除/新增/延后。A已有参考还存活单列因果跟踪覆盖，不加回新预警分子、不称实际持仓。

提示负担按预警数、确认升级数、同bar合并总标签数，和全池每天/每100有效合约日。同bar父子不重复计数。已结束父的child仍是质量事实，默认不画新入场框。正例定义仍是未来24根收盘+4ATR先于-2ATR；不是人工均线金标、不是净盈利。

固定工程目标：总标签较V3减少至少50%；>=8%子组及时召回至少80%，V3正事件保留至少90%。这些是明确可失败的研究目标，不是保证，不得事后下调。不满足则只提供研究候选，不能切换线上或宣称完成“零漏报”。经济条件独立：扣费相对匹配随机正超额、置换Holm p<.01。两候选父信号是主经济检验，child升级不创建第二笔交易；确认当根的独立假想入场可描述但本轮不评分，免重复开仓。原V3同样只将early作本轮收益基线。

## 顺序与成本

先提交所有精确builder/plan/tests。全局prepare所有信号与原标签、对照日程后，才evaluate。各检测先执行全部预热，不在START强制空仓。source SHA认证后加载pickle，连续1H数据不补空。

所有组共享同币、UTC周、因果ATR桶最多3随机入场，统一排除三组当前/过去12根父预警及升级；不跨未来排除。相同decision共用随机索引。原20bp往返名义成本、次根真实open、5根结构/2ATR初始止损、2R激活4ATR跟踪、无固定止盈。A信号收盘参考占用不是这个实际open交易路径；主收益仍由原simulate_trade执行，不能根据未来exit控制检测。缓存只复用同source/同tick/同decision/同cutoff且原执行源码认证通过的结果，未匹配决策用原引擎。

经济结果是独立事件不是账户NAV，不报告虚假组合收益/最大回撤。原始成本未覆盖完整funding/冲击/滑点。四时段完整/前31日/后30日/案例夜，逐周仅描述。两候选全期资产块置换校正；单特征量比/TR描述AUC与top-decile毛净/胜率/匹配随机；无训练val AUC不适用。

## 工程交付

Python两独立因果状态机、前缀/时间边界/反例tests；独立Pine两模式，默认A仅作为预登记待验研究模式，非上线。早预警小标记无盈亏框，确认参考框另起当前确认价且不重置A内部占用；主线/信号线、零轴、6均线保留，不加柱子。源码静态与原生编译/可用验证分别披露。Pine要求barstate.isconfirmed；不使用未来数据或回填信号(官方依据 https://www.tradingview.com/pine-script-docs/concepts/repainting/ )。

报告Markdown转HTML，包含HYPE/NEAR/PEPE完整阶段、固定算法选取一次成功保留与一次漏报/失败图。产物注册、独立算术/因果审计和learning笔记。本任务不联系其他用户会话，不改监控/Bark/TG/ACTIVE/实盘订单/其他研究。

首次prepare在新检测开始之前被分母断言拦截：标签文件含预热14904行，8046是研究窗内行。修正为以冻结研究窗断言，完整原标签字节仍保留。没有运行新配置市场检测/收益评分，没有修改标签/分母或重跑已评分结果。
