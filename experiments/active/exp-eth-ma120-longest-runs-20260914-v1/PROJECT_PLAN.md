# ETH SMA120 / EMA120 连续区间搜索

Owner 2026-09-14 指定 ETHUSDT.P，5min / 15min / 1h。此前明确允许任何时间段；本配置首次使用已研究过的 holdout-era 历史，记 exposure #1，不称盲验。

这是描述性搜索，不生成 SPIKE 信号，不修改 Pine、监控、自动审查、通知、止盈止损或订单。不筛最赚钱币，不优化120参数。

固定三个口径：收盘同时严格在两线上/下；开收实体同时严格在两线上/下（允许影线）；整根含影线同时在两线上/下。相等即断开。SMA120 基于收盘120根；EMA120首收盘播种递推；至少120根才比较。缺根打断段并重启均线，缺失不填补；1h要求完整4根15min合成。

选取既有 OKX ETH 5m缓存和最长单一15m深度缓存，1h由同源15m合成，不拼接不同下载版本、不换交易所。各自全覆盖榜和共同时间窗榜同时给出。全样本起止不同，不直接把最长长度差归因于周期。

一根不满足即结束，区间持续时长=根数×周期；结束时间是最后满足K线的收盘时间，下一根失败只在其收盘才可确认。缓存边界和缺口截断标注删失。最长是事后描述；可在线知道的是当前已持续根数。区间首尾价差不等于交易收益。

验收：相等边界、影线与实体分歧、断流、SMA预热、前缀不变性、完整重采样；逐赢家对整段条件及相邻失败根独立复核；完整CSV、输入SHA、六个收盘赢家图及实体对照图、Markdown与HTML、Notion待验证观察。无分类模型/下单策略，所以AUC、交易净收益、匹配随机开仓不适用；用合成边界与独立逐根检查代替非适用的盈利统计。
