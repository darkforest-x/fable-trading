# Profit3R event expansion and two YOLO arms

Owner explicitly authorized the complete offline pipeline and RTX3060 training on2026-09-22. This is an experiment-specific P0/P1 training exception, not global eligibility or production approval.

1. Reconcile1043 original events; freeze next-open entry,core stop,3R first passage,12h horizon,20bp cost.
2. Freeze source batches and scan unchanged morphology;deduplicate before profit filtering;retain failures and censoring.
3. At3000-5000 independent retained events,build A one-view and B two-view inputs with fixed right endpoint. Never meet capacity by repeats or weakened gates.
4. Shared chronological splits;physical future separation;geometry,SHA,duplicate,boundary audits.
5. Train sequentially on3060. Both40epochs;B has more exposures,so compare complete practical recipes rather than claim pure-position effect.
6. Evaluate full later candidate pool including failures;save costs,matched controls and limits. No promotion.

Coupled contract repairs and data/training stages are explicitly authorized. Optional risk/horizon clarification remains open;otherwise apply disclosed defaults.

Expansion addendum: prepare a separately frozen official 1m archive on the3060 machine while the original six timeframe batches finish onMac. `extension_1m.json` reuses the same655-symbol exchange snapshot and unchanged2019-09 through2026-08 historical ceiling. No morphology, entry, stop, cost or horizon changes. Compressed storage must preserve sourceCSV SHA; timeframe-specific evaluation is required because12h spans different numbers of bars. Independent-event capacity still controls training.

2026-09-22 Owner capacity amendment: "到1500个就开始处理 处理好了直接去训练吧" supersedes step3's original minimum. Mining stopped at1506 qualified train events after round017; no further expansion is required before the requested A/B training. This counts distinct deduplicated events, not statistically independent returns.

2026-09-22 rendering correction before training: Owner identified flattened images. The legacy6-percent relative-price floor squeezes low-amplitude1m/3m windows. Freeze `dataset_plan_owner1500_v4.json` before rebuilding a new `datasets/ma_profit3r_owner1500_v4` root. The only rendering change is `render.price_scale=visible_range_v1`: fit each visible window's low/high and six causal MAs, plus6-percent-of-span padding at each edge. Preserve original v3 images, cohort, boundaries, splits, colors, dimensions, MA support, trade barriers/costs and all training arguments. Recompute boxes with the same revised transform; never reuse old normalized y geometry. No shared-renderer default is changed. Audit unknown/mixed policies, future mutation, label blindness, sample/split equality, image/label integrity, and sample render-to-label geometry before3060 training. Local scaling also magnifies MA separation; no production or accuracy claim follows from a clearer image. A/B context variants may change vertical framing as well as position and remain full recipe comparisons.
