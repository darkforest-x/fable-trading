# Next bounded question — native15m exit on original V5 opportunities

Proposal only. Not implemented, registered as V15, run, selected or accepted.
V14 support actually ran and failed60/min11 against80/min12; no returns read or
computed. Do not alter20, reduce support limits or repackage a nearby grid.
Overall profitability goal remains active, unmet and not blocked.

## Bounded V1--V14 deduplication

- V1 `config.json` baseline251, `15m_first`/`5m_native40` use exit_mode=colour,
  an opposite STATE; there is already an original251 15m state-exit result.
  Initial builder commitc4e611f. Do NOT claim15m has never been tried.
- V2 is111 slope-filtered opportunities and staged realization; V3--V4 change
  prior context/K2 entry with old5m state semantics.
- V5 (`hourly_impulse_transition_research.py`, af9d4d8) fixes original251/462/154
  and introduces true aligned-to-opposite transition, management remains5m.
  V6 changes pending realignment afterK2; not the desired direct-entry contrast.
- V7--V8 use286cases849controls959zones without the original hourly body-cross
  requirement. V8 `PROJECT_PLAN.md` and config (e460539) already compare native5m
  and15m true transitions, but on that different population.
- V9 (12df0a0) still V7 population, management_minutes5/decision_minutes15;
  not native15m indicator aggregation. V10 support, V11 deadline, V12 frozenMA,
  V13 prior4h entry gate and V14 prior20 support do not add native15m here.

No exact same251/462/154 native5-vs-native15 true-transition comparison found in
these bounded committed configurations. This is not proof of global absence.
No new raw price or outcome calculation was used for this deduplication.

## Prospective distinction before investing in replay

Keep original251cases462controls154fixedtriples, original signal/entry prices,
K1 stop,72h,20bp. Remove bothV13 andV14 entry gates; do not add deadline/frozenMA
or partial TP. Compare native5mSMA40 versus native15mSMA40 using V5 true flip.

First commit and freeze all713 own initial state/availability/support rows for
BOTH timeframes, before outcomes. Include initial opposite/unknown and gap
recovery, no pooling or initial-opposite cases silently excluded. If original
15m entries already aligned and followup colours continuously valid, trueflip
may equal V1 old15m state exit; call any identical result a semantic/parity
replication, not a new exit edge. Differences need explicit mechanisms, not
renaming old results. Known5m initial opposite count cannot be transferred15m.

Native5→native15 changes indicator aggregation, decision clock AND effectiveMA
memory3h20m→10h. It is one frozen native-management SPEC comparison, not a pure
decision-frequency effect or a claim to isolate three independent mechanisms.

Do not call the old `hourly_impulse_k2_research.simulate_requests` unchanged:
it hardcodes5m. Existing `hourly_impulse_management_research.simulate_native`
selects timeframe from policy and is the reuse candidate; inspect its explicit
entry/stop/intrabar parity before using it. Alloldfield5m baseline parity first.

Retain all251 opportunities and original154control triples/97unknowns for D/I,
same serial occupancy rules, outcome/time alignment and cost stress. Report
earlier/later exits, avoided and added hard stops, captured/lost trend, and
foregone wins. No held-MFE feature may select trades: heldMFE<1R stops at the
old exit and says nothing about later movement. Label paired retrospective
mechanism analysis separately from prospective entry logic.

Old61.35%matching coverage still cannot pass90%; repeated2023--24 is not fresh
independent validation. A positive narrower mechanism is not goal completion
or production authority. No TradingView overwrite/live changes from this note.
