# IMACD 零轴启动监控纠正

Owner 2026-09-08 明确纠正：“监控不对啊，出信号了是指刚突破零轴啊”。本轮落实该监控定义，继续既有 Mac/OKX 所有永续合约、1H/4H 和 TG 授权，不进行收益调参或交易。

## 唯一信号定义

已收盘且完成指标预热，前根 IMACD 主线 md 精确为0，本根首次大于0为向上启动，首次小于0为向下启动。连续非零不重复；非零返回0不是启动；无零轴根的直接正负切换不归入“横盘后离轴”。不等待 ATR 范围，不依据 sb 金叉死叉。六均线密集与高周期仅作背景，保持原计算与取值时钟。

## 贯穿范围

- 唯一主事件为zero_breakout，版本imacd-zero-axis-monitor-v2；旧entry/release/exit/retest为图表观察。
- 扫描入队、发送前防线、API默认列表、24h计数、TG回执统计、页面箭头使用同一新口径。
- 已发送旧消息保留历史，不删除、不重命名为新信号。旧pending退役；启动先校准OKX时间并持久化首次cutover，此前重建信号只能历史展示，重启不重置cutover。
- 不修改生产执行器、模型、Pine、成本/障碍、新鲜度30分钟、120秒扫描间隔或任何凭据。
- 既有全部日期授权覆盖固定配置实时观察；这是本纠正配置第1次观察holdout日期，无经济评分。

## 验证

前后事件定义对照、零轴±微小值/延续/回零/无零直接反向/密集假/HTF拒绝、未来扰动、API与队列隔离、旧回执保留与时钟偏移cutover。源码冻结后重启实际473合约、946周期窗口，检查所有主事件 previous_md=0/current_md非零/前零根数≥1；检查新协议通知不含旧类、不含cutover之前历史。页面实查和HTML报告交付。收益AUC/p/胜率及随机入场不适用，用上述因果反事实与故障注入对照。
