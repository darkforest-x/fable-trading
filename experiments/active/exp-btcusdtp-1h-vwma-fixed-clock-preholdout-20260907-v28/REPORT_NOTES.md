# V28 report and chart contract

Technical audience as continuing methods-first trading experiment. One portable
HTML under repository requirement; canonical artifact/shared builder only.
CLI/immutable CSV/tests provide reproducibility instead of redundant notebook.

Required shape: title; technical summary;scope/288case/284pair definitions;
oldSMA versusnewVWMA descriptive comparison;fourfold conditional means;
24monthly observations;distribution/assumptions;validation;uncertainty;
next steps and furtherquestions. Reproduction added by repository requirement.

Two full-width native bar charts serve different jobs. Monthly mean4h cost
threshold uses24 UTC months, chronological discrete comparisons and zero line;
not cumulative portfolio performance. It retains own count/gross/control
count/background/excess for audit. The distribution is an explicit50bp binned
frequency display with every occupied and intermediate empty bin, all tails,
not a fit or filter. Both use one blue root, signed labels/order and visible
zero context. Adjacent paragraphs interpret each. Sparse overall/two-version
and exact fourfold lookup tables need no further decorative charts.

Snapshot comes from actual SQLite queries over saved case/paired labels.
Relative source paths and query metadata retained. Narrative source is the
complete reviewed report; no fabricated single-file provenance for joined data.
Official packaged verifier owns final QA. Structural_only means no installed
compatible browser; keep semantic chart tables and disclose no browser/mobile,
light-dark/source-dialog visual validation. Never install a browser to pass.
