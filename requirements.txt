pandas>=2.0
scipy>=1.10
matplotlib>=3.7
numpy>=1.24

# 阶段 2a 检测层（YOLO）
ultralytics>=8.4
opencv-python>=4.10
torch>=2.4
scikit-learn>=1.3
lightgbm>=4.0

# 看板（前后端）
fastapi>=0.100
uvicorn>=0.23

# 图片工具（fix_images.py、部分检测脚本）
Pillow>=10.0

# 治理注册表（artifacts/registry.yaml、experiments/registry.yaml）
# 纯 Python、无编译依赖；两个 scripts/ 早已在用，只是从未写进本文件。
PyYAML>=6.0
