> **注**(2026-07-20 更新):本文件是 07-07 定稿的路线图,按历史阅读。三阶段已全部走完:
> 2b 验收通过(07-08)→ 阶段 3 v11 池 accept 回测通过(07-18,holdout 第 5 次消耗)→
> **当前处于实盘阶段**(VPS 执行层 + 前向 100 笔新鲜裁决 + H-TIP v12 检测层实验)。
> 实时状态见 `HANDOFF.md` 顶部;本周计划见 `analysis/week_plan_20260720.md`。

> **Owner 授权的 ETH 3m pilot v2 目标重置（2026-07-29）**：owner 已明确“只需要判断
> 是不是”，并授权“弄 v2 数据集”。因此本轮不是在 v1 YOLO 上做可比的单变量调参，而是一次
> 有记录的任务定义重构：从固定右缘目标框检测改为当前 causal tip 的
> `short_start / no_start` 图像二分类，同时成组修正事件去重、序列负例与 200+60 bar
> 时间隔离。训练标签只认 owner 实际确认过的当前 tip；扫描端 `tip/tip-1/tip-2` 的定位容差
> 不得推导为事件寿命标签。该授权仅覆盖数据集构建与审计；不包含训练、阈值选择、holdout、
> promote 或实盘切换。

> **后续诊断训练授权与结果（2026-07-30）**：owner 随后明确“直接去3060跑吧”，并允许与
> PID 93656 并跑。固定配方只训练一次且不碰 holdout/promote/ACTIVE；21 epoch 早停后，固定
> p=0.50 的 val 为 TP0/FP0/TN34/FN8，未通过预注册 TP≥6/8 第一门，故按 fail-fast 停在静态
> val，不运行 smoke、不扫描阈值。详见 `analysis/p_eth3m_short_pilot_v2_cls_diag_20260730.md`。

> **失败根因复盘与数据重建门（2026-07-30）**：后续语义审计确认，当前 30 正/107 负来自
> 两个不同人工问题，provenance 对类别纯度 100%；`anchor==first_below` 构造元数据可得
> 136/137=99.27%，证明 target 与锚点生成规则混杂。按完整 `[T-199,T+60]` 暴露区间，
> 137 张只有 32 个依赖块，30 正图只有 23 块、val 正例只有 5 块。下一版必须统一逐行回答
> current-T“现在是否做空”，人工可看固定未来 3h、模型图物理截止 T；先过 240 张 D0 的
> 盲重复/source-only/依赖块 Gate，再扩 600 与 2,000。详见
> `analysis/output/eth3m_v2_problem_analysis_20260730/report.html`。

> **Owner 授权恢复 Local Signal V2 Stage A（2026-08-11）**：owner 明确允许恢复交接文档的
> 离线形态预训练。该授权是一次有记录的课程式训练恢复：Stage A 允许在历史、pre-holdout
> 窗口中看到 decision 后的真实 K，以改变 `crop_start_bar` 并打散形态位置；它只能学习表征，
> `production_eligible=false`，不得直接回测、promote、部署或写 forward。最终生产裁决仍只认
> 严格因果 Stage B + 真 tip。此前仅加右侧空白的 `causal_blank_w30_v3` 被 owner 目视否决，
> 因为框相对真实 K 线内容仍固定在最右端。

> **Owner 最终目标二次收紧（2026-08-11，覆盖上述 Local Signal V2 后续方向）**：owner 明确检测的是
> 局部“完美平台/启动形态”语义，不是盘口 tip，也不是固定坐标模板。ETH 参考的核心约 4–7 根，
> 只在两条竖线之间，禁止把右侧快速下跌包进红框。输入上下文不再固定 20–30 根，必须从最短充分
> 窗口开始动态变化；首轮候选约 14–22 根，后续继续按 precision 向更短收缩。核心结束后只允许
> **3–5 根确认，3 根优先、5 根硬封顶，6–10 根撤出**；验收分别报告 delay 3/4/5 的
> precision/recall/FP 与首次命中。后续涨跌不能自动定义核心正例。该决定同时改变目标时间语义、位置
> 分布与验收重点，已获 owner 明确批准作为有记录的多变量任务重置。昨晚 Stage A 数据、权重、
> 日志和候选挖掘继续保留并作为底座；旧 W20–30 图与原始框只作复核来源，不直接成为新训练合同。
> ETH 图是最终语义和边界参考，不替代旧资产。延迟形态模型仍为
> `production_eligible=false`；若未来接入实盘，必须以完整窗口右端作为信号时间，另行批准执行
> 延迟预算，禁止把它表述成新鲜盘口检测。
>
> **动态短窗校准结果（2026-08-11）**：首轮30张按post 3/4/5各10张、pre 6–10、旧核心5/7根
> 重渲染后，框中心自然覆盖53%–71%，说明无需固定最右或正中即可满足短延迟。但目视也确认旧核心
> proposal中仍有把启动大K包入框的噪声；这批只校准几何与上下文长度，全部禁止训练。下一门不是
> 继续调crop坐标，而是按Owner ETH语义同时裁决类别与核心边界，再生成正式短窗正/难负例。
>
> **Codex空头语义一审（2026-08-11）**：30张分为保留5、改框4、难负例候选4、多头镜像隔离17。
> 该分组只用于压缩Owner校准成本，不是金标。当前ETH例只确认空头；多头镜像在Owner选择排除、
> 独立类别或方向归一化前保持`mirror_unconfirmed`，禁止同时污染正例和负例。空头代表板确认后，
> 才把同一语义批量应用到200张审查池。
>
> **Owner确认并扩200（2026-08-11）**：Owner确认先只做空并认可代表板绿/橙/红方向；多头镜像
> 排除但不得当负例。冻结200事件已重渲染为W14–22，Codex一审得到保留40、逐图改框61、难负例
> 候选25、镜像排除74。61张已用带K线序号的工作板逐图找启动边界并重渲染，不用统一偏移；
> 新核心宽度4–7根、post保持3–5根、动态窗缩为W14–20。该确认仍是协议级而非逐样本级，故当前
> 全部不可训练；下一门是Owner验收61张新边界，并决定是否授权Codex一审结果作为正式样本标签。
> 当前200仅是从2,020个Stage-A train正事件中、符合旧框后3–5根条件的316个候选里抽出的语义
> 校准包，不是正式训练规模。人工审核页允许额外查看未来48根/12小时，但训练短窗与未来审核图
> 已按目录、manifest和SHA物理隔离；Owner确认后必须回到完整历史池扩正例与难负例。

> **Owner纠正标签血缘（2026-08-11，覆盖上段61张Codex重框下一步）**：既然原始Label Studio
> 金标及Owner逐框short裁决仍完整存在，就不得由Codex再凭视觉重画核心。新方案逐框精确联结
> `⭐原手框 ∩ Owner short`，从原手框正中心截取4–7根橙框，外层重裁W12–17，后文3–5封顶；
> 未来48根只供人工审核。当前精确交集71框，可恢复原始PNG 69框，0 holdout。旧61个橙框降为
> 失败对照，不得写入训练。Owner确认中心裁切合同后，同法扩到1,361个Owner-short独立框，再按
> 时间切分、依赖块去重并补真实背景/难负例。

> **全量中心裁切数据与首臂训练（2026-08-11）**：Owner回复“可以”后，1,361行Owner-short
> 标注全部按原框中心合同规划；15组相同市场窗+相同核心框的历史别名在split前去重，得到1,346
> 个独立目标。1,287个依赖块按85/15时间切分+150 bars purge后正例train/val/drop=
> 1,143/202/1，实际隔离162 bars；同币、同split、同W真实空背景train/val=1,143/200，2个val
> 无安全背景而诚实缺失。Owner随后明确“直接去训练吧”，3060已启动1:1 easy-negative baseline
> `owner_lsv2_short_gold_center_v1_ft`以挖hard negatives。按交接规范，第二臂才使用1:2/1:3且hard
> negative占大头；未读holdout、未promote、未改ACTIVE、未部署。

# 项目路线图（三阶段）

原则：每阶段有明确验收标准，不通过不进入下一阶段；单变量实验纪律；极简实现。

**架构决定（项目所有者，2026-07-07）**：保留 YOLO，采用两阶段架构——
"检测层"（YOLO 找双均线密集区域）+ "判断层"（数值特征分类器判断是否会真正启动）。
检测层解决旧项目"固定框假检测"的问题；判断层解决"图形好看 ≠ 会涨"的问题。

## 阶段 1：P0 alpha 检验（已完成，2026-07-07）

**目标**：回答"人工标注的'完美启动结构'正样本，其未来收益分布是否显著优于同规则预筛的负样本"。

**结论**（详见 `analysis/p0_alpha_report.md`）：

- 收益端（future_favorable_pct，MFE）：**无 alpha**。合并去重 301 正 / 1365 负，
  Mann-Whitney p = 0.844，rank-biserial r = −0.007，正负样本中位 MFE 几乎相同（1.79% vs 1.73%）；
- 风险端（future_adverse_pct，MAE）：**有显著 alpha**。p ≈ 9.2e-14，r = +0.274，
  正样本中位回撤 −0.39% vs 负样本 −1.19%；
- 判定：人工标签识别的是"跌不动的点"而非"会涨的点"，**不能直接当判断层的分类目标**。

**对阶段 2 的直接影响**：判断层标签采用 **triple-barrier 结果导向标注**（P0 无收益端 alpha 触发的路线）；
人工标签降级为辅助过滤/特征。

## 阶段 2：两层架构

### 2a 检测层：YOLO 检测双均线密集区域

- **标注全自动、规则化**：按明确的均线间距阈值（如 fast_spread / full_spread 上限）在
  历史 K 线图上自动生成标注框，框的位置和宽度随真实密集区变化（不再是固定右缘竖条）；
- 样本量可达数万（旧项目合成数据 mAP50 0.98 已证明 YOLO 能学会这类几何模式）；
- 关闭 fliplr/mosaic/hsv 等破坏时间方向与红绿语义的增强；
- 检测层只回答"哪里有密集区"，**不做任何涨跌判断**。

**验收标准（2a）**：

- 真实数据验证集 mAP50 ≥ 0.90，且与纯规则扫描（同阈值直接算指标）对照，检出一致率 ≥ 95%——
  YOLO 在这里是规则的视觉替身，一致率低说明标注生成或训练有 bug；
- 验证集按时间切分，禁止随机切分。

### 2b 判断层：密集区域 → "是否会真正启动"分类器

- 对检测层输出的每个密集区域，从 OHLCV 直接计算数值特征：均线间距序列（收敛速度/形态）、
  密集持续根数、量比/量能 z 分数、波动率位置、与慢速均线的相对位置等
  （P0 已验证 ma_spread_pct 与未来 MFE 的 Spearman ρ≈0.4，特征路线有依据）；
- 轻量分类器（LightGBM 起步）；
- **标签：triple-barrier**（止盈/止损/超时，标签 = 先触哪个障碍），直接对齐交易盈亏；
  这是 P0 结论决定的——人工标签无收益端 alpha，不能当主目标。
  人工标签可作为可选辅助特征或风控叠加（其"低回撤"信息真实存在），须单变量验证增益；
- 按时间切分训练/验证，禁止随机切分；特征与标签严格无前视。

**验收标准（2b）**：

- 时间外验证集 AUC 显著 > 0.5（置换检验 p < 0.01）；
- top-decile 信号的 triple-barrier 期望收益扣除 0.2% 往返成本后为正；
- 每次特征/参数改动记入实验日志（单变量纪律）。

### 2b-v2 迭代（项目所有者决策，2026-07-07）

2b-v1 结论"有信号、没利润"（holdout AUC 0.591 / p=0.002，但 top-decile 净收益 -0.11%）。
三项决策：

1. **障碍结构**：TP 4×ATR / SL 2×ATR + `atr_pct ≥ 0.0015` 下限过滤
   （中位毛利 TP ≈ 1.13%，为成本 0.2% 的 5.6 倍；下限保留约 83% 样本）；
2. **数据补齐**：`src/data/fetch_okx.py` 拉取 ~55 币种 400 天 15m 历史至 `data/kline_fetched/`
   （沙盒无法访问 OKX，需在本机运行）；
3. **候选池**：新增 expanded 阈值预设（strict 的 ~1.6 倍放宽，本项目自定义，非旧项目移植）。

**实验矩阵**（新数据上）：strict × v2 障碍、expanded × v2 障碍，各自独立训练评估；
两池对比回答"扩池收益是否大于基线稀释代价"。

**holdout 诚实声明**：v1 的 holdout（≥2026-05-04）已消耗一次；新数据向过去补齐，
向未来仅多 4 天，时间上无法完全换新 holdout。缓解：标签结构已变（新障碍），
且 v2 全程只允许在最终对照时各评估 holdout 一次。此风险记录在案。

## 阶段 3：回测框架 + 冻结 holdout + 实验纪律

**目标**：把"检测层 + 判断层"整条流水线放进带成本的事件驱动回测，得到可信的样本外绩效。

**做法**：

- 简单事件驱动回测（taker 费 + 滑点 + 资金费近似），检测 → 判断 → 持仓 → 平仓全链路；
- **冻结 holdout**：留出最近 N 个月数据，全程不参与检测层标注阈值调整、判断层调参，
  只在最终验收时跑一次；
- **单变量实验纪律**：每次只改一个变量（一个特征、一个阈值、一个参数），
  实验记录（假设/改动/结果）追加到实验日志，无论成败都保留。

**验收标准**：

- holdout 上（仅跑一次）：扣费后收益为正，PF ≥ 1.3，最大回撤 ≤ 20%，交易数 ≥ 100 笔；
- 不满足则回到阶段 2，禁止在 holdout 上迭代。
