# 研究议程（进化引擎）— 2026-07-09 首版

> **2026-07-20 现状**：主线已进入 **VPS 实盘 + 前向 100 笔新鲜裁决**。  
> 当前最高优先假设是 **H-TIP**（`owner_v12_htip` 训练中，不自动 promote）。  
> 周执行清单：`analysis/week_plan_20260720.md`；实时状态：`HANDOFF.md` 顶部。  
> 旧优先序 H9→H10→H1 的发现级已完成；**确认级一律前向**，勿再 val 挖矿。

> **2026-07-16 增补（历史）**:①标注价值曲线已出样本外验证;②标杆过采样×3 证伪;
> ③**H-TS** 已跑完 07-17（时间泄漏非 F1 虚高主因，见 `analysis/p2a_hts_report.md`）;
> H-R8 窗口不重叠; **H-EX 执行层已上 VPS live**（不再是 demo 前置项）。

> 本文件是系统的"假设发生器"：agent 主动提出假设 → 按纪律测试 → 无论成败入库 →
> 滚动补充新假设。owner 不需要口述实验，只需要裁决方向性分歧。
>
> **两级制度（防止挖矿式过拟合）**：
> - **发现级**：train/val 上自由探索，结果只算"线索"；
> - **确认级**：只有前向数据（或未来新划的冻结窗口）能把线索升级为结论。
>   发现级赢 10 次不如确认级赢 1 次。
> 负结果照样入库。

## 状态图例
🟢 已验证（发现级） · 🔴 已证伪 · 🟡 排队中 · 🔵 进行中 · ⚪ 未开始

## A. 出场结构族（回应"趋势策略不该只有简单止盈止损"）

已测：固定 TP4-8×ATR 🟢(TP5 最优)、纯拖尾 2-4×ATR 🔴(起步即被震出)、
拉长 horizon 🔴(edge 在 ~5h 内兑现完毕)。**已知形态特性：短促脉冲而非持久趋势**，
下面的假设都要与这个事实相容：

| # | 假设 | 设计 | 判定 | 状态 |
|---|---|---|---|---|
| H1 | **分批止盈**：半仓 2.5×ATR 落袋 + 尾仓 3×ATR 拖尾——锁住脉冲利润同时保留尾部 | labeling 加 `label_candidate_scaled`（realized_ret = 两段加权）；进 sweep | 净@maker > TP5 基线 +0.02%/笔 | 🟢 **发现级强通过（07-09）**：SWAP val AUC 0.6106、p=0.001、top 净@maker +0.326%，maker 组合 PF 2.825/maxDD 0.29%；成为最强发现级候选，待前向确认 |
| H2 | **保本推移**：+1.5×ATR 后 SL 上移至入场价——把 44% 胜率的左尾砍掉 | labeling 加 breakeven 变体 | 净不降且 maxDD 显著改善 | 🔴 **发现级不通过（07-09）**：SWAP val AUC 0.5172、p=0.1738，统计不显著；虽 maxDD 好于 TP5，但不能升级 |
| H3 | **结构出场**：收盘跌破 EMA21 即出——用形态自身定义"脉冲结束" | labeling 加 MA-exit 变体 | 对比 TP5 的净与持仓时长 | 🟢 **发现级通过（07-15）**：top 净@maker 显著高于 TP5、持仓更短；见 `analysis/p15_h3_ma_exit.md`；确认级待 shadow 前向 |
| H4 | 时间衰减紧缩：持仓每 12 根 SL 收紧 0.25×ATR | 同上 | 同上 | 🔴 **未通过（07-15）**：与 TP5 持平；`analysis/p15_h4_time_decay.md` |
| H5 | 波动率自适应：障碍倍数按 atr_pct 分位缩放（高波动给更宽） | 同上 | 分波动率分层看净收益 | 🟢 **边际（07-15）**：净略升；不切主线；`analysis/p15_h5_vol_adaptive.md` |
| H6 | 反向信号出场：同币种出现向下密集破位则提前离场 | 需先做 H10 | — | ⚪ 依赖 H10；低优先（主线已在实盘） |

## B. 多时间框架（回应"不该写死 15m"）

前提工程：fetch_okx/update_okx/loader/build/train 的 bar 参数化已完成（P1.5 R0，2026-07-09）。

| # | 假设 | 设计 | 判定 | 状态 |
|---|---|---|---|---|
| H7 | **主流币 5m 池**：BTC/ETH/SOL 等前 15 币，5m 密集脉冲更频繁、maker 更易成交 | 独立池独立训练；horizon 折算（72×15m≈216×5m？不——按"事件时长"重标定，先 sweep {96,144,216}根） | val 显著性 + 净@maker>0 且笔数 ≥3× | 🔴 **发现级证伪（07-09）**：5m val 仅 0.63× 15m baseline，未达到机会数 ≥3×；filled-only 净@maker 全负。TON_USDT_SWAP 在 OKX 不存在，实际 14/15 主流币 |
| H8 | **山寨 30m/1h 池**：低流动性币在高 TF 上噪音更低、密集更"真" | 同上，horizon {24,48,72}根 | 同上 | 🟢 **30m 发现级通过（07-09）**：30m h72 AUC 0.6297、p=0.001、top 净@maker +0.484%、filled-only +0.521%；但样本仅 0.24× 15m，是低频高质量线索。1H 样本 52-55，p 未达 0.01，不确认 |
| H9 | **高层趋势过滤**：15m 信号 + 1h 趋势对齐才进场 | `src/judgment/trend_filter.py` + `scripts/h9_trend_filter.py`（1h 由 15m 聚合，严格用已完结 1h bar，无前视） | 发现级有效；未确认切主线 | 🟢 **发现级通过（07-09）**：spot `h1_above_ma` top 净@maker +0.152%→+0.203%，maker 组合 PF 1.271→1.520；SWAP 方向复现，maker PF 0.964→1.204（above）/1.281（slope），但样本 44-49 笔且未过 PF 1.3；特征版 top 净 +0.026%→+0.180% 但 AUC 0.560→0.553。详见 `analysis/p15_h9_report.md`，确认级等前向 |

## C. 方向与宇宙

| # | 假设 | 设计 | 判定 | 状态 |
|---|---|---|---|---|
| H10 | **做空侧（第二优先）**：向下密集破位的镜像策略——合约可做空，机会面直接翻倍，且与多头低相关 | candidates 镜像规则（ext_up→ext_down 等）+ 镜像标签；独立池 | 同 2b 验收标准 | 🟢 **发现级通过（07-09）**：SWAP short TP5/SL2 val AUC 0.6174、p=0.001、top 净@maker +0.205%，maker filled-only +0.131%；但 ma_spread baseline 净@maker +0.343%，模型价值未超过单特征。详见 `analysis/p15_h10_short_report.md` |
| H11 | 币种分层模型：主流/山寨分开训练（体量差异大，特征分布不同） | expanded 池按市值二分，各自训练 vs 合池 | 分层是否稳定优于合池 | 🔴 **未稳定碾压合池（07-15）**：`analysis/p2b_h11_tiered.md` |
| H12 | 资金费率特征：极端 funding（拥挤度代理）时密集突破更易失败 | funding 数据已有 | 特征重要性 + 净提升 | 🟡 低优先；实盘期先盯真实资金费成本 |

## D. 特征与信号质量

| # | 假设 | 设计 | 判定 | 状态 |
|---|---|---|---|---|
| H13 | BTC 大盘状态：BTC 1h 趋势/波动作为全币种共享特征（山寨跟随大盘） | 加 3-4 个 BTC 状态特征 | 单变量增益 | 🔴 **未通过（07-15）**：`analysis/p2b_h13_btc_regime.md` |
| H14 | 主动买卖比 / taker 近似买压 | factor_ic_screen | 同上 | 🔴 **dead IC（07-15）**：`analysis/p2b_factor_ic_vol.md` |
| H15 | 密集"质量"二阶特征 | ma_order / convergence / bandwidth | 同上 | 🔴 **dead IC（07-15）**：selection-conditioned；`analysis/p2b_h15_quality.md` |
| H16 | **放量突破入场**（owner 直觉）：密集后首根带上+volume_z 再进 | 入场变体 vs 现行 | 净@maker **与** 胜率同时改善 | 🟡 量价族仍可做；**排在 H-TIP 之后** |
| H17 | 缩量深度 vol_dryup | factor | IC / 净增益 | 🔴 **dead IC（07-15）** |
| H18 | OBV 斜率区分真假密集 | factor | 同 H17 | 🔴 **dead IC（07-15）** |
| H19 | 外部 alpha 因子 IC 筛选 | factor_ic_screen | 存活再单变量 | 🟡 已有脚本；非实盘阻塞 |
| **H-TIP** | **tip 右缘分布**：中图标注→右缘重渲克隆，使 live tip 可被检出 | `build_htip_dataset` + finetune v12 | tip_hit≥0.20 且 frozen-F1 回撤≤0.03；再 shadow→owner 切流 | 🔵 **进行中（07-19/20）** `owner_v12_htip`；计划 `analysis/h_tip_plan.md` + `week_plan_20260720.md` |
| H-TS | 检测训练图截止 holdout | 踢 post_cutoff 重训 | frozen-F1 | 🟢 **已跑（07-17）**：非 F1 虚高主因 |
| H-EX | 执行层实盘括号/滑点 | VPS executor | 可运行+ledger | 🟢 **已上线 live**（确认级仍靠前向 100 笔） |

## 优先队列（2026-07-20 起）

1. **H-TIP / v12**——唯一阻塞「实时 tip 进裁决」的检测层假设；周计划见 `week_plan_20260720.md`  
2. **前向 100 笔新鲜积累**——被动；不调门、不清账  
3. **H1 shadow 确认级**（可选带宽）——发现级已强；主线仍 TP5/SL2  
4. **H3 shadow**（可选）——发现级通过；不接 executor  
5. H16 等发现级——仅在 H-TIP 落地或明确放弃后  

历史队列 H9/H10/H1/H7–H8 的发现级结果保留在上表，**不再作为默认开工顺序**。

## 议程维护规则

- 每完成一个假设：状态更新 + 结果一行 + 指向报告；新想法随时追加（编号递增）；
- 每批实验后必须自问并记录："这次 val 又被看了几眼？"；
- 任何发现级的赢家想上主线：先冻结 → 前向确认 → 才许改 HANDOFF 主线配置；
- 实盘期：**改新鲜度门 / 脉冲任务 / ACTIVE / 清 forward_log = owner 决策**（见 `CLAUDE.md` 实盘纪律）。
