# spike 指标启动与模型确认通知（2026-09-09）

## 当前展示与 Bark 周期（2026-09-09 晚，v1.12.0）

Owner 最新要求覆盖本文件下方历史口径：

| 周期 | 前端信号与模型确认 | Bark 原始启动 | Bark YOLO 追加 |
|---|---|---|---|
| 5m / 15m / 30m | 保留 | 关闭 | 关闭 |
| 1H / 4H / 日线 | 保留 | 开启 | 开启 |

Telegram 全部关闭。5m 重新扫描，沿用原始启用时间；过去已 disabled 的5m候选保持终态。
静音只清退 Bark pending，不改已发送/失败/未知历史回执，不终止15m/30m正在检测的候选。
前端当前权限读取 runtime.bark_timeframes / display_only_timeframes，不从 direct_timeframes 猜测。
Windows3060读取同一个Mac服务；本机客户端需同步5m开图白名单，Mac/Windows均支持 interval=5。

扫描使用5m/15m/30m/1H/2H/4H/1Dutc/1Wutc共8条公开行情流。完整分页冷启动最多24N请求，
475合约在8请求/秒下请求预算1425秒，约24分钟，短周历史会减少请求，还需计算与网络时间。
最短边界约N请求，全对齐最多8N请求；整轮仍等待120秒。30分钟新鲜度和所有交易/模型参数不变。
这是工程展示/通知范围调整，没有新的收益评估或模型泛化结论。

以下各节保留当时上线记录，当前行为以上表为准。

Owner 本轮明确要求把先前全景图中的模型检测接入当前 Mac 前端和信号通知。
本文件记录当前局部授权：15m / 30m / 1H / 4H 的本机通知串联。
它不改变 ACTIVE、frozen、VPS forward、执行器、实盘订单或训练资格。

2026-09-09 Owner 最新授权：关闭5min、开启30min；15m / 30m / 1H / 4H 原收盘箭头先发送指标启动；
YOLO 通过后另外发送确认。全部 Telegram 通知关闭，仅保留 Bark。此处覆盖此前15m等待模型及双渠道口径。

## 关闭5m、开启30m（2026-09-09，覆盖下方5m历史上线记录）

当前扫描与通知周期为15m/30m/1H/4H，5m停止后续扫描和两阶段通知；已有事件和终态回执保留。
5m未发送Bark项标记skipped，未完成模型候选标记disabled，原因timeframe_disabled_by_owner。
发送出口和模型入口再次检查当前周期，不会因持久旧候选或队列而继续推送5m。
30m有独立direct/model首次启用时间，不能借用原5m或其他周期的时间补发历史。
前端以30m替换5m筛选，interval=30。Pine自动高周期实际为2H；不是4H，仍仅作背景，不增加过滤。

30m本机数据流为15m/30m/1H/2H/4H/1Dutc，共6条。完整720根历史、每流3页时预算18N请求，
475合约在8请求/秒下约1069秒（约18分钟），还需计算与网络耗时。15m单独边界约N请求，
30m边界约2N，全对齐约6N；整轮结束仍等待120秒。新鲜度30分钟、限流、模型参数和TG关闭规则均保持。
近零12根在30m为6小时；只是复用规则，不声称30m已经证明更高收益。没有修改Pine或初始止损。

实机 v1.10.0 / 177bc89 于北京时间15:24部署；30m direct/model 首次启用分别为
15:24:16.702 / 15:24:16.685。旧18项启用元数据和旧TG266/Bark250队列中的终态回执保持；
TG无新行，5m待发Bark与pending/error模型候选均为0，10个未完成5m候选已标记disabled。
运行源码SHA全部一致。BTC30m实际图表240根、间隔1800000ms、全部已收盘；前端30m筛选已点验。
752项监控/边界测试与53项前端测试通过。部署后全市场1900窗口正常预热中，尚不宣称全覆盖就绪；
证据 `output/qa/spike_30m_switch_20260909/`，没有发送测试或启动通知。

## 5m 扩展（2026-09-09）

Owner 明确要求 5min 也发送信号；沿用收盘启动先发 Bark、YOLO 通过追加的两阶段规则，TG 仍关闭。
前端信号与蓄势观察均支持5m筛选，网页备用链接使用 interval=5，Mac入口支持5m。
高周期背景按Pine自动映射为1H；不是15m。近零12根在5m代表1小时，阈值与模型权重未调整。
5m模型复用是工程接入，尚未证明其周期泛化或收益；初始止损只做诊断，没有改Pine止损。

以下是本机通知扫描的容量预算，不涉及VPS执行脉冲：

| 项目 | 扩展前 | 增加5m后 |
|---|---:|---:|
| 每合约独立行情流 | 4 | 5 |
| 冷启动最多约3页/流 | 12N请求 | 15N请求 |
| N=475、8请求/秒的冷启动请求下限 | 712.5秒 | 890.6秒 |
| 只有最短周期更新时 | N请求 | N请求 |
| 四小时/日线同时更新时 | 最多4N请求 | 最多5N请求 |

全轮完成后仍等待120秒；5m边界N/8约59.4秒，全对齐5N/8约296.9秒，另有计算与排队开销。
实际延迟可能跨过一根5m，不能宣称5m通知必在5分钟内抵达。冷启动可超过15分钟，按扫描进度判断。
扫描、发送前校验、前端三处新鲜度继续为30分钟；没有改任一门限，也不保证端到端延迟。

实机 v1.9.0 / d22f5ed：14:51启用，475合约×4周期首轮于15:04:40完成，794.39秒。
首轮跨收盘边界有31项等待最新确认K；第二轮于15:09:12完成，151.81秒、1900/1900、错误0，
15:09:59健康检查ok=true，5m K线已从旧14:50推进至15:05。单次成功不是持续端到端时延保证。
旧16项启用元数据及旧TG266/Bark231回执保持，TG没有新增行；前端5m筛选、SNDK5m真实K线与interval=5已验。
测试748项后端/边界、53项前端通过，最后错误文案调整另41项定向通过；证据 `output/qa/spike_5m_20260909/`。
`after_verified.json` 另核对3条真实5m原始启动（NOK、ROK、OPN）及1条NOK的YOLO追加，Bark均已接受。
四条由收盘至Bark接受约3.75–8.70分钟，包含初次预热追赶，不能代表稳态分布，也不是手机展示回执。

## 当前规则

1. 原 IMACD V2.2 可见 `focusRelease` 箭头先登记为候选。
   原协议 `imacd-tv-visible-start-monitor-v3` 保留；近零12根 / 0.10 ATR / showMarks=false 不改。
2. 从原箭头 p 当根收盘到 p+9 收盘，最多检查10个收盘时点。
   每个时点仅渲染截止该根的18/19根输入，使用原白底1280×742绘图器及 close 源六均线。
3. 模型须同方向、核心4或5根，核心结束至当前输入右端2–9根；核心与原冻结蓄势段（含箭头p）有至少1根实际K线重叠。
   先选最早通过端点，再按检测分数降序、窗长升序、检测ID排序。
4. p至确认端点主线必须持续同方向非零；回零、反向或缺失md优先取消，即使同根有模型框也不通过。
   行情缺根 / 推理失败保留异常状态，重试未完成端点；超过等待范围且历史已无法恢复时诚实记为历史不可用，不能冒称已检测无命中。
5. 确认产生独立 `yolo_confirmed` 事件，协议 `imacd-yolo-confirmation-monitor-v1`。
   原箭头时间/收盘价与模型确认时间/收盘价分开，通知新鲜度从模型确认收盘计算。

冻结配置：`owner-grade-v1-w18w19-post2to9-wait9`。
权重：`analysis/output/ma_launch_owner_grade_a8000_neg24000_v1/ma_launch_owner_grade_a8000_neg24000_v1_y11s_ft1280_full40/weights/best.pt`。
SHA256：`862705b999594355c1133640acc540f4de19b561889e89d9e050ddad5c6db838`。
推理参数：conf=.25、iou=.70、imgsz=1280、rect=true、half=false、max_det=300，MPS可用时用MPS，否则CPU。
不使用离线研究里“未来必须有完整随访”的条件；没有未来收益、下一根开盘或图表主题作为输入。

## 页面与通知

前端默认显示指标启动，支持15m / 30m / 1H / 4H；另可切换模型确认、等待确认及全部已登记候选，仍支持周期筛选、深浅主题及图表放大。
原始蓄势观察保持独立，不等于信号。确认详情有原箭头、模型核心区间紫框及确认竖线。
紫框纵向是模型核心时间内K线高低包络，非重绘原YOLO纵向边界，也不是止盈止损。
检测分数不是胜率。图表保留原箭头和确认两个时点，不把后来走势当作当时模型输入。

Bark 接受以下事件，前端仅显示 Bark 回执。

|周期|原指标收盘启动|YOLO 同方向确认|
|---|---|---|
|15m / 30m / 1H / 4H|直接推送“指标启动 · 未经 YOLO 确认”|另发“YOLO 确认 · 等待 N 根”|

同根通过也保留两种独立消息。模型证明、权重、参数、p..p+9窗口与30分钟新鲜度均不变。
直接通知使用独立启用政策 `imacd-direct-start-notifications-v1`，原事件协议仍为 `imacd-tv-visible-start-monitor-v3`。
新政策首次启用前（含恰好启用时刻）的箭头不补推；旧 raw 政策启用时间不能授权新通知。
30m 新增独立的指标直接通知与模型确认周期启用时间；15m/1H/4H 及 Bark 通道既有启用时间保留，不补发启用前的30m箭头。5m历史启用时间仅留档，不再授权发送。
只有首次插入原事件时才能创建该阶段 outbox；已有记录不能因切换、补齐行情或重启重新入队。
初始化模型与直接通知政策使用加法，不互相退休对方 pending 消息。重启不重发历史或未知回执。
同一渠道中，原箭头仍在发送或等待限流重试时，模型确认让其先发送；原箭头已失败、未知、跳过或等待到过期则不阻挡新的确认。
发送失败不等于信号失败，未知回执不自动重试。Bark 的 sent 仅证明服务接受，不证明手机展示。
同一原箭头的确认与 Bark outbox 在一个SQLite事务内提交；不同原箭头即使同时确认也各有身份。
Telegram sender 与 model gate 的 Telegram 支路默认关闭，监控不启动 TG 线程、不读取 TG 凭据、不生成 TG 配图或新待发送项；启动测试入口也关闭。
启动时旧 TG pending 标记 skipped/telegram_disabled_by_owner，sent、failed、unknown 等历史回执保留。Bark 队列不受影响。
旧 Telegram 凭据不删除或修改；历史发送机制仅保留用于兼容和显式假发送测试，当前监控没有重新开启入口。
原始OHLC仍只保存在内存；模型队列单线程，按合约×周期合并等待输入，不阻塞公开行情扫描。

### Bark 手机打开方式（v1.8.1，2026-09-09）

新发送消息的 `url` 使用 `https://www.tradingview.com/chart/`，按 TradingView 官方
Universal Link 声明优先打开 iPhone App；精确到 OKX 币种及5m/15m/1H/4H的原网页链接放在正文“网页备用”。
官方 AASA 明确排除带非空 `symbol` 的 `/chart/` 和8字符布局ID链接，不能仅把网页地址换成未经验证的 scheme。
这里仅请求打开 App，不声称自动切换币种、周期或加载特定指标。Owner 随后实点确认 App 能打开，
但币种与周期未切换：当前 `url` 本来就未携带两者，不是已验证的参数丢失或转码错误。
尚未找到官方证实能同时定位币种和周期的 iOS 外部入口。Owner 提供的手机分享链接
`https://cn.tradingview.com/x/GpAAywbJ`、`https://cn.tradingview.com/x/wXnIZOgm` 已实读：
分别是 ETHUSDT.P 4H、LITUSDT.P 4H 的 PNG 快照页面。cn 与 www 的 AASA 规则相同，没有 `/x/*`。
页面源码只显示对应 `s3.tradingview.com/snapshots/...png`；官方 snapshot JS 的看图按钮
另建网页 `chart?symbol=...`，不传周期，未发现 iOS 精确定位入口。因此不能把截图ID改成chart/layout ID。
本次核对没有修改生产点击地址或重启服务；精确币种/周期的入口仍为通知正文“网页备用”。
不能把 AASA 排除某类 Universal Link 扩大成所有自定义 scheme / Siri 路径必然不支持的结论。
系统关联或用户默认打开偏好仍可能影响其他设备。点击“网页备用”仍是打开对应网页图表。

`copy` 字段为完整 `OKX:币种.P`，供系统通知长按展开后的“复制”按钮使用；
没有设置 `autoCopy`，普通点击不会自动复制。Bark 历史记录不保留该独立复制字段，历史菜单复制的是全文。
已收到的旧历史消息不会被更新；只影响新发出的指标启动与 YOLO 确认，未改事件身份、启用边界或重推历史。

依据：[TradingView AASA](https://www.tradingview.com/apple-app-site-association)、
[Bark 打开逻辑](https://github.com/Finb/Bark/blob/8c7973fa7a7791c766f70bfe044d6044e16a3045/Common/Client.swift#L88-L98)、
[通知复制按钮](https://github.com/Finb/Bark/blob/8c7973fa7a7791c766f70bfe044d6044e16a3045/notificationContentExtension/NotificationViewController.swift#L75-L95)。

API：`/api/signals` 默认仅本协议的 `yolo_confirmed`；`/api/signals?kind=tv_start` 按各周期直接通知启用边界返回15m/30m/1H/4H箭头；
`/api/candidates` 是原箭头及模型状态；
`/api/status` 的 `runtime.model_gate` 显示模型加载、错误、队列与候选计数。
`/api/health.ok` 同时要求行情扫描和模型就绪。正常预热可能约15分钟，不要反复重启。

## 延迟预算（不变更原30分钟数值）

|阶段|15m|30m|1H|4H|
|---|---:|---:|---:|---:|
|箭头至最晚可确认的K线|9×15m=135分钟|9×30m=270分钟|9小时|36小时|
|全轮结束后等待|120秒|120秒|120秒|120秒|
|全市场扫描、模型队列耗时|实时读scan.duration_seconds及model_gate.queue_depth；无固定保证|同左|同左|同左|
|单端点2图首次MPS实测|原15m约1.9秒，仅单实例冒烟，不代表全市场峰值|未经30m单独测速|未单独测速|未单独测速|
|确认收盘至允许推送|≤30分钟|≤30分钟|≤30分钟|≤30分钟|

第一行是允许等待模型的结构窗口，不是“原箭头仍新鲜”。模型通知入队、Bark发送前复核、前端新鲜徽章均使用确认收盘+30分钟。
15m/30m/1H/4H 直接阶段都没有模型等待，但仍须箭头收盘，再经历扫描和通知服务耗时；三个环节使用原箭头收盘+30分钟。
两个阶段的日期/价格分别展示，不把模型确认改写成更早的指标入场价。模型异常不阻断任何周期的直接阶段；健康页仍会如实报告模型异常。
超过新鲜度的真实确认可以保留为历史，但不能把检测完成的墙钟时间当成信号发生时间。
新登记候选须仍有可能落在9根等待+30分钟内；不为7天旧箭头补跑大批模型。
本机通知流独立，不更改VPS执行器原有30分钟门或15分钟脉冲。

## 验证与边界

```bash
.venv/bin/python -m pytest tests/monitor tests/boundaries -q
node --test tests/monitor/frontend_cards.test.cjs tests/monitor/frontend_theme.test.cjs
.venv/bin/python -m yoyo.monitor.manage status
```

覆盖两方向及四周期、同根取消、等待上限、错误重试、跨重启进度、同端点共享推理、独立渠道切换、过去不补发、未知回执不重试、未来扰动不改变模型输入、原绘图器像素一致。
测试用临时数据库及合成价格，网络发送被替身截断，不往真实通知队列塞测试信号。
收益/AUC/胜率等不适用于本次工程接入；没有新增收益验证，也不能因示例图形看起来好就声称模型已证明盈利。

模型来源为原15m研究权重；30m/1H/4H在当前版本按Owner授权复用同一冻结规则，并未重新训练或证明周期泛化收益。
原监控重启会用新的720根起点播种递归指标，边界附近结果可能轻微变化；本次只保证原箭头记录、检查游标和推送身份持久化，不声称重新播种后逐像素恒等。
同一模型核心可能关联不同原箭头，沿用原研究规则；本次没有另加跨箭头合并阈值。
Mac须保持开机联网，权限/外部通知服务失败在现有回执中如实显示。

## 同日初始止损诊断（14:46 固定快照）

Owner 觉得初始止损过窄，授权分析。本轮只检查现有规则，没有改变 Pine、ATR 下限或 TP/SL。
Pine V2.6 默认“信号收盘”参考价，long 取启动 K 的 low、short 取 high；正风险才画框，完全没有最小 ATR 距离。
研究 `altcoin_accounting.py` 却使用下一根开盘与 2×信号 ATR，因此两份 R 合同不一致。

|周期|候选|正风险可量化|零风险不画框|止损/价格中位|止损/ATR中位|既有0.2%成本/R中位|
|---|---:|---:|---:|---:|---:|---:|
|15m|158|141|17|0.270%|0.453|0.741R|
|1H|31|30|1|0.466%|0.436|0.430R|
|4H|5|5|0|1.134%|0.437|0.176R|

194 个原箭头均找回精确已收盘 K，核对 OHLC、方向、收盘价和时间；176 个正风险逐条独立重算通过。
不按之后涨跌筛选，不计算止损率或收益；18 个零风险仍保留在候选分母。5m 尚未启用时采样，不存在5m结果。
ATR来自当时本机图表的因果值，未独立重播其完整历史播种。4H仅5例且有巨大启动K，不能概括整个周期。
0.2%是既有研究成本假设，不是账户实际费率，也没有纳入真实成交、价差、滑点和资金费。
未来更改宜分别对照“原单根极值止损”“最小ATR风险距离”“已确认结构边界外缓冲”，固定信号和账户风险预算，逐项检验。
本诊断未选择任何最优 ATR 倍数，也没有证明放宽止损能提高净收益。
原始证据与 HTML：`output/qa/spike_stop_review_20260909/{diagnostic.json,diagnostic.csv,index.html}`。
