# 空账本参与拼接后要重新确认审计列类型

- **问题**：638 流中有仅含表头的空账本，拼接后 `censored` 与部分索引列变成 object。独立审计的位取反产生 -1/-2，后续数值比较也因 object 拒绝运行；没有发现成交数值差异。
- **死胡同**：单流 CSV 解析正确和非空合成样本通过，并不能证明全币池拼接后仍保持相同 dtype。只修复第一处掩码，不检查数值比较入口，会在下一道审计再次失败。
- **有效路径**：布尔掩码显式转 bool，共同成交的数值矩阵显式转 float，再按同样的经济恒等式和容差重跑。保留两次失败审计日志，收益回放与统计源结果没有重写。
- **通用规则**：空流是正常数据情况；在 CSV 合并边界定义和核对类型，不能靠 NumPy 或 pandas 隐式推断替代数据契约。
- **牵连**：`yoyo/evaluation/spike_v126_audit.py`；不改变任何过滤、收益、删失或成本规则。
