# 报告指令适配不能改写代码围栏

- **问题**：复用既有报告构建器时，版本指令转换会改写代码示例里的字面来源标记，破坏完整原文保留。
- **死胡同**：全局正则替换看起来只改版本名，却无法分辨真正的报告指令与围栏中的文字。
- **有效路径**：沿用既有围栏解析状态，只在围栏外转换独立指令行；反引号与波浪线两种围栏均加回归测试。
- **通用规则**：凡是承诺“不改正文”的适配层，必须用包含指令字面值的代码块测试文本恒等，不能只验证图表元数据。
- **牵连**：hourly_impulse_cadence_report.py、对应报告测试；此修复不涉及行情、参数或经济回测。
