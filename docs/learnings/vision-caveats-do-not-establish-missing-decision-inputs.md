# Model caveats must be checked against the actual decision contract

- **问题**：An ETHFI visual review listed missing volume, unreadable exact prices and limited right-edge candles, prompting the question whether the model needed more input or finer rules.
- **死胡同**：Adding indicators because a model mentions them, or treating token count as proof of sufficient evidence, would change the task without establishing why the current verdict occurred. No new volume gate or launch-distance threshold was adopted.
- **有效路径**：Inspect the saved request, exact image, criteria and snapshot separately. Exchange `84535481ee2b404eb90f2320f8db71c1` sent a 1440×800 chart with 120 candles, five references, time metadata and max reasoning (9,190 input tokens), but no numeric OHLC/MA table. Volume was not a required criterion. The snapshot showed the large down candle at 22:00–22:30, the source signal at 23:30 and observation at 23:33; the stated current-edge extension had visible support independent of the generic caveats.
- **通用规则**：Separate missing required evidence, optional context and output boilerplate. Define convergence, launch and extension with owner-reviewed boundary examples before setting numeric gates. Original-signal morphology and current observation state are different questions; later data must not retroactively justify the former. Any proposed detail image or numeric context must use the same frozen endpoint and supplied MA series, then be evaluated separately from prompt changes.
- **牵连**：`yoyo/vision_research/schemas.py::DEFAULT_CRITERIA`, `zhipu.py::_REVIEW_INSTRUCTIONS`, `server.py::current_review_context`; run `998015a6df7e416fb6e57604918484b7`. This was a read-only input/contract audit; runtime rules, references and recognition records were unchanged, and no extra provider inference was requested.
