# 周度选币必须按确认时钟切换，不能按信号K线开盘切换

- **问题**：周日最后一根在周一00:00确认并下一open入场，但回放按信号bar的open查名单，晚切了一根。
- **死胡同**：检查排名特征shift(1)就宣称整条选币链正确；特征无前视，并不保证名单应用的时钟正确。
- **有效路径**：排名用周一open前完整7日，名单应用查询signal open+TF，即decision close/entry open；同时修候选和随机对照。15m/1H/4H分别测试新入选与退出名单，保留旧结果并重新锁旧年候选后复评2026。
- **通用规则**：状态更新时钟、特征可见时钟、决策时钟要逐一对齐。非前视的延迟错误也会改变评估，不应以“没有未来函数”为理由忽略。
- **牵连**：`membership_mask`、周度名单、对照资格、holdout重复访问记录；本次只修实现时钟，没有根据审计收益改候选、阈值、风险或退出。
