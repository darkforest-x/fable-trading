# 规划"修复"之前先读代码——你要修的东西可能已经存在

- **问题**：交接文档里把"purged CV/embargo 泄漏修正"列为第一优先级改进，
  执行时读 `train.py` 才发现 `PURGE_WINDOW` 早已实现，两个切分边界都清除了。
- **死胡同**：凭领域知识推断"triple-barrier 标签必然有边界泄漏，需要修"——
  推断本身没错，错在没有先验证代码现状就写进了路线图和报告的风险声明。
  更早的信号被忽略了：实验输出里 train 结束时间和 val 开始时间之间有 ~18.5 小时
  的间隔，这正是 embargo 存在的证据，当时没有追问为什么。
- **有效路径**：执行前第一步是读实现（`grep -n "purge\|embargo\|PURGE" src/`），
  发现已实现后核对窗口数学（73 根 = entry 偏移 1 + horizon 72），确认正确，
  然后修正错误文档而不是写多余代码。
- **通用规则**：把任何"改进项"写进路线图之前，先花两分钟 grep 代码确认它真的缺失；
  切分边界出现无法解释的时间间隔时，第一假设是"已有 purge 逻辑"。
  发现文档写错了，修正要留痕（勘误标记），不要悄悄改掉。
- **牵连**：`src/judgment/train.py:54-66`、`analysis/p2b_v2_report.md` 第 5 节勘误、
  `HANDOFF.md` 第 1 步作废标记。
