# 模块测试通过不代表时间与分桶语义接上了

- **问题**：独立模块已有67项合成检查，但组合前仍发现匹配器把合法最低波动桶0当缺失，统计回退路径把已是决策收盘的signal_time再加4小时。
- **死胡同**：每个模块用自己的手工fixture能验证局部算法，却不会发现另一模块输出的是0至4而非1至5，或者时间已经完成open到close转换。空交易的object列还会让数值比较直接崩溃。
- **有效路径**：把真实输出契约带入匹配与统计回归；加入月末20点、桶0、全空交易，以及54个纯合成袖套跑完装载到manifest的整链测试。空配对不做浮点比较，限幅同分信号保留全部边界同分，避免输入顺序选top样本。
- **通用规则**：并行模块合并后先检查枚举边界、时间语义及空集，再运行无真实行情的整链反例；局部测试数量不能替代契约验证。
- **牵连**：`altseason_trend_engine.py`、`altseason_trend_statistics.py`、`altseason_trend_research.py`及对应tests。全部修复在首次真实回测前完成；不改变既有生产模块。
