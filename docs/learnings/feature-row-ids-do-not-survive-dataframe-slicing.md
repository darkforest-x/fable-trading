# 特征里的绝对行号不能直接索引切片后的数据

- **问题**：完整数据能找到形成期强动能，切成绘图区间后同一信号的历史证据静默变空。
- **死胡同**：将原始focus_start_i直接传给新DataFrame.iloc，默认两者仍共享行号原点。
- **有效路径**：按确认时刻截断后取near_zero_bars+1根，验证根数及小时连续性；缺历史明确拒绝。补测试将绝对行号设为9621，截断数据仍保留已知动能。
- **通用规则**：跨数据切片使用时间或稳定键；位置仅在明确同一原点内有效。空结果可能是对齐错误，不一定意味着条件未发生。
- **牵连**：`super_trend_candidate.evidence_at` 与前缀不变性测试；不改变指标、阈值或线上行为。
