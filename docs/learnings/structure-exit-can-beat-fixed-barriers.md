# 结构出场（收盘破 EMA21）可在 val 上显著短持仓并抬 top 净

- **问题**：固定 TP5/SL2 持仓偏长；H3 用形态自身定义脉冲结束是否经济上更好。
- **死胡同**：默认「无硬 TP/SL 的出场风险更大所以更差」——忽略了本池脉冲多为短促、长持仓被噪声吃掉的事实（与 H1 scaled 结论同向）。
- **有效路径**：同池对照只改标签函数；发现级门槛 = top 净@maker ≥ TP5 且 p<0.01。H3 通过（约 +0.51% vs +0.15%，均持仓 12 vs 20 根）。
- **通用规则**：出场结构实验优先「同入场、同特征、只换 labeler」；通过后仍是发现级，不自动替换冻结前向主线。
- **牵连**：`label_candidate_ma_exit`；`scripts/h3_ma_exit_sweep.py`；`analysis/p15_h3_ma_exit.md`；对比 H1 scaled
