# 只改变左侧上下文不会改变核心距右端的根数

- **问题**：Owner 在 v6 三窗口样本中发现所有正例框都靠近训练图倒数第五根。核对完整清单：1,841 个事件、5,523 张正图，pre7/post5、pre9/post5、pre11/post5 各1,841张；核心末根之后始终恰好5根 K 线。
- **死胡同**：为保留原确认时钟，仅改变左侧长度，却把它称为完整的三种位置增强。这会改变像素位置和 K 线密度，但没有打散核心距右端的 bar 距离。原图重绘哈希一致只证明复现，不证明增强设计满足 Owner 目标。
- **有效路径**：核对 manifest 的 pre/post 分布、builder 的 endpoint=core_end+5 与真实标签，确认这是裁剪策略造成的固定锚点，而不是审核红框绘制偏移。严格计数核心末根是训练图倒数第6根；其后有5根。未来审核图又额外显示40根，所以“后5根”只相对于蓝色训练截止线成立。
- **通用规则**：位置增强要分别检查像素位置、K线密度与目标距右端的根数。固定右端距离存在位置捷径风险，但是否学到捷径仍需独立回放验证。若改变窗口右端，必须逐视图重记可见时钟、检查标签成立时点与切分；不得把后文增加后的图当作更早可用的信号，也不得只移动框而不移动真实行情窗口。
- **牵连**：`yoyo/datasets/ma_morphology_threeview.py::render_views`、`datasets/ma_launch_owner1500_morph_v6_threeview_ready_20260925_v1/manifest.jsonl`、`yoyo/datasets/ma_morphology_future_review.py`。当前训练包技术准备完成不等于位置增强设计已验收；本次诊断不修改样本、不启动训练。
