# 回放终态测试必须真的走到每一种返回分支

- **问题**：V9新保护引擎裁剪为独立模块时漏掉了固定入场回放的期末返回。提前止损与反向退出测试通过，但1H真实前期数据出现期末持仓后，固定回放返回None，逐笔parity立即停止了实验。
- **死胡同**：把包含censored字段的parity断言称为“覆盖未平仓”，不代表测试样本真的存活到最后一根；在中途已平仓的fixture上检查相同字段，无法覆盖函数末尾。
- **有效路径**：补回boundary_mark的完整censored记录；构造价格始终不触保护、也没有反向信号的样本，分别开关新规则，强制串行与固定路径走到最后一根，断言非空记录、终态原因、价格和censored口径。失败运行目录保留，修复提交后完整重跑。
- **通用规则**：每一种终态应有一个明确迫使其发生的fixture。用路径触发条件和返回值形状验证覆盖，不以测试名或字段列表替代分支证据。
- **牵连**：`spike_v9_cost_be2_engine.replay_fixed_entry`、`tests/evaluation/test_spike_v9_cost_be2_engine.py`、实验`exp-spike-v9-cost-be2-20260915-v1`首次失败记录。
