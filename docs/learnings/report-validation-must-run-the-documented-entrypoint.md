# 报表验证必须运行交付的入口命令

- **问题**：UTF-8报表脚本能经compile和py_compile通过，但本机Python3.9.6直接执行脚本时报Non-UTF-8错误，报表尚未生成。
- **死胡同**：把编译检查通过等同于用户可运行；再次解码完整源字节仍成功，说明仅检查文本内容不能覆盖入口差异。
- **有效路径**：增加显式UTF-8编码声明后，直接运行文档给出的命令成功；再验证生成HTML中的全部86图切换。未证明解释器内部根因，不把现象扩大为普遍Python限制。
- **通用规则**：检查最终交付物之前，至少运行一次报告中的真实入口命令；编译或import检查只能作为补充。
- **牵连**：`scripts/report_spike_september_trade_audit.py`、本地Python3.9.6；本次修复不改变数据或交易结果。
