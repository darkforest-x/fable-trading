# 审核页的默认间距会把关键K线挤出可见区域

- **问题**：顶部说明、标签、无框选项逐行排列，图顶在338px；未来图底部758px，被624px起的固定提交栏挡住。
- **死胡同**：仅把控件排成一行、压缩Header后，图顶仍为218px，未来图底638px；原生标签组和Choices的上下各1em边距还在。只观察主图已完整会漏掉右侧未来图的遮挡。
- **有效路径**：删除重复说明，把控件放同一行，缩减Header间距，再限定于该控件行清除两个原生组margin。图顶190px，左右图底536/610px，均在624px提交栏上方；两图尺寸仍为597×346和725×420左右。
- **通用规则**：先量顶部占用与每张图的底部相对固定栏的位置；压缩外围布局优先于裁图或改变标注画布。保留Image属性、控件身份及选项值，并分别验证左右图的完整可见性。
- **牵连**：`configs/labelstudio/owner_unified_future40.xml`；LS内置Header/View/Style。真实1470×670浏览器窗口验证，XML语义对照与服务端校验通过；没有修改预框、答案或图片。
