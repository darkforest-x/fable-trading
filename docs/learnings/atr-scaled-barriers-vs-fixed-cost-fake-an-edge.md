# ATR 等比障碍 + 固定成本 = 假的"高波动 alpha";判据是毛 PF,不是净收益

**日期**:2026-07-26
**类型**:反直觉结论(验 short-only 链路判断层时发现)

## 一句话

障碍是 **TP5×ATR / SL2×ATR**(收益按 ATR 等比放大),成本却是**固定 0.2%**。
于是"只做高 ATR"必然让**净收益**变好——但那不是选出更好的交易,而是**下更大的注**。
实测高波动分位其实是**更差**的交易:**毛 PF 1.39→1.26、胜率 0.33→0.22 双双下降**。

## 怎么暴露的

1. 100 币 short 走查:5 折里 4 折 `best_iteration=1`(LGBM 只学一轮)、`rho_mean` 为负,
   top-decile 净值却为正 → **"判断层"其实退化成一个阈值**。
2. 把边写成显式规则对照(IT-17):唯一**5/5 折全正**的是 `atr_pct_HIGH`(均值 +0.54%@0.2%);
   而**正确早停**的 LGBM 只有 -0.03%,**还不如裸池 +0.13%**。
3. 矛盾点:`Spearman(atr_pct, realized_ret)` **5 折全负**(-0.20~-0.30)——高波动**更容易亏**,
   但取最高波动 10% **却净赚最多**。两者只能同时成立于"赔率放大"机制。
4. 决定性检验(IT-18):**毛 PF 是尺度不变的**,等比放大不会改变它。
   实测毛 PF **1.39 → 1.26 下降**,胜率 **0.330 → 0.220 下降**,
   而毛均值 +0.0018 → +0.0037 上升、净@0.2% -0.0002 → +0.0017 上升。
   → **ANTI-SELECTION**:高波动是更差的交易,净值变好纯粹是固定成本被稀释。

## 教训

- **要把两件事分开断,别混成一句话**(本文初稿就混了,已勘误):
  1. **不是预测能力**:毛 PF 与胜率随 ATR **下降** → 高波动本身是更差的交易。
  2. **但净收益的改善是真的**:障碍按 ATR 等比、成本按名义额固定 → 高波动的
     **赔付/成本比**更高。**净 PF 也随 ATR 上升**(0.2% 下 0.95→1.21)。
- **勘误(重要)**:初稿写"同样的净值提升可以用仓位翻倍平凡地得到"——**错**。
  **PF 是尺度不变量**:仓位翻倍等比放大盈亏与成本,净 PF 不变;按风险定仓
  (仓位 ∝ 1/ATR)时高 ATR 的成本占风险比反而更低。**这个好处扛得住风险归一化。**
  正确说法:**它不是 alpha,是成本效率**;代价是更低胜率、更大波动。
- **`best_iteration=1` 是红旗不是细节**:模型只学一轮 = "判断层"退化成单特征阈值。
  遇到就**把它写成显式规则重测**——本次正是这一步把假边拆穿的。
- **秩相关与 top-decile 净值矛盾时,别选对自己有利的那个报**;矛盾本身就是机制线索。

## 顺带发现(需 owner 决策,勿自行改)

同一批候选在 **maker 0.06%** 下,**各波动分位 PF 普遍 1.14–1.38**(不需要任何选择);
在 **0.2%** 下则普遍 ~1.0。**这条链路是否成立,几乎完全取决于成本假设**——
而成本假设按 CLAUDE.md 是 **owner 决策**,不得自行改动。要推进就先定成本口径。

## 相关

- 脚本:`scripts/it17_short_rule_vs_lgbm.py`(规则 vs LGBM)、
  `scripts/it18_atr_edge_mechanism.py`(毛 PF 分位判据)
- 产物:`analysis/output/it17_short_rule_vs_lgbm.json` / `it18_atr_edge_mechanism.json`
- [[full-mode-causality-is-behavioral-not-structural]](同一轮验收)
- [[dense-cluster-has-no-causally-tradeable-direction-edge]]
- 呼应 lab IT-06/IT-07:主导特征永远是波动率——**现在知道为什么了**。
