# 均线族迁移必须轮换整条证据链

- **问题**：检测层已用 SMA/EMA20/60/120，但判断候选、特征名、冻结模型、缓存和前向账本仍混有旧均线语义；只换扫描器会让新候选被旧模型评分，并把两种策略写进同一个 100 笔账本。
- **死胡同**：把实验模块导入到主线，或继续沿用 `close_vs_ema55` / `close_vs_ema200` 兼容列和旧 `forward_log.csv`。这些做法表面能运行，实际破坏模型可解释性、指纹追踪和前向验收独立性。
- **有效路径**：把 MA20/60/120 设为唯一候选实现；按真实 EMA60/120 重命名特征；从匹配数据重新冻结模型并切 ACTIVE；使分数缓存按新数据 SHA 失效；从模型生效后的下一根 bar 开新前向账本；旧工件只保留审计。
- **通用规则**：任何会改变候选分布或特征含义的策略迁移，都按“数据集 → 特征 schema → 模型 → 阈值 → 缓存 → 前向日志 → 看板”逐层轮换，禁止复用旧账本。
- **牵连**：`src/judgment/candidates.py`、`features.py`、`frozen.py`、`forward_types.py`、`models/ACTIVE`、`data/forward_log_ma206.csv`、看板 API 与项目状态文档。
