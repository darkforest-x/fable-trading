# 默认参考图必须保留框的语义和确认层级

- **问题**：Owner 要求复用以前的均线密集框作 Gemini 默认参考，但既有图库同时包含核心框、启动段框和事后回看画面。
- **死胡同**：选择最新图库或看到红框就叫密集区，会把启动 K 线和已发散均线混作收拢核心；整组认可也不等于逐个框已经人工确认。
- **有效路径**：先查原 manifest 的核心首末根、框的构成与来源，再看原图；复用已有像素与几何，记录 SHA 和确认层级。参考图明确为回看示例，不把框外后续走势或旧训练 split 当作本次标签资格。
- **通用规则**：默认范例是模型任务定义的一部分，必须核对语义。只在从未编辑的参考库初始化，不能用默认值覆盖用户删改。
- **牵连**：`yoyo/vision_research/defaults.py`、实验 `default_references/manifest.json`、`tests/vision_research/test_defaults.py`；训练和生产资格仍为 false。
