# 包含式接口还要验证服务器实际时间分辨率

- **问题**：上一条inclusive-api-cursors-need-strict-progress笔记中的减1毫秒方案，在实际Rubik保留边缘仍返回原边界记录。
- **死胡同**：数学上end<oldest并不保证服务器按该精度比较。HBAR冻结响应请求1768161599999仍返回1768161600000；不能只凭mock宣布修复成功。
- **有效路径**：额外真实只读探针：减1000毫秒和减完整4H均返回空页。实现选择前一个完整周期格点；已要求返回时间落在周期格点，因此不会跳过合法中间bar。Funding接口保持独立after语义。
- **通用规则**：修接口分页需要至少一个真实保留边缘探针。数字参数的格式精度不等于服务器比较精度；以允许的采样格点推进更明确。
- **牵连**：yoyo/data/okx_altcoin_snapshot.py；旧原始页和先前失败coverage保留，重新采集是工程修复，不是重选研究阈值。
