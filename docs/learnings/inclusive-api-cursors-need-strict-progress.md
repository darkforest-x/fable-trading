# 历史接口的包含式游标必须严格越过末行

- **问题**：OKX Rubik OI/taker 到1440条保留边缘时重复返回同一最旧记录，被采集器报作无法后退。
- **死胡同**：将返回的最小ts原样作为下一页end。这个游标在普通页可去重推进，但最后一条会无限重复；mock没有覆盖保留边缘。
- **有效路径**：实际冻结响应显示end包含边界。Rubik下一页用oldest-1毫秒，资金费率after仍用exclusive原值；空页是历史耗尽，仍如实报告缺失，不伪装完整覆盖。
- **通用规则**：分页测试同时覆盖普通重叠页和单记录保留边缘，按每个接口实际包含/排除语义推进。
- **牵连**：yoyo/data/okx_altcoin_snapshot.py；原始成功页保留不覆盖，source_fetched_at与缺口统计留证。
