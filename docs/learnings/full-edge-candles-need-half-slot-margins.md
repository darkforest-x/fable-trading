# 最右核心必须完整显示，不能靠增加未来 K 线补边距

- **问题**：低延迟图把核心放近右端后，旧渲染器把最后一根中心放到画布边缘，实体及原始框伸出画布；后 0 根的 10 个正例都触发。只看标签中心合法会漏掉。
- **死胡同**：截掉框改变了原始边界；额外加真实未来 K 线虽能把框放进画面，却增加等待。随类别加空白还可能给模型位置提示。
- **有效路径**：统一按整根 K 线槽布局，左右各保留半槽，所有类别、时点和 split 同用。原 TXT 先还原到 bar/price 坐标，再投影，保证实体和完整框不越界。小批量实际图目测通过；冻结物理框后扰动各截止之后全部价格，当前 PNG、框与时钟保持不变。
- **通用规则**：数据窗口右端决定延迟；画布边距解决显示完整性，两者必须独立。每事件最多三种左侧上下文共享该案例最早符合既定可见证据的时点，不能为了增强而增加等待。
- **牵连**：`ma_morphology_early_dataset.py`；`test_ma_morphology_early_dataset.py`；2026-09-26 Owner明确“差不多了吧，去训练吧”，授权本次研究重训。早期标签仍是规则候选，不是逐样本金标；现有负例额外经过仅使用当前可见信息的密集启动否决筛查，旧 Grade-A 密集阈值不变，ATR只用核心结束时已知值，不借用未显示的 core+2。训练授权不包含模型激活、通知切换或盈利结论。
