# 过滤好的审核队列，还需要默认按钮真正进入它

- **问题**：Label Studio同项目保留旧2513题和新1043题，筛选页设置正确，但Owner看到总数3556，点默认Label All后混入旧题。当前项目选择HL2预测版本，旧未审题因此可能没有预框。
- **死胡同**：只在说明里要求每次打开下拉菜单选AsDisplayed，或以为URL带tab44和labeling=1就会限定队列。LS1.13.1主按钮会把持久化模式重新设为all；空模型版本也不能在next-task接口自动混用新旧预框。
- **有效路径**：项目和标签直接显示本周1043题；限定项目77/tab44的列表主按钮转发到真实AsDisplayed按钮，保留原生过滤与启动行为。找不到唯一目标就停止，不回退全量。旧已打开的编辑页、答案和草稿不自动跳转或覆盖；旧无目标答案也不能统一补框。
- **通用规则**：验证列表过滤之外，还要点击用户默认会点的主入口，确认实际首题来源与预框。让常用操作正确，比要求用户记住异常菜单路径可靠。
- **牵连**：`yoyo/review/label_studio_queue_entry.js`、`install_label_studio_queue_entry.py`、原右键提交补丁与Label Studio项目77/view44。双补丁按安装逆序恢复，保留完整原包和收据。
