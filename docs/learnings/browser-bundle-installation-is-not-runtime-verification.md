# 脚本已安装，不代表浏览器已经运行新脚本

- **问题**：审核入口补丁的磁盘与HTTP响应SHA一致，但实际点击仍进入旧队列；浏览器中的按钮也没有新提示。
- **死胡同**：仅凭安装收据、普通reload或已发送强刷按键宣布部署成功。浏览器性能记录显示脚本仍来自缓存，解码长度1262058，实际新版为1266839；检查URL发现仍含tab44，不能把问题归因于丢失筛选参数。
- **有效路径**：从真实页面主执行环境读取已加载资源长度和补丁版本，确认旧右键补丁存在、新入口补丁缺失。Page.reload(ignoreCache=true)后新版本出现；实际点击默认按钮进入HL2任务37277，模式filtered、预框与未来图可见，未提交答案。
- **通用规则**：本地服务静态包改动要分别验证磁盘字节、HTTP响应、浏览器执行版本和用户路径。入口测试失败时先确认运行的代码，再修改正确源码。
- **牵连**：`yoyo/review/install_label_studio_queue_entry.py`、Label Studio现有静态资源版本URL、3060浏览器首次更新需Ctrl+F5；不要刷新用户有未保存修改的旧页。
