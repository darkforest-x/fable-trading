# 历史箭头还在而盈亏框消失，先查对象保留策略

- **问题**：V5 向前回看时，仅最近几次参考有完整盈亏框，较旧箭头却仍存在。
- **死胡同**：把这种差异当成信号重绘或重新放宽检测门；只增加平台对象上限也不能修复 rrKeep=8 的主动删除。分别管理不限量标签还会让平台先回收标签，留下半组图形。
- **有效路径**：区分事件产生、单参考归属、对象淘汰三个层次。保持前两者，用一个绘图组拥有两框、价格标签及全部里程碑；预算内先删整组再分配新组，确认和退出标签另设有界队列。
- **通用规则**：历史视觉缺项先跟踪对象的创建条件、主动删除和平台GC；承诺保留多少组时须计算每组最坏对象数及瞬时分配上限。不要把“无限箭头”描述成“无限完整历史图形”。
- **牵连**：yoyo/evaluation/pine/spike_burst_v5.pine；rrKeep；max_labels_count/max_boxes_count/max_lines_count。本轮60组最多120框、180线、480标签，保留信号与风险数学。
