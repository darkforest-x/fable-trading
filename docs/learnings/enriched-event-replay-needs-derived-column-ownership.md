# 前缀回放必须明确派生列归属

- **问题**：V36 首次真实前缀验证在收益计算前中止：已加 `flow_defined` 的请求再次与新计算结果合并，pandas 生成 `_x/_y` 后缀，后续字段不存在。
- **死胡同**：合成测试只传原始请求，漏测了真实 runner 会把已增强请求交回同一个转换器。跳过前缀验收会把这条错误路径隐藏掉。
- **有效路径**：转换器明确重算并替换自己拥有的派生列，保留身份与时钟；加入损坏旧派生值后的幂等重算测试。修复和提交先于重跑；首轮未生成收益、未写产物、未调整假设。
- **通用规则**：有 merge 的增强器至少测试原始输入和已增强输入两种调用；不要让默认后缀成为隐式契约。
- **牵连**：`yoyo/evaluation/owner_k1k2_genuine_flow.py::gate_events`、V36 前缀审计与合成测试。
