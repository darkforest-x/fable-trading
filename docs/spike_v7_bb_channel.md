# SPIKE V7 · BB 压缩通道

交付时间：2026-09-12。基于 `yoyo/evaluation/pine/spike_burst_v6.pine`，新增独立文件 `yoyo/evaluation/pine/spike_burst_v7.pine`。

## 显示规则

- 主图：SMA200 ± 2 倍总体标准差的细上下轨；中线默认隐藏。
- 带宽为 `(上轨 − 下轨) / abs(中线)`；带宽不高于此前 500 根带宽的第 10 百分位时，以淡金色填充通道。
- 通道本身等待 200 根连续有效数据；压缩分类额外等待 500 根带宽历史，缺口后重新预热。
- 可设置通道长度、标准差倍数、颜色、中线，以及仅显示压缩段。
- BB 仅负责显示。没有 RSI、ChartArt 买卖订单、额外箭头、标签、警报、K 线或背景染色。

V6 的多空确认、止损、趋势保护、反向退出、风险框、R 里程碑及副图保持原实现。BB 默认值来自所提供的通道和本轮研究定义，不宣称最优参数。

## 验证

1. 去除三个 `V7 BB VISUAL` 区块并归一化版本显示名称后，V7 与 V6 的非空、非注释代码逐行一致。
2. V6 SHA256：`b3147f1357dc210a7b19d839a9f72163e29f1ae4e58cc299fa942c38c402fe07`。
3. V7 SHA256：`f02893c55749f3f4e57fe6f1c49296eeabb8180e966d96bd883f0f30d133fb52`。
4. Mac TradingView 原生 Pine 编辑器于 09:32:16（北京时间）显示“SPIKE V7 · BB 压缩通道”已保存；09:32:38 编译日志显示“完成”。
5. 添加到当前图表触发 Basic 指标数量上限提示。日志虽显示“已添加到图表”，图表工具栏没有 V7，因此不将该日志当作成功上图证据。原图 SPIKE BURST / SPIKE V6 / SPIKE V1+ 保留。完整图表视觉验收尚未完成。

本次是显示功能交付，不包含新的收益实验、模型训练、监控部署或通知变更。AUC、胜率与收益比较不适用于本次代码验证；对应的严格对照为去除新增显示代码后与 V6 完全一致。

## 使用

在 TradingView“我的脚本”中选择 **SPIKE V7 · BB 压缩通道**。需先为新指标留出可用图表名额。脚本已独立保存，原 V6 没有被覆盖。
